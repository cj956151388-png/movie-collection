(() => {
  const APP_NOW = new Date();
  const CURRENT_MONTH = `${APP_NOW.getFullYear()}-${String(APP_NOW.getMonth()+1).padStart(2,'0')}`;
  const NOW_YEAR = APP_NOW.getFullYear();
  const { uid, safeJSON, splitList, esc, escAttr, localToday, csvEscape } = window.CineverseCoreUtils;
  const { DEFAULT_SETTINGS, THEME_PRESETS, EMPTY_HOME } = window.CineverseAppConstants;
  const { normalizeSettings, normalizeMovie, restore:restoreState, backup:backupState } = window.CineverseState;
  const Domain = window.CineverseDomain;
  const { uniq, uniqBy, mediaTypeLabel, mediaTypeIcon, posterHue } = Domain;
  const { app:V2_KEY } = window.CineverseState.keys;
  // Public test build: TMDb credentials are never hard-coded; each browser stores its own key locally.

  const VERIFIED_QUOTE_LIBRARY=window.CineverseVerifiedQuotes||[];
  const QUOTE_LAST_KEY='movie-collection-v2-last-quote';


  const appStore=window.CineverseState.createStore();
  const stateGateway=window.CineverseState.createGateway(appStore);
  window.CineverseStateGateway=stateGateway;
  let appState=appStore.getState();
  appState.settings=normalizeSettings(appState.settings);
  appState.tmdbMatchCenter={lastScanAt:'',lastTotal:0,lastAuto:0,lastManual:0,rows:[],resumeIds:[],paused:false,...(appState.tmdbMatchCenter||{})};
  if(!Array.isArray(appState.tmdbMatchCenter.rows))appState.tmdbMatchCenter.rows=[];if(!Array.isArray(appState.tmdbMatchCenter.resumeIds))appState.tmdbMatchCenter.resumeIds=[];
  appState.home=appState.home||{};
  if(Array.isArray(appState.home.radar))appState.home.radar=appState.home.radar.filter(r=>!String(r?.id||'').startsWith('radar-demo-'));
  appState.home.random=[];
  stateGateway.replace(appState,{persist:true,silent:true,source:'app-main',reason:'startup-normalize'});
  function save(){stateGateway.replace(appState,{persist:true,silent:true,source:'app-main',reason:'app-save'});if(typeof updateSettingsStorage==='function'&&els?.settingsView&&!els.settingsView.classList.contains('hidden'))updateSettingsStorage()}


  const $=id=>document.getElementById(id);
  const els={
    profileAvatar:$('profileAvatar'),profileName:$('profileName'),profileRole:$('profileRole'),homeGreetingName:$('homeGreetingName'),yearWatched:$('yearWatched'),yearCompareNote:$('yearCompareNote'),wantCount:$('wantCount'),planCount:$('planCount'),planDone:$('planDone'),totalCount:$('totalCount'),planTopDone:$('planTopDone'),planTopAll:$('planTopAll'),planPercent:$('planPercent'),planBar:$('planBar'),homeHeroYear:$('homeHeroYear'),homePlanMonthLabel:$('homePlanMonthLabel'),radarGrid:$('radarGrid'),recentGrid:$('recentGrid'),recentSeeAll:$('recentSeeAll'),planList:$('planList'),randomPoster:$('randomPoster'),randomTitle:$('randomTitle'),randomMeta:$('randomMeta'),randomQuote:$('randomQuote'),reroll:$('reroll'),watchTonight:$('watchTonight'),monthRadarCount:$('monthRadarCount'),monthNinePlus:$('monthNinePlus'),monthTopDirector:$('monthTopDirector'),quickAdd:$('quickAdd'),globalSearch:$('globalSearch'),toast:$('toast'),sidebarQuoteCard:$('sidebarQuoteCard'),sidebarQuoteText:$('sidebarQuoteText'),sidebarQuoteCredit:$('sidebarQuoteCredit'),quoteNextBtn:$('quoteNextBtn'),
    homeView:$('homeView'),libraryView:$('libraryView'),detailView:$('detailView'),libraryGrid:$('libraryGrid'),libraryEmpty:$('libraryEmpty'),libraryPagination:$('libraryPagination'),libKeyword:$('libKeyword'),libMediaType:$('libMediaType'),libYear:$('libYear'),libYearOptions:$('libYearOptions'),libDirector:$('libDirector'),libDirectorOptions:$('libDirectorOptions'),libCountry:$('libCountry'),libCountryOptions:$('libCountryOptions'),libStatus:$('libStatus'),libRatingOp:$('libRatingOp'),libRatingScore:$('libRatingScore'),libTag:$('libTag'),libTagOptions:$('libTagOptions'),libPlan:$('libPlan'),libPlanOptions:$('libPlanOptions'),libSort:$('libSort'),libShownText:$('libShownText'),libPageText:$('libPageText'),libFirstPage:$('libFirstPage'),libPrevPage:$('libPrevPage'),libPageNumbers:$('libPageNumbers'),libNextPage:$('libNextPage'),libLastPage:$('libLastPage'),libTotal:$('libTotal'),libWatched:$('libWatched'),libWant:$('libWant'),libPlanned:$('libPlanned'),cardViewBtn:$('cardViewBtn'),listViewBtn:$('listViewBtn'),multiBtn:$('multiBtn'),clearLibFilters:$('clearLibFilters'),bulkAddTitlesBtn:$('bulkAddTitlesBtn'),bulkAddTitlesDialog:$('bulkAddTitlesDialog'),bulkAddTitlesClose:$('bulkAddTitlesClose'),bulkAddTitlesCancel:$('bulkAddTitlesCancel'),bulkAddTitlesText:$('bulkAddTitlesText'),bulkAddTitlesStatus:$('bulkAddTitlesStatus'),bulkAddTitlesOnly:$('bulkAddTitlesOnly'),bulkAddTitlesMatch:$('bulkAddTitlesMatch'),addMovieBtn:$('addMovieBtn'),batchTmdbBtn:$('batchTmdbBtn'),batchTmdbInline:$('batchTmdbInline'),batchTmdbInlineText:$('batchTmdbInlineText'),batchTmdbInlineBar:$('batchTmdbInlineBar'),batchBar:$('batchBar'),batchCount:$('batchCount'),batchSelectShown:$('batchSelectShown'),batchPlan:$('batchPlan'),batchCancel:$('batchCancel'),
    batchTmdbDialog:$('batchTmdbDialog'),batchTmdbClose:$('batchTmdbClose'),batchTmdbStop:$('batchTmdbStop'),batchTmdbDone:$('batchTmdbDone'),batchTmdbProgressText:$('batchTmdbProgressText'),batchTmdbProgressPct:$('batchTmdbProgressPct'),batchTmdbProgressBar:$('batchTmdbProgressBar'),batchTmdbSummary:$('batchTmdbSummary'),batchTmdbPending:$('batchTmdbPending'),movieModal:$('movieModal'),movieForm:$('movieForm'),movieModalTitle:$('movieModalTitle'),movieModalClose:$('movieModalClose'),movieCancelBtn:$('movieCancelBtn'),movieDeleteBtn:$('movieDeleteBtn'),movieMediaTypeInput:$('movieMediaTypeInput'),tmdbMovieQuery:$('tmdbMovieQuery'),tmdbMovieSearchBtn:$('tmdbMovieSearchBtn'),tmdbMovieSearchState:$('tmdbMovieSearchState'),tmdbMovieResults:$('tmdbMovieResults'),movieTitleInput:$('movieTitleInput'),movieOriginalTitleInput:$('movieOriginalTitleInput'),movieYearInput:$('movieYearInput'),movieReleaseDateInput:$('movieReleaseDateInput'),movieReleaseDateLabel:$('movieReleaseDateLabel'),movieLastAirDateInput:$('movieLastAirDateInput'),movieRuntimeInput:$('movieRuntimeInput'),movieRuntimeLabel:$('movieRuntimeLabel'),movieSeasonsInput:$('movieSeasonsInput'),movieEpisodesInput:$('movieEpisodesInput'),movieTvStatusInput:$('movieTvStatusInput'),movieTmdbIdInput:$('movieTmdbIdInput'),movieDirectorInput:$('movieDirectorInput'),movieCreatorInputLabel:$('movieCreatorInputLabel'),movieCountryInput:$('movieCountryInput'),movieGenresInput:$('movieGenresInput'),movieStatusInput:$('movieStatusInput'),movieRatingInput:$('movieRatingInput'),movieTagsInput:$('movieTagsInput'),movieOverviewInput:$('movieOverviewInput'),moviePosterInput:$('moviePosterInput'),
    detailBack:$('detailBack'),detailPoster:$('detailPoster'),detailPosterTitle:$('detailPosterTitle'),detailTitle:$('detailTitle'),detailFavorite:$('detailFavorite'),detailOriginal:$('detailOriginal'),detailCreatorLabel:$('detailCreatorLabel'),detailDirectors:$('detailDirectors'),detailCountries:$('detailCountries'),detailGenres:$('detailGenres'),detailRuntimeLabel:$('detailRuntimeLabel'),detailRuntime:$('detailRuntime'),detailReleaseLabel:$('detailReleaseLabel'),detailRelease:$('detailRelease'),detailMediaType:$('detailMediaType'),detailSeriesFact:$('detailSeriesFact'),detailSeriesMeta:$('detailSeriesMeta'),detailTmdb:$('detailTmdb'),detailStatusBtn:$('detailStatusBtn'),detailPlanBtn:$('detailPlanBtn'),detailEditBtn:$('detailEditBtn'),detailAddWatchBtn:$('detailAddWatchBtn'),detailAddWatchBtn2:$('detailAddWatchBtn2'),detailOverview:$('detailOverview'),detailTags:$('detailTags'),detailRating:$('detailRating'),detailStars:$('detailStars'),detailLastWatch:$('detailLastWatch'),detailPlanMeta:$('detailPlanMeta'),detailRadarBadge:$('detailRadarBadge'),detailRadarDate:$('detailRadarDate'),detailPublicScore:$('detailPublicScore'),detailMatchScore:$('detailMatchScore'),detailRadarReason:$('detailRadarReason'),detailWatchList:$('detailWatchList'),detailWatchCount:$('detailWatchCount'),detailFirstWatch:$('detailFirstWatch'),detailTotalMinutes:$('detailTotalMinutes'),detailTrend:$('detailTrend'),detailRelated:$('detailRelated'),
    quickWatchDialog:$('quickWatchDialog'),quickWatchSearch:$('quickWatchSearch'),quickWatchList:$('quickWatchList'),quickWatchClose:$('quickWatchClose'),quickWatchCancel:$('quickWatchCancel'),watchModal:$('watchModal'),watchForm:$('watchForm'),watchModalClose:$('watchModalClose'),watchCancelBtn:$('watchCancelBtn'),watchDateInput:$('watchDateInput'),watchRatingInput:$('watchRatingInput'),watchVenueInput:$('watchVenueInput'),watchNoteInput:$('watchNoteInput'),detailPlanDialog:$('detailPlanDialog'),detailPlanForm:$('detailPlanForm'),detailPlanDate:$('detailPlanDate'),detailPlanClose:$('detailPlanClose'),detailPlanCancel:$('detailPlanCancel'),detailPlanTitle:$('detailPlanTitle'),detailPlanCurrent:$('detailPlanCurrent'),detailPlanClear:$('detailPlanClear'),detailPlanSave:$('detailPlanSave'),
    planView:$('planView'),planPrevMonth:$('planPrevMonth'),planNextMonth:$('planNextMonth'),planToday:$('planToday'),planPageMonthLabel:$('planPageMonthLabel'),planAddMovie:$('planAddMovie'),planPageTotal:$('planPageTotal'),planPageDone:$('planPageDone'),planPagePercent:$('planPagePercent'),planPageMinutes:$('planPageMinutes'),planPageSummary:$('planPageSummary'),planCalendarBtn:$('planCalendarBtn'),planListBtn:$('planListBtn'),planCalendarWrap:$('planCalendarWrap'),planCalendarTitle:$('planCalendarTitle'),planCalendar:$('planCalendar'),planSide:$('planSide'),planRing:$('planRing'),planRingPercent:$('planRingPercent'),planProgressCopy:$('planProgressCopy'),planStatusBars:$('planStatusBars'),planUnscheduledCount:$('planUnscheduledCount'),planUnscheduled:$('planUnscheduled'),planListPanel:$('planListPanel'),planPageList:$('planPageList'),
    planMovieModal:$('planMovieModal'),planMovieForm:$('planMovieForm'),planMovieModalClose:$('planMovieModalClose'),planMovieCancel:$('planMovieCancel'),planMovieSelect:$('planMovieSelect'),planMovieMonth:$('planMovieMonth'),planMovieDate:$('planMovieDate'),
    watchedView:$('watchedView'),watchedYearFilms:$('watchedYearFilms'),watchedYearSessions:$('watchedYearSessions'),watchedSeasonMarks:$('watchedSeasonMarks'),watchedYearMinutes:$('watchedYearMinutes'),watchedAvgRating:$('watchedAvgRating'),watchedRewatchCount:$('watchedRewatchCount'),watchedKeyword:$('watchedKeyword'),watchedYear:$('watchedYear'),watchedRating:$('watchedRating'),watchedDirector:$('watchedDirector'),watchedCountry:$('watchedCountry'),watchedSort:$('watchedSort'),watchedShownText:$('watchedShownText'),watchedFeed:$('watchedFeed'),watchedPageText:$('watchedPageText'),watchedPrev:$('watchedPrev'),watchedNext:$('watchedNext'),watchedMonthlyBars:$('watchedMonthlyBars'),watchedRatingDist:$('watchedRatingDist'),watchedRewatchList:$('watchedRewatchList'),watchedThisYear:$('watchedThisYear'),watchedRecordBtn:$('watchedRecordBtn'),
    statsView:$('statsView'),statsYear:$('statsYear'),statsPaceTitle:$('statsPaceTitle'),statsPaceLegend:$('statsPaceLegend'),statsInsightsTitle:$('statsInsightsTitle'),statsPreferenceNote:$('statsPreferenceNote'),statsTopTitle:$('statsTopTitle'),statsTopNote:$('statsTopNote'),statsSummaryTitle:$('statsSummaryTitle'),statsMovieCount:$('statsMovieCount'),statsTvCount:$('statsTvCount'),statsCopySummary:$('statsCopySummary'),statsFilmCount:$('statsFilmCount'),statsSessionCount:$('statsSessionCount'),statsSeasonMarks:$('statsSeasonMarks'),statsTotalHours:$('statsTotalHours'),statsTotalHoursNote:$('statsTotalHoursNote'),statsAvgRating:$('statsAvgRating'),statsNinePlus:$('statsNinePlus'),statsRewatch:$('statsRewatch'),statsYearCompare:$('statsYearCompare'),statsMonthlyChart:$('statsMonthlyChart'),statsRatedCount:$('statsRatedCount'),statsRatingDonut:$('statsRatingDonut'),statsDonutAvg:$('statsDonutAvg'),statsRatingLegend:$('statsRatingLegend'),statsInsights:$('statsInsights'),statsDirectors:$('statsDirectors'),statsCountries:$('statsCountries'),statsGenres:$('statsGenres'),statsTags:$('statsTags'),statsTopFilms:$('statsTopFilms'),statsSummaryCopy:$('statsSummaryCopy'),
    matchView:$('matchView'),matchLastScan:$('matchLastScan'),matchStartScan:$('matchStartScan'),matchRetryProblems:$('matchRetryProblems'),matchResetSkipped:$('matchResetSkipped'),matchDedupSeasons:$('matchDedupSeasons'),matchPauseResume:$('matchPauseResume'),matchProgress:$('matchProgress'),matchProgressText:$('matchProgressText'),matchProgressPct:$('matchProgressPct'),matchProgressBar:$('matchProgressBar'),matchStatTotal:$('matchStatTotal'),matchStatAuto:$('matchStatAuto'),matchStatPending:$('matchStatPending'),matchStatNoResult:$('matchStatNoResult'),matchStatFailed:$('matchStatFailed'),matchTabs:$('matchTabs'),matchKeyword:$('matchKeyword'),matchList:$('matchList'),
    radarView:$('radarView'),radarTotal:$('radarTotal'),radarStrong:$('radarStrong'),radarHigh:$('radarHigh'),radarCollected:$('radarCollected'),radarKeyword:$('radarKeyword'),radarMatch:$('radarMatch'),radarSort:$('radarSort'),radarPageGrid:$('radarPageGrid'),radarEmpty:$('radarEmpty'),radarTasteTags:$('radarTasteTags'),radarPulseCopy:$('radarPulseCopy'),radarPulseBar:$('radarPulseBar'),radarSignalList:$('radarSignalList'),radarAutoUpdateBtn:$('radarAutoUpdateBtn'),radarImportBtn:$('radarImportBtn'),radarGoLibrary:$('radarGoLibrary'),radarImportDialog:$('radarImportDialog'),radarImportForm:$('radarImportForm'),radarImportClose:$('radarImportClose'),radarImportCancel:$('radarImportCancel'),radarImportText:$('radarImportText'),
    sidebarBrandTitle:$('sidebarBrandTitle'),sidebarBrandSubtitle:$('sidebarBrandSubtitle'),settingsView:$('settingsView'),settingsProfileName:$('settingsProfileName'),settingsProfileRole:$('settingsProfileRole'),settingsProfileAvatarFile:$('settingsProfileAvatarFile'),settingsProfileAvatarClear:$('settingsProfileAvatarClear'),settingsProfileAvatarPreview:$('settingsProfileAvatarPreview'),settingsProfilePreviewName:$('settingsProfilePreviewName'),settingsProfilePreviewRole:$('settingsProfilePreviewRole'),settingsSaveProfile:$('settingsSaveProfile'),themePresets:$('themePresets'),settingsWallpaperFile:$('settingsWallpaperFile'),settingsWallpaperClear:$('settingsWallpaperClear'),settingsWallpaperPreview:$('settingsWallpaperPreview'),settingsSaveAppearance:$('settingsSaveAppearance'),settingsResetAppearance:$('settingsResetAppearance'),settingsTmdbLanguage:$('settingsTmdbLanguage'),settingsTmdbStatus:$('settingsTmdbStatus'),settingsTestTmdb:$('settingsTestTmdb'),settingsSelfTestTmdb:$('settingsSelfTestTmdb'),settingsExportJson:$('settingsExportJson'),settingsImportJson:$('settingsImportJson'),settingsExportCsv:$('settingsExportCsv'),settingsImportCsv:$('settingsImportCsv'),settingsDoubanMode:$('settingsDoubanMode'),settingsDoubanAutoTmdb:$('settingsDoubanAutoTmdb'),settingsDoubanSyncWatchDate:$('settingsDoubanSyncWatchDate'),settingsDoubanDrop:$('settingsDoubanDrop'),settingsDoubanFiles:$('settingsDoubanFiles'),settingsDoubanFileList:$('settingsDoubanFileList'),settingsDoubanLast:$('settingsDoubanLast'),settingsDoubanStatus:$('settingsDoubanStatus'),settingsDoubanImport:$('settingsDoubanImport'),settingsDoubanClear:$('settingsDoubanClear'),settingsStorageCopy:$('settingsStorageCopy'),imageCropDialog:$('imageCropDialog'),imageCropTitle:$('imageCropTitle'),imageCropStage:$('imageCropStage'),imageCropImage:$('imageCropImage'),imageCropZoom:$('imageCropZoom'),imageCropReset:$('imageCropReset'),imageCropClose:$('imageCropClose'),imageCropCancel:$('imageCropCancel'),imageCropApply:$('imageCropApply')
  };

  const libraryState={view:'card',multi:false,selected:new Set(),editingId:null,page:1,pageSize:36,exclude:{year:false,director:false,country:false,tag:false,plan:false}};
  const batchTmdbState={running:false,stop:false,pending:[],total:0,processed:0,completed:0,skipped:0,failed:0};
  const matchCenterState={running:false,stop:false,processed:0,total:0,auto:0,tab:'pending'};
  const radarState={tab:'week'};
  const detailState={movieId:null,detachedMovie:null,planMonth:null};
  const planState={month:CURRENT_MONTH,view:'calendar',prefillDate:''};
  const watchedState={page:1,pageSize:12};
  const statsState={year:String(NOW_YEAR)};
  function toastMsg(text){els.toast.textContent=text;els.toast.classList.add('show');clearTimeout(els.toast._t);els.toast._t=setTimeout(()=>els.toast.classList.remove('show'),1800)}
  const quoteState={current:null};
  function quoteCredit(q){if(!q)return '— 光影格言库';if(q.type==='movie_line')return `— 《${q.film||'未知影片'}》${q.character?` · ${q.character}`:''}`;return `— ${q.author||'未知影人'}`}
  function chooseVerifiedQuote({avoidCurrent=true}={}){
    const lastId=localStorage.getItem(QUOTE_LAST_KEY)||'';
    const currentId=avoidCurrent?quoteState.current?.id||'':'';
    let pool=VERIFIED_QUOTE_LIBRARY.filter(q=>q.id!==lastId&&q.id!==currentId);
    if(!pool.length)pool=VERIFIED_QUOTE_LIBRARY.filter(q=>q.id!==currentId);
    if(!pool.length)pool=VERIFIED_QUOTE_LIBRARY;
    const q=pool[Math.floor(Math.random()*pool.length)]||null;
    if(q){quoteState.current=q;localStorage.setItem(QUOTE_LAST_KEY,q.id);renderSidebarQuote(q)}
    return q;
  }
  function renderSidebarQuote(q){if(!q)return;if(els.sidebarQuoteText)els.sidebarQuoteText.textContent=`“${q.textZh}”`;if(els.sidebarQuoteCredit)els.sidebarQuoteCredit.textContent=quoteCredit(q)}
  function showQuoteSource(q=quoteState.current){if(!q||!els.quoteSourceDialog)return;els.quoteSourceText.textContent=`“${q.textZh}”`;els.quoteSourceCredit.textContent=quoteCredit(q);els.quoteSourceVerification.textContent=q.verification==='A'?'A · 强核验':'B · 剧本 / 权威转录核验';els.quoteSourceType.textContent=q.sourceType||'—';els.quoteSourceTitle.textContent=q.sourceTitle||'—';els.quoteSourceTranslation.textContent=q.translationNote||'中文为收藏夹展示用自译/意译。';els.quoteSourceLink.href=q.sourceUrl||'#';els.quoteSourceLink.classList.toggle('hidden',!q.sourceUrl);els.quoteSourceDialog.showModal()}
  const MovieQuoteEngine={
    getAll:()=>VERIFIED_QUOTE_LIBRARY.map(q=>({...q})),
    getCurrent:()=>quoteState.current?{...quoteState.current}:null,
    random:()=>chooseVerifiedQuote({avoidCurrent:true}),
    count:()=>VERIFIED_QUOTE_LIBRARY.length
  };
  window.MovieQuoteEngine=MovieQuoteEngine;

  function metrics(){return Domain.metrics(appState,{year:NOW_YEAR,month:CURRENT_MONTH})}
  function renderMetrics(){const m=metrics();els.yearWatched.textContent=m.yearWatched;els.wantCount.textContent=m.want;els.planCount.textContent=m.plans;els.planDone.textContent=m.done;els.totalCount.textContent=m.total;const pct=m.plans?Math.round(m.done/m.plans*100):0;els.planTopDone.textContent=m.done;els.planTopAll.textContent=m.plans;els.planPercent.textContent=pct+'%';els.planBar.style.width=pct+'%';if(els.yearCompareNote){const delta=m.yearWatched-m.prevYearWatched;els.yearCompareNote.textContent=m.prevYearWatched?`较去年 ${delta>=0?'+':''}${delta}`:(m.yearWatched?'去年暂无记录':'今年还没有观看记录')}if(els.homeHeroYear)els.homeHeroYear.textContent=NOW_YEAR;if(els.homePlanMonthLabel)els.homePlanMonthLabel.textContent=monthLabel(CURRENT_MONTH);if(els.batchPlan)els.batchPlan.textContent=`加入 ${monthLabel(CURRENT_MONTH)}`}
  function renderRadar(){
    els.radarGrid.innerHTML='';
    const rows=radarItems().filter(m=>!m.ignored&&m.category==='week'&&isCurrentWeek(m.discoveredAt)).sort((a,b)=>(b.match||0)-(a.match||0)).slice(0,4);
    if(!rows.length){els.radarGrid.innerHTML='<div class="home-radar-empty"><div><b>本周暂无真实雷达数据</b>进入「电影雷达」从 TMDb 更新或导入本周雷达后，这里会自动显示评分、匹配度与真实海报。</div></div>';return}
    rows.forEach((m,i)=>{
      const el=document.createElement('article');el.className='movie-card';const collected=findMovieForRadar(m),posterUrl=m.posterUrl||collected?.info?.posterUrl||'';
      const art=posterUrl?`<img src="${escAttr(posterUrl)}" alt="${escAttr(m.title)} 海报" loading="lazy">`:`<div class="poster-art"><span>${esc(m.title)}</span></div>`;
      el.innerHTML=`<div class="poster ${m.poster||'p'+((i%4)+1)}">${art}<span class="badge ${m.kind||'focus'}">${esc(m.badge||'推荐')}</span></div><div class="movie-body"><div class="movie-name">${esc(m.title)}</div><div class="movie-meta">${esc(m.meta||m.year||'')}</div><div class="scores"><span class="score-public">公众口碑 ★ ${m.public!=null?Number(m.public).toFixed(1):'—'}</span><span class="score-match">匹配度 ${m.match!=null?Math.round(m.match):'—'}%</span></div><div class="movie-actions"><button data-fav-radar="${escAttr(m.title)}">${collected?'已收藏':'＋ 收藏'}</button><button data-plan-radar="${escAttr(m.title)}">${collected&&getPlan(collected,CURRENT_MONTH)?'✓ 已在计划':'＋ 加入计划'}</button></div></div>`;els.radarGrid.appendChild(el)
    })
  }
  function renderRecent(){els.recentGrid.innerHTML='';const rows=watchEntries().filter(x=>x.watch.date).sort((a,b)=>String(b.watch.date).localeCompare(String(a.watch.date))).slice(0,4);if(!rows.length){els.recentGrid.innerHTML='<div class="home-radar-empty"><div><b>还没有真实观看记录</b>记录第一次观看后，这里会显示最近的作品、日期与评分。</div></div>';return}rows.forEach(({movie,watch})=>{const el=document.createElement('article');el.className='recent';el.dataset.openDetail=movie.id;el.style.cursor='pointer';const d=String(watch.date).slice(5).replace('-','月')+'日',season=isSeasonSourceWatch(movie,watch)?`第 ${watch.sourceSeason} 季`:'';const info=movie.info||{},posterUrl=String(info.posterUrl||'').trim(),director=(info.directors||[])[0]||(movie.mediaType==='tv'?'主创待补全':'导演待补全'),genres=(info.genres||[]).slice(0,2).join(' / ')||mediaTypeLabel(movie),publicScore=info.tmdbVoteAverage??movie.radar?.publicReputation;el.innerHTML=`<div class="mini-poster${posterUrl?' has-image':''}">${posterUrl?`<img src="${escAttr(posterUrl)}" alt="${escAttr(info.title)} 海报" loading="lazy">`:`<span>✦</span>`}</div><div class="recent-identity"><div class="t">${esc(info.title)}</div><div class="recent-creator"><span>导演 ${esc(director)}</span><span>类型 ${esc(season||genres)}</span></div></div><div class="recent-metric recent-time"><span>观看时间</span><strong>${esc(d)}</strong></div><div class="recent-metric recent-personal"><span>我的评分</span><strong>${watch.rating!=null?'★ '+Number(watch.rating).toFixed(1):'未评分'}</strong></div><div class="recent-metric recent-public"><span>公众口碑</span><strong>${publicScore!=null?'★ '+Number(publicScore).toFixed(1):'—'}</strong></div>`;els.recentGrid.appendChild(el)})}
  function renderPlan(){els.planList.innerHTML='';const entries=planEntries(CURRENT_MONTH).filter(x=>x.plan.status!=='deferred').sort((a,b)=>String(a.plan.plannedDate||'9999').localeCompare(String(b.plan.plannedDate||'9999'))).slice(0,4);if(!entries.length){els.planList.innerHTML='<div class="plan-row"><span>本月还没有观影计划</span><span class="date">—</span><span class="status wait">待安排</span></div>';return}entries.forEach(({movie,plan})=>{const el=document.createElement('div');el.className='plan-row';const date=plan.plannedDate?plan.plannedDate.slice(5):'待定';el.innerHTML=`<span>${esc(movie.info.title)}</span><span class="date">${esc(date)}</span><span class="status ${plan.status==='completed'?'done':'wait'}">${plan.status==='completed'?'已看':plan.status==='deferred'?'延期':'待看'}</span>`;el.style.cursor='pointer';el.dataset.openDetail=movie.id;els.planList.appendChild(el)})}
  const homeRandomState={movieId:null,recentIds:[]};
  function randomCandidates(){const all=(appState.movies||[]).filter(m=>m.personal?.status==='want'&&!(m.watchHistory||[]).length&&m.info?.title),ids=Array.isArray(appState.home?.featured)?appState.home.featured:[],featured=ids.map(id=>all.find(m=>m.id===id)).filter(Boolean);return featured.length?featured:all}
  function pickRandomMovie(forceNew=false){const list=randomCandidates();if(!list.length){homeRandomState.movieId=null;return null}let current=list.find(m=>m.id===homeRandomState.movieId);if(current&&!forceNew)return current;let pool=list.filter(m=>m.id!==homeRandomState.movieId&&!homeRandomState.recentIds.includes(m.id));if(!pool.length)pool=list.filter(m=>m.id!==homeRandomState.movieId);if(!pool.length)pool=list;const next=pool[Math.floor(Math.random()*pool.length)];if(next){if(homeRandomState.movieId)homeRandomState.recentIds=[homeRandomState.movieId,...homeRandomState.recentIds].slice(0,6);homeRandomState.movieId=next.id}return next||null}
  function renderRandom(forceNew=false){
    const m=pickRandomMovie(forceNew);
    if(!m){els.randomPoster.classList.remove('has-image');els.randomPoster.innerHTML='';els.randomTitle.textContent='想看片单还是空的';els.randomMeta.textContent='先收藏几部想看的电影';els.randomQuote.textContent='有了真实的“想看”记录后，星星会从中随机抽取一部。';$('randomDirector').textContent='—';$('randomGenres').textContent='—';$('randomPublicScore').textContent='—';$('randomRuntime').textContent='—';els.watchTonight.disabled=true;els.reroll.disabled=true;return}
    els.watchTonight.disabled=false;els.reroll.disabled=randomCandidates().length<2;const i=m.info||{};
    els.randomPoster.classList.toggle('has-image',Boolean(i.posterUrl));els.randomPoster.innerHTML=i.posterUrl?`<img src="${escAttr(i.posterUrl)}" alt="${escAttr(i.title)} 海报">`:'';
    els.randomTitle.textContent=i.title||'未命名电影';const meta=[];if(i.originalTitle&&i.originalTitle!==i.title)meta.push(i.originalTitle);if(i.year)meta.push(i.year);if((i.genres||[]).length)meta.push((i.genres||[]).slice(0,2).join(' / '));if(i.runtime)meta.push(`${i.runtime} 分钟`);els.randomMeta.textContent=meta.join(' · ')||'资料待补全';
    const copy=(i.overview||'').trim();const tags=[...(i.genres||[]),...(m.personal?.tags||[])].filter(Boolean),publicScore=i.tmdbVoteAverage??m.radar?.publicReputation;$('randomDirector').textContent=(i.directors||[]).slice(0,2).join(' / ')||(m.mediaType==='tv'?'主创待补全':'导演待补全');$('randomGenres').textContent=(i.genres||[]).slice(0,2).join(' / ')||mediaTypeLabel(m);$('randomPublicScore').textContent=publicScore!=null?`★ ${Number(publicScore).toFixed(1)}`:'—';$('randomRuntime').textContent=i.runtime?`${i.runtime} 分钟`:'—';els.randomQuote.textContent=copy?(copy.length>180?copy.slice(0,180)+'…':copy):(tags.length?`关键词：${uniq(tags).slice(0,5).join(' · ')}`:'来自你的真实想看片单。')
  }
  function renderMonthInsight(){
    const radarCount=radarItems().filter(r=>!r.ignored&&String(r.discoveredAt||'').startsWith(CURRENT_MONTH)).length;
    const monthRows=watchEntries().filter(x=>String(x.watch.date||'').startsWith(CURRENT_MONTH)),actualRows=monthRows.filter(x=>!isSeasonSourceWatch(x.movie,x.watch));const nineIds=new Set(actualRows.filter(x=>Number(x.watch.rating??x.movie.personal?.rating??-1)>=9).map(x=>x.movie.id));
    const counts=new Map();for(const {movie} of actualRows)for(const d of (movie.info.directors||[]))counts.set(d,(counts.get(d)||0)+1);const top=[...counts.entries()].sort((a,b)=>b[1]-a[1]||a[0].localeCompare(b[0],'zh-Hans-CN'))[0];
    els.monthRadarCount.textContent=radarCount;els.monthNinePlus.textContent=nineIds.size;els.monthTopDirector.textContent=top?.[0]||'—'
  }

  function hasPlan(m,month=CURRENT_MONTH){return Domain.hasPlan(m,month)}
  function displayStatus(m){return Domain.displayStatus(m,CURRENT_MONTH)}

  const {MEDIA_FILTER:LIB_MEDIA_FILTER,STATUS_FILTER:LIB_STATUS_FILTER,SORT_FILTER:LIB_SORT_FILTER}=window.CineverseLibrary;
  function populateDatalist(list,values,formatter=x=>x){if(!list)return;list.innerHTML='<option value="全部"></option>'+values.map(v=>`<option value="${escAttr(formatter(v))}"></option>`).join('')}
  function populateSelect(select,values){if(!select)return;const current=select.value,emptyLabel=select.options[0]?.textContent||'全部';select.innerHTML=`<option value="">${esc(emptyLabel)}</option>`+values.map(value=>`<option value="${escAttr(value)}">${esc(value)}</option>`).join('');if(values.some(value=>String(value)===current))select.value=current}
  const resolveMappedFilter=window.CineverseLibrary.resolveMappedFilter;
  function renderLibraryFilters(){const movies=appState.movies||[];populateDatalist(els.libYearOptions,uniq(movies.map(m=>m.info.year).filter(Boolean)).sort((a,b)=>b-a).map(String));populateDatalist(els.libDirectorOptions,uniq(movies.flatMap(m=>m.info.directors||[])).sort((a,b)=>a.localeCompare(b,'zh-Hans-CN')));populateDatalist(els.libCountryOptions,uniq(movies.flatMap(m=>m.info.countries||[])).sort((a,b)=>a.localeCompare(b,'zh-Hans-CN')));populateDatalist(els.libTagOptions,uniq(movies.flatMap(m=>m.personal?.tags||[])).sort((a,b)=>a.localeCompare(b,'zh-Hans-CN')));populateDatalist(els.libPlanOptions,uniq(movies.flatMap(m=>(m.plans||[]).map(p=>p.month))).sort().reverse())}
  function syncRatingFilterUI(){const op=els.libRatingOp.value;const enabled=!['all','unrated'].includes(op);els.libRatingScore.disabled=!enabled;if(!enabled)els.libRatingScore.value='';const toggle=els.libRatingScore.closest('.manual-filter-input-wrap')?.querySelector('.manual-filter-toggle');if(toggle)toggle.disabled=!enabled}
  function getFilteredMovies(){
    return window.CineverseLibrary.filterMovies(appState.movies,{keyword:els.libKeyword.value,mediaType:els.libMediaType.value,year:els.libYear.value,director:els.libDirector.value,country:els.libCountry.value,status:els.libStatus.value,ratingOp:els.libRatingOp.value||'all',ratingTarget:els.libRatingScore.value,tag:els.libTag.value,plan:els.libPlan.value,sort:els.libSort.value,exclude:libraryState.exclude},{displayStatus,mediaTypeLabel});
  }
  function renderLibraryMetrics(){const ms=appState.movies||[];els.libTotal.textContent=ms.length;els.libWatched.textContent=ms.filter(m=>m.personal?.status==='watched').length;els.libWant.textContent=ms.filter(m=>m.personal?.status==='want').length;els.libPlanned.textContent=ms.filter(m=>hasPlan(m)).length}
  function libraryPageData(){
    const filtered=getFilteredMovies(),data=window.CineverseLibrary.paginate(filtered,libraryState.page,libraryState.pageSize),{totalPages,start,pageMovies}=data;
    libraryState.page=data.page;
    return {filtered,totalPages,start,pageMovies};
  }
  function renderLibraryPagination(filteredCount,totalPages,start,pageCount){
    const hasItems=filteredCount>0;
    els.libraryPagination.classList.toggle('hidden',!hasItems);
    if(!hasItems)return;
    els.libPageText.textContent=`第 ${libraryState.page} / ${totalPages} 页 · ${start+1}-${start+pageCount} / ${filteredCount} 部`;
    els.libFirstPage.disabled=libraryState.page<=1;els.libPrevPage.disabled=libraryState.page<=1;els.libNextPage.disabled=libraryState.page>=totalPages;els.libLastPage.disabled=libraryState.page>=totalPages;
    const current=libraryState.page,pages=[];
    if(totalPages<=7){for(let i=1;i<=totalPages;i++)pages.push(i)}else{
      pages.push(1);let a=Math.max(2,current-2),b=Math.min(totalPages-1,current+2);
      if(current<=4)b=6;if(current>=totalPages-3)a=totalPages-5;
      if(a>2)pages.push('…');for(let i=a;i<=b;i++)pages.push(i);if(b<totalPages-1)pages.push('…');pages.push(totalPages)
    }
    els.libPageNumbers.innerHTML=pages.map(p=>p==='…'?'<span class="library-page-ellipsis">…</span>':`<button class="${p===current?'active':''}" data-library-page="${p}" ${p===current?'aria-current="page"':''}>${p}</button>`).join('');
  }
  function goLibraryPage(page,scroll=true){
    const totalPages=Math.max(1,Math.ceil(getFilteredMovies().length/libraryState.pageSize));libraryState.page=Math.min(Math.max(1,Number(page)||1),totalPages);renderLibrary();
    if(scroll)requestAnimationFrame(()=>els.libraryGrid.scrollIntoView({block:'start',behavior:appState.settings.motion?'smooth':'auto'}));
  }
  function renderLibrary(){
    renderLibraryFilters();renderLibraryMetrics();const {filtered,totalPages,start,pageMovies}=libraryPageData();
    els.libShownText.textContent=filtered.length?`当前页 ${start+1}-${start+pageMovies.length} · 筛选结果 ${filtered.length} / 总库 ${appState.movies.length} 部`:`当前显示 0 / ${appState.movies.length} 部`;
    els.libraryGrid.innerHTML='';els.libraryGrid.classList.toggle('list-view',libraryState.view==='list');els.libraryGrid.classList.toggle('multi-mode',libraryState.multi);els.libraryEmpty.classList.toggle('hidden',filtered.length>0);els.libraryGrid.classList.toggle('hidden',filtered.length===0);
    for(const m of pageMovies){const [sc,st]=displayStatus(m),isTv=m.mediaType==='tv';const el=document.createElement('article');el.className='lib-card';const poster=m.info.posterUrl?`<div class="lib-poster has-image"><img src="${escAttr(m.info.posterUrl)}" alt="${escAttr(m.info.title)} 海报" loading="lazy"></div>`:`<div class="lib-poster" style="filter:hue-rotate(${posterHue(m)}deg)"><div class="poster-title">${esc(m.info.title)}</div></div>`;const series=isTv?[m.info.numberOfSeasons!=null?m.info.numberOfSeasons+'季':null,m.info.numberOfEpisodes!=null?m.info.numberOfEpisodes+'集':null].filter(Boolean).join(' · '):'';const creator=(m.info.directors||[]).join(' / ')||(isTv?'主创未知':'导演未知');const runtime=m.info.runtime?(isTv?'单集约 '+m.info.runtime+' 分钟':m.info.runtime+' 分钟'):(isTv?(series||'剧集资料待补全'):'时长未知'),wants=m.personal?.want==null?m.personal?.status==='want':Boolean(m.personal.want),watched=(m.watchHistory||[]).length>0,planned=hasPlan(m);el.innerHTML=`<input class="lib-select" type="checkbox" data-select-id="${escAttr(m.id)}" ${libraryState.selected.has(m.id)?'checked':''}>${poster}<button class="fav-star ${m.personal?.favorite?'on':''}" data-favorite-id="${escAttr(m.id)}" title="收藏">★</button><span class="status-pill ${sc}">${st}</span><div class="lib-body"><div class="lib-title-wrap" data-open-detail="${escAttr(m.id)}"><div class="lib-title"><span class="media-type-pill ${escAttr(m.mediaType)}">${mediaTypeIcon(m)} ${mediaTypeLabel(m)}</span>${esc(m.info.title)}</div><div class="lib-meta">${m.info.year||'年份未知'} · ${esc(creator)} · ${esc(runtime)}${series&&m.info.runtime?' · '+esc(series):''}</div></div><div class="lib-rating"><span class="score">★ ${m.personal?.rating!=null?m.personal.rating.toFixed(1):'未评分'}</span>${m.radar?.discovered?'<span class="radar-mark">✦ 雷达发现</span>':''}</div><div class="lib-tags">${(m.personal?.tags||[]).slice(0,3).map(t=>`<span class="lib-tag">${esc(t)}</span>`).join('')}</div><div class="lib-actions"><button class="${wants?'active':''}" data-lib-want="${escAttr(m.id)}">${wants?'已想看':'想看'}</button><button class="${watched?'active':''}" data-lib-watched="${escAttr(m.id)}">${watched?'已看过':'看过'}</button><button class="${planned?'active':''}" data-lib-plan="${escAttr(m.id)}">${planned?'已加入计划':'加入计划'}</button><button data-lib-watch="${escAttr(m.id)}">添加观影记录</button></div></div>`;els.libraryGrid.appendChild(el)}
    renderLibraryPagination(filtered.length,totalPages,start,pageMovies.length);updateBatchBar()
  }
  function updateBatchBar(){els.batchBar.classList.toggle('hidden',!libraryState.multi);els.batchCount.textContent=`已选 ${libraryState.selected.size} 部`;els.multiBtn.classList.toggle('active',libraryState.multi)}
  function toggleExclude(key,btn){libraryState.exclude[key]=!libraryState.exclude[key];btn.classList.toggle('active',libraryState.exclude[key]);btn.textContent=libraryState.exclude[key]?'排除':'包含';libraryState.page=1;renderLibrary()}
  function clearFilters(){els.libKeyword.value='';[els.libMediaType,els.libYear,els.libDirector,els.libCountry,els.libStatus,els.libTag,els.libPlan].forEach(x=>x.value='');els.libRatingOp.value='all';els.libRatingScore.value='';syncRatingFilterUI();els.libSort.value='最近更新';libraryState.exclude={year:false,director:false,country:false,tag:false,plan:false};document.querySelectorAll('.exclude-toggle').forEach(b=>{b.classList.remove('active');b.textContent='包含'});libraryState.page=1;renderLibrary()}
  function cycleStatus(m){const order=m.mediaType==='tv'?['want','watching','watched','paused','dropped']:['follow','want','watched'];const cur=m.personal?.status||'want';m.personal.status=order[(order.indexOf(cur)+1)%order.length];if(m.personal.status==='watched'&&!m.watchHistory?.length)m.watchHistory=[{date:localToday(),rating:m.personal.rating,note:'',venue:''}];m.updatedAt=new Date().toISOString();save();renderAll();toastMsg(`《${m.info.title}》状态已更新为「${displayStatus(m)[1]}」`)}
  const tmdbClient=window.CineverseTmdbClient.createClient({getLanguage:()=>appState.settings.tmdbLanguage||'zh-CN'});
  const TmdbMatch=window.CineverseTmdbMatch;
  const {cleanTitle:cleanTmdbTitle,seasonInfoFromMovie,seasonGroupKey,compactSeasonTargets,candidateTitle,candidateOriginal,candidateDate}=TmdbMatch;
  const DoubanImport=window.CineverseDoubanImport;
  const {parseCsvRows:parseCsv,parseTitlePair:doubanParseTitlePair,cleanTitle:doubanCleanTitle,parseFile:parseDoubanFile}=DoubanImport;
  function tmdbCredential(){return 'proxy'}
  const tmdbFetch=(path,params={})=>tmdbClient.request(path,params);
  const tmdbImage=window.CineverseTmdbClient.image;

  function movieNeedsTmdb(m){const i=m?.info||{},tv=m?.mediaType==='tv',unknown=m?.mediaType==='unknown',seasonal=Boolean(seasonInfoFromMovie(m));return (seasonal&&m.mediaType!=='tv')||unknown||!i.tmdbId||!i.originalTitle||!(tv?i.firstAirDate||i.releaseDate:i.releaseDate)||!(i.directors||[]).length||!(i.countries||[]).length||!(i.genres||[]).length||!i.posterUrl||!i.overview}
  function seasonSourceRecord(movie){const x=seasonInfoFromMovie(movie),hist=(movie.watchHistory||[]).filter(w=>String(w.venue||'').includes('豆瓣')).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||''))),latest=hist[0]||null;return{id:String(movie.info?.doubanId||movie.external?.douban?.id||''),title:x?.sourceTitle||movie.info?.title||'',originalTitle:x?.sourceOriginal||movie.info?.originalTitle||'',season:x?.season??null,status:movie.personal?.status||'',rating:movie.personal?.rating??latest?.rating??null,date:latest?.date||'',comment:movie.personal?.shortReview||'',lastImportedAt:movie.external?.douban?.lastImportedAt||movie.updatedAt||''}}
  function annotateSeasonHistory(movie,sourceSeason){for(const w of movie.watchHistory||[]){if(String(w.venue||'').includes('豆瓣')){if(sourceSeason!=null&&w.sourceSeason==null)w.sourceSeason=sourceSeason;if(movie.info?.doubanId&&!w.sourceDoubanId)w.sourceDoubanId=String(movie.info.doubanId);if(!w.sourceTitle)w.sourceTitle=movie.info?.title||''}}}
  function mergeUniqueHistories(target,source,sourceSeason){target.watchHistory=target.watchHistory||[];annotateSeasonHistory(source,sourceSeason);for(const w of source.watchHistory||[]){const item={...w};const key=x=>[x.date||'',x.rating??'',x.sourceDoubanId||'',x.sourceSeason??'',x.note||''].join('|');if(!target.watchHistory.some(x=>key(x)===key(item)))target.watchHistory.push(item)}}
  function collapseTvSeriesForMovie(anchor,bundle){if(!anchor||bundle?.mediaType!=='tv')return{anchor,merged:0};const d=bundle.detail||{},hint=seasonInfoFromMovie(anchor),baseKey=cleanTmdbTitle((d.original_name||'')||(hint?.originalBase||hint?.titleBase||anchor.info.originalTitle||anchor.info.title));const candidates=(appState.movies||[]).filter(m=>m.id!==anchor.id).filter(m=>{if(m.mediaType==='tv'&&Number(m.info?.tmdbId)===Number(d.id))return true;const sx=seasonInfoFromMovie(m);if(!sx)return false;const k1=cleanTmdbTitle(sx.originalBase||sx.titleBase),k2=cleanTmdbTitle(sx.titleBase||sx.originalBase);return !!baseKey&&(k1===baseKey||k2===baseKey||cleanTmdbTitle(d.name||'')===k1||cleanTmdbTitle(d.name||'')===k2)});const group=[anchor,...candidates];anchor.external=anchor.external||{};anchor.external.doubanSeasons=Array.isArray(anchor.external.doubanSeasons)?anchor.external.doubanSeasons:[];const srcRecords=[];for(const m of group){const sx=seasonInfoFromMovie(m);if(sx)annotateSeasonHistory(m,sx.season);const rec=sx?seasonSourceRecord(m):null;if(rec&&rec.id&&!srcRecords.some(x=>x.id===rec.id))srcRecords.push(rec);for(const r of (m.external?.doubanSeasons||[]))if(r?.id&&!srcRecords.some(x=>String(x.id)===String(r.id)))srcRecords.push(r);if(m.id!==anchor.id)mergeUniqueHistories(anchor,m,sx?.season);anchor.personal.tags=uniq([...(anchor.personal.tags||[]),...(m.personal?.tags||[])]);anchor.personal.favorite=Boolean(anchor.personal.favorite||m.personal?.favorite);anchor.plans=uniqBy([...(anchor.plans||[]),...(m.plans||[])],x=>[x.month,x.status,x.plannedDate||''].join('|'));anchor.radar={...(anchor.radar||{}),...(m.radar?.discovered?m.radar:{}),discovered:Boolean(anchor.radar?.discovered||m.radar?.discovered)};if(!anchor.personal.shortReview&&m.personal?.shortReview)anchor.personal.shortReview=m.personal.shortReview}
    anchor.external.doubanSeasons=uniqBy([...(anchor.external.doubanSeasons||[]),...srcRecords],x=>String(x.id||'')).filter(x=>x.id);const importedRatings=anchor.external.doubanSeasons.map(x=>Number(x.rating)).filter(x=>Number.isFinite(x)&&x>0),distinct=[...new Set(importedRatings)];if(distinct.length===1)anchor.personal.rating=distinct[0];else if(distinct.length>1)anchor.personal.rating=null;const watchedSeasons=anchor.external.doubanSeasons.filter(x=>x.status==='watched'&&Number(x.season)>0).map(x=>Number(x.season));if(watchedSeasons.length){const total=Number(d.number_of_seasons)||null,max=Math.max(...watchedSeasons),uniqueCount=new Set(watchedSeasons).size;anchor.personal.status=(total&&max>=total&&uniqueCount>=total)?'watched':'watching'}else if(anchor.external.doubanSeasons.some(x=>x.status==='want'))anchor.personal.status='want';anchor.mediaType='tv';anchor.info.title=d.name||hint?.titleBase||anchor.info.title;anchor.info.originalTitle=d.original_name||hint?.originalBase||anchor.info.originalTitle;anchor.info.tmdbId=d.id||anchor.info.tmdbId;anchor.updatedAt=new Date().toISOString();const removeIds=new Set(candidates.map(x=>x.id));appState.movies=appState.movies.filter(m=>!removeIds.has(m.id));for(const id of removeIds){libraryState.selected.delete(id);removeMatchRow(id)}removeMatchRow(anchor.id);return{anchor,merged:removeIds.size}}
  const fetchTmdbBundle=(tmdbId,mediaType='movie')=>tmdbClient.bundle(tmdbId,mediaType);
  function applyTmdbBundleToMovie(movie,bundle,{overwrite=false}={}){const d=bundle.detail||{},c=bundle.credits||{},type=bundle.mediaType==='tv'?'tv':'movie',i=movie.info||{};movie.mediaType=type;const tv=type==='tv';const creatorNames=tv?uniq([...(d.created_by||[]).map(x=>x.name),...(c.crew||[]).filter(x=>(x.jobs||[]).some(j=>['Series Director','Director','Executive Producer'].includes(j.job))).slice(0,6).map(x=>x.name)].filter(Boolean)):uniq((c.crew||[]).filter(x=>x.job==='Director').map(x=>x.name).filter(Boolean));const countries=(d.production_countries||[]).map(x=>zhCountry(x.iso_3166_1,x.name)).filter(Boolean);if(tv&&!countries.length)(d.origin_country||[]).forEach(code=>countries.push(zhCountry(code,code)));const genres=(d.genres||[]).map(x=>x.name).filter(Boolean);const put=(key,value,valid=v=>v!==''&&v!=null)=>{if(valid(value)&&(overwrite||!i[key]||(Array.isArray(i[key])&&!i[key].length)))i[key]=value};put('tmdbId',d.id||i.tmdbId);put('tmdbVoteAverage',d.vote_average!=null?Number(d.vote_average):null);put('originalTitle',tv?(d.original_name||''):(d.original_title||''));const date=tv?(d.first_air_date||''):(d.release_date||'');put('releaseDate',date);if(tv)put('firstAirDate',d.first_air_date||'');put('year',date.slice(0,4)?Number(date.slice(0,4)):null);put('runtime',tv?((d.episode_run_time||[])[0]||null):(d.runtime||null));put('directors',creatorNames,v=>Array.isArray(v)&&v.length);put('countries',countries,v=>Array.isArray(v)&&v.length);put('genres',genres,v=>Array.isArray(v)&&v.length);put('overview',d.overview||'');put('posterUrl',tmdbImage(d.poster_path,'w500'));if(tv){put('lastAirDate',d.last_air_date||'');put('numberOfSeasons',d.number_of_seasons??null);put('numberOfEpisodes',d.number_of_episodes??null);put('tvStatus',d.status||'')}if(!i.title)i.title=tv?(d.name||d.original_name||''):(d.title||d.original_title||'');movie.updatedAt=new Date().toISOString();return movie}

  function compactTmdbCandidate(c){return c?{id:c.id,media_type:c.media_type||'movie',title:c.title||'',original_title:c.original_title||'',release_date:c.release_date||'',name:c.name||'',original_name:c.original_name||'',first_air_date:c.first_air_date||'',poster_path:c.poster_path||'',overview:c.overview||''}:null}
  function matchCenterRows(){appState.tmdbMatchCenter=appState.tmdbMatchCenter||{lastScanAt:'',lastTotal:0,lastAuto:0,lastManual:0,rows:[]};appState.tmdbMatchCenter.rows=Array.isArray(appState.tmdbMatchCenter.rows)?appState.tmdbMatchCenter.rows:[];return appState.tmdbMatchCenter.rows}
  function matchRowFor(movieId){return matchCenterRows().find(r=>r.movieId===movieId)}
  function upsertMatchRow(row){const rows=matchCenterRows(),idx=rows.findIndex(r=>r.movieId===row.movieId),clean={...row,candidates:(row.candidates||[]).map(compactTmdbCandidate).filter(Boolean),updatedAt:new Date().toISOString()};if(idx>=0)rows[idx]={...rows[idx],...clean};else rows.push(clean);return clean}
  function removeMatchRow(movieId){appState.tmdbMatchCenter.rows=matchCenterRows().filter(r=>r.movieId!==movieId)}
  function matchCounts(){const rows=matchCenterRows().filter(r=>appState.movies.some(m=>m.id===r.movieId));return{pending:rows.filter(r=>r.status==='pending').length,noresult:rows.filter(r=>r.status==='noresult').length,failed:rows.filter(r=>r.status==='failed').length,skipped:rows.filter(r=>r.status==='skipped').length}}
  function matchStatusLabel(s){return s==='pending'?'待确认':s==='noresult'?'无结果':s==='failed'?'请求失败':s==='skipped'?'暂缓':'待处理'}
  function matchWatchDate(movie){const ds=(movie.watchHistory||[]).map(w=>w.date).filter(Boolean).sort();return ds.length?ds[ds.length-1]:'—'}
  function matchCenterProgress(text,pct){const p=Math.max(0,Math.min(100,Math.round(pct)));els.matchProgress.classList.remove('hidden');els.matchProgressText.textContent=text||'准备扫描…';els.matchProgressPct.textContent=p+'%';els.matchProgressBar.style.width=p+'%';els.matchStartScan.disabled=matchCenterState.running;els.matchRetryProblems.disabled=matchCenterState.running;els.matchResetSkipped.disabled=matchCenterState.running;if(els.matchDedupSeasons)els.matchDedupSeasons.disabled=matchCenterState.running;if(els.matchPauseResume){const paused=Boolean(appState.tmdbMatchCenter?.paused&&(appState.tmdbMatchCenter?.resumeIds||[]).length);els.matchPauseResume.disabled=!matchCenterState.running&&!paused;els.matchPauseResume.textContent=matchCenterState.running?'暂停扫描':paused?'继续扫描':'暂停扫描'}}
  function renderMatchCenter(){if(els.matchPauseResume){const paused=Boolean(appState.tmdbMatchCenter?.paused&&(appState.tmdbMatchCenter?.resumeIds||[]).length);els.matchPauseResume.disabled=!matchCenterState.running&&!paused;els.matchPauseResume.textContent=matchCenterState.running?'暂停扫描':paused?'继续扫描':'暂停扫描'}
    if(!els.matchView)return;const center=appState.tmdbMatchCenter||{},counts=matchCounts();
    els.matchStatTotal.textContent=center.lastTotal||0;els.matchStatAuto.textContent=center.lastAuto||0;els.matchStatPending.textContent=counts.pending;els.matchStatNoResult.textContent=counts.noresult;els.matchStatFailed.textContent=counts.failed;
    els.matchLastScan.textContent=center.lastScanAt?`最近扫描：${new Date(center.lastScanAt).toLocaleString('zh-CN',{hour12:false})} · 人工确认 ${center.lastManual||0} 部${counts.skipped?` · 暂缓 ${counts.skipped} 部`:''}`:'尚未运行集中扫描';
    els.matchTabs.querySelectorAll('[data-match-tab]').forEach(b=>b.classList.toggle('active',b.dataset.matchTab===matchCenterState.tab));
    const q=(els.matchKeyword.value||'').trim().toLowerCase();let rows=matchCenterRows().filter(r=>['pending','noresult','failed','skipped'].includes(r.status)).filter(r=>{if(matchCenterState.tab!=='all'&&r.status!==matchCenterState.tab)return false;const m=appState.movies.find(x=>x.id===r.movieId);if(!m)return false;return !q||[m.info.title,m.info.originalTitle,m.info.doubanId,r.reason].some(v=>String(v||'').toLowerCase().includes(q))});
    rows.sort((a,b)=>String(b.updatedAt||'').localeCompare(String(a.updatedAt||'')));
    if(!rows.length){const label=matchCenterState.tab==='pending'?'没有待确认电影 ✦':matchCenterState.tab==='all'?'目前没有未处理的匹配项目 ✦':`没有“${matchStatusLabel(matchCenterState.tab)}”项目`;els.matchList.innerHTML=`<div class="match-empty">${label}<br>${center.lastScanAt?'可以继续导入豆瓣片单，或重新扫描资料不完整的影视作品。':'点击右上角“扫描未匹配影视”开始。'}</div>`;return}
    els.matchList.innerHTML=rows.map(row=>{const m=appState.movies.find(x=>x.id===row.movieId);if(!m)return'';const cs=(row.candidates||[]).slice(0,6);return `<article class="match-item" data-match-row="${escAttr(m.id)}"><div class="match-item-top"><div class="match-source"><div class="match-source-title"><b>《${esc(m.info.title)}》</b><span class="media-type-pill ${escAttr(m.mediaType)}">${mediaTypeLabel(m)}</span><span class="match-status-badge ${escAttr(row.status)}">${esc(matchStatusLabel(row.status))}</span></div><div class="match-source-original">${m.info.originalTitle?`外文原名：${esc(m.info.originalTitle)}`:'未导出外文原名，将以中文名匹配'}</div><div class="match-source-meta"><span>豆瓣 ID：${esc(m.info.doubanId||'—')}</span><span>看过标记 / 观看时间：${esc(matchWatchDate(m))} · 不参与 TMDb 匹配</span><span>当前 TMDb：${esc(m.info.tmdbId||'未关联')}</span></div><div class="match-source-reason">${esc(row.reason||'需要人工确认')}</div></div><div class="match-candidates">${cs.length?cs.map(c=>`<div class="match-candidate" data-match-use="${escAttr(m.id)}" data-tmdb-id="${escAttr(c.id)}" data-tmdb-type="${escAttr(c.media_type||'movie')}">${c.poster_path?`<img src="${escAttr(tmdbImage(c.poster_path,'w92'))}" alt="">`:'<div class="ph">无海报</div>'}<div class="match-candidate-copy"><b><span class="tmdb-kind ${escAttr(c.media_type||'movie')}">${c.media_type==='tv'?'剧集':'电影'}</span>${esc(candidateTitle(c)||'未命名')}</b><span>${candidateOriginal(c)&&candidateOriginal(c)!==candidateTitle(c)?esc(candidateOriginal(c))+'<br>':''}${esc(candidateDate(c).slice(0,4)||'年份未知')} · TMDb ${esc(c.id)}</span><em>选用这个结果 →</em></div></div>`).join(''):`<div class="match-no-candidates">当前没有可选候选。<br>可以重试 TMDb，或进入手动编辑。</div>`}</div></div><div class="match-item-actions"><button class="retry" data-match-retry="${escAttr(m.id)}">重新搜索</button><button class="primary" data-match-edit="${escAttr(m.id)}">手动编辑</button><button class="muted" data-match-skip="${escAttr(m.id)}">${row.status==='skipped'?'恢复处理':'以后处理'}</button></div></article>`}).join('');
  }
  async function resolveMatchCenterCandidate(movieId,tmdbId,tmdbType){const movie=appState.movies.find(m=>m.id===movieId);if(!movie)return;const row=matchRowFor(movieId),candidate=(row?.candidates||[]).find(c=>String(c.id)===String(tmdbId)&&(!tmdbType||(c.media_type||'movie')===tmdbType));const el=els.matchList.querySelector(`[data-match-use="${CSS.escape(movieId)}"][data-tmdb-id="${CSS.escape(String(tmdbId))}"]`);if(el)el.style.opacity='.55';try{const bundle=await fetchTmdbBundle(tmdbId,candidate?.media_type||movie.mediaType);applyTmdbBundleToMovie(movie,bundle);const merged=bundle.mediaType==='tv'?collapseTvSeriesForMovie(movie,bundle).merged:0;removeMatchRow(movieId);appState.tmdbMatchCenter.lastManual=(appState.tmdbMatchCenter.lastManual||0)+1;save();renderAll();renderMatchCenter();toastMsg(`《${movie.info.title}》已关联 ${mediaTypeLabel(movie)} · TMDb #${tmdbId}${merged?` · 合并 ${merged} 个分季条目`:''}`)}catch(err){upsertMatchRow({movieId,status:'failed',candidates:row?.candidates||[],reason:`写入失败：${err.message}`});save();renderMatchCenter();toastMsg(`关联失败：${err.message}`)}finally{if(el)el.style.opacity=''}}
  async function retryMatchCenterMovie(movieId){const movie=appState.movies.find(m=>m.id===movieId);if(!movie)return;try{tmdbCredential()}catch(err){toastMsg(err.message);setView('settings');return}upsertMatchRow({movieId,status:'pending',candidates:[],reason:'正在重新搜索…'});renderMatchCenter();try{const match=await matchBatchTmdbMovie(movie);if(match.auto){const bundle=await fetchTmdbBundle(match.auto.id,match.auto.media_type||movie.mediaType);applyTmdbBundleToMovie(movie,bundle);if(bundle.mediaType==='tv')collapseTvSeriesForMovie(movie,bundle);removeMatchRow(movieId);appState.tmdbMatchCenter.lastAuto=(appState.tmdbMatchCenter.lastAuto||0)+1;toastMsg(`《${movie.info.title}》已自动匹配`)}else if((match.candidates||[]).length){upsertMatchRow({movieId,status:'pending',candidates:match.candidates,reason:match.reason||'需要手动确认'})}else{upsertMatchRow({movieId,status:'noresult',candidates:[],reason:match.reason||'TMDb 没有返回候选'})}}catch(err){upsertMatchRow({movieId,status:'failed',candidates:[],reason:`请求失败：${err.message}`})}save();renderAll();renderMatchCenter()}
  async function runMatchCenterScan({confirmStart=true,onlyProblems=false,resume=false}={}){if(matchCenterState.running)return;try{tmdbCredential()}catch(err){toastMsg(err.message);setView('settings');return}const skippedIds=new Set(matchCenterRows().filter(r=>r.status==='skipped').map(r=>r.movieId));let targets;if(resume){const ids=appState.tmdbMatchCenter.resumeIds||[];targets=ids.map(id=>appState.movies.find(m=>m.id===id)).filter(Boolean).filter(movieNeedsTmdb).filter(m=>!skippedIds.has(m.id));if(!targets.length){appState.tmdbMatchCenter.resumeIds=[];appState.tmdbMatchCenter.paused=false;save();matchCenterState.running=false;renderMatchCenter();toastMsg('没有可继续的匹配任务');return}}else{targets=compactSeasonTargets((appState.movies||[]).filter(movieNeedsTmdb).filter(m=>!skippedIds.has(m.id)));if(onlyProblems){const ids=new Set(matchCenterRows().filter(r=>r.status==='noresult'||r.status==='failed').map(r=>r.movieId));targets=targets.filter(m=>ids.has(m.id))}if(!targets.length){toastMsg(onlyProblems?'没有需要重试的项目':'没有需要扫描的未匹配影视 ✦');renderMatchCenter();return}if(confirmStart&&!confirm(`将扫描 ${targets.length} 部影视作品。自动确认仅使用：外文原名唯一精确匹配 → 中文名唯一精确匹配。豆瓣标记时间和影片年份都不会参与自动确认。开始吗？`))return;if(!onlyProblems){appState.tmdbMatchCenter.lastTotal=targets.length;appState.tmdbMatchCenter.lastAuto=0;appState.tmdbMatchCenter.lastManual=0;appState.tmdbMatchCenter.rows=matchCenterRows().filter(r=>r.status==='skipped')}else{for(const m of targets)removeMatchRow(m.id)}appState.tmdbMatchCenter.resumeIds=targets.map(m=>m.id)}matchCenterState.running=true;matchCenterState.stop=false;matchCenterState.processed=0;matchCenterState.total=targets.length;matchCenterState.auto=0;appState.tmdbMatchCenter.paused=false;setView('match');matchCenterProgress(`${resume?'继续':'准备'}扫描 ${targets.length} 部影视作品…`,0);renderMatchCenter();for(const movie of targets){if(matchCenterState.stop)break;const i=matchCenterState.processed+1;matchCenterProgress(`正在匹配 ${i}/${targets.length} · 《${movie.info.title}》`,matchCenterState.processed/targets.length*100);try{if(movie.info.tmdbId&&!seasonInfoFromMovie(movie)){const bundle=await fetchTmdbBundle(movie.info.tmdbId,movie.mediaType);applyTmdbBundleToMovie(movie,bundle);if(bundle.mediaType==='tv')collapseTvSeriesForMovie(movie,bundle);matchCenterState.auto++}else{const match=await matchBatchTmdbMovie(movie);if(match.auto){const bundle=await fetchTmdbBundle(match.auto.id,match.auto.media_type||movie.mediaType);applyTmdbBundleToMovie(movie,bundle);if(bundle.mediaType==='tv')collapseTvSeriesForMovie(movie,bundle);matchCenterState.auto++}else if((match.candidates||[]).length){upsertMatchRow({movieId:movie.id,status:'pending',candidates:match.candidates,reason:match.reason||'需要手动确认'})}else{upsertMatchRow({movieId:movie.id,status:'noresult',candidates:[],reason:match.reason||'TMDb 没有返回候选'})}}}catch(err){upsertMatchRow({movieId:movie.id,status:'failed',candidates:[],reason:`请求失败：${err.message}`})}matchCenterState.processed++;appState.tmdbMatchCenter.resumeIds=(appState.tmdbMatchCenter.resumeIds||[]).filter(id=>id!==movie.id);if(matchCenterState.processed%2===0)save();renderMatchCenter();await new Promise(r=>setTimeout(r,80))}const paused=matchCenterState.stop&&(appState.tmdbMatchCenter.resumeIds||[]).length>0;appState.tmdbMatchCenter.paused=paused;appState.tmdbMatchCenter.lastScanAt=new Date().toISOString();appState.tmdbMatchCenter.lastAuto=(resume||onlyProblems)?(appState.tmdbMatchCenter.lastAuto||0)+matchCenterState.auto:matchCenterState.auto;if(!paused)appState.tmdbMatchCenter.resumeIds=[];save();matchCenterState.running=false;const remain=(appState.tmdbMatchCenter.resumeIds||[]).length;matchCenterProgress(paused?`已暂停：本轮完成 ${matchCenterState.processed} 部 · 还有 ${remain} 部可继续`:`扫描完成：本轮自动匹配 ${matchCenterState.auto} 部 · 剩余待处理 ${matchCounts().pending+matchCounts().noresult+matchCounts().failed} 部`,paused?Math.round(matchCenterState.processed/Math.max(1,targets.length)*100):100);renderAll();renderMatchCenter();toastMsg(paused?'TMDb 扫描已暂停，可随时继续':'TMDb 匹配中心扫描完成')}

    function batchTmdbUpdateUi(text='',pct=0){const p=Math.max(0,Math.min(100,Math.round(pct)));els.batchTmdbInline.classList.toggle('hidden',!batchTmdbState.running);els.batchTmdbInlineText.textContent=text||'正在处理…';els.batchTmdbInlineBar.style.width=p+'%';els.batchTmdbProgressText.textContent=text||'准备开始';els.batchTmdbProgressPct.textContent=p+'%';els.batchTmdbProgressBar.style.width=p+'%';els.batchTmdbBtn.disabled=batchTmdbState.running;els.batchTmdbStop.disabled=!batchTmdbState.running}
  function renderBatchTmdbSummary(){const s=batchTmdbState;els.batchTmdbSummary.innerHTML=`<div class="batch-tmdb-stat"><b>${s.total}</b><span>需要补全</span></div><div class="batch-tmdb-stat"><b>${s.completed}</b><span>自动完成</span></div><div class="batch-tmdb-stat"><b>${s.pending.length}</b><span>待你确认</span></div><div class="batch-tmdb-stat"><b>${s.failed}</b><span>未找到 / 失败</span></div>`}
  function renderBatchTmdbPending(){renderBatchTmdbSummary();const rows=batchTmdbState.pending;if(!rows.length){els.batchTmdbPending.innerHTML=`<div class="batch-tmdb-empty">${batchTmdbState.running?'正在扫描影视库…':'没有需要你确认的项目 ✦'}</div>`;return}els.batchTmdbPending.innerHTML=rows.map(row=>{const movie=appState.movies.find(m=>m.id===row.movieId);if(!movie)return'';const candidates=(row.candidates||[]).slice(0,4);return `<div class="batch-tmdb-item" data-batch-row="${escAttr(row.movieId)}"><div class="batch-tmdb-item-head"><div><b>《${esc(movie.info.title)}》</b><span>${movie.info.originalTitle?esc(movie.info.originalTitle)+' · ':''}${row.reason?esc(row.reason):'需要确认'}</span></div><button class="batch-tmdb-manual" data-batch-edit="${escAttr(movie.id)}">手动编辑</button></div><div class="batch-tmdb-candidates">${candidates.length?candidates.map(c=>`<div class="batch-tmdb-candidate">${c.poster_path?`<img src="${escAttr(tmdbImage(c.poster_path,'w92'))}" alt="">`:'<div class="ph">无海报</div>'}<div class="batch-tmdb-candidate-copy"><b><span class="tmdb-kind ${escAttr(c.media_type||'movie')}">${c.media_type==='tv'?'剧集':'电影'}</span>${esc(candidateTitle(c)||'未命名')}</b><span>${esc(candidateOriginal(c)&&candidateOriginal(c)!==candidateTitle(c)?candidateOriginal(c)+' · ':'')}${esc(candidateDate(c).slice(0,4)||'年份未知')} · TMDb ${esc(c.id)}</span></div><button data-batch-tmdb-candidate="${escAttr(row.movieId)}" data-tmdb-id="${escAttr(c.id)}" data-tmdb-type="${escAttr(c.media_type||'movie')}">选用</button></div>`).join(''):'<div class="batch-tmdb-empty">没有搜索到候选结果，请手动编辑或稍后重试。</div>'}</div></div>`}).join('')}
  async function resolveBatchTmdbCandidate(movieId,tmdbId,tmdbType){const movie=appState.movies.find(m=>m.id===movieId);if(!movie)return;const btn=els.batchTmdbPending.querySelector(`[data-batch-tmdb-candidate="${CSS.escape(movieId)}"][data-tmdb-id="${CSS.escape(String(tmdbId))}"]`);if(btn)btn.disabled=true;try{const row=batchTmdbState.pending.find(x=>x.movieId===movieId),candidate=(row?.candidates||[]).find(c=>String(c.id)===String(tmdbId)&&(!tmdbType||(c.media_type||'movie')===tmdbType)),bundle=await fetchTmdbBundle(tmdbId,candidate?.media_type||tmdbType||movie.mediaType);applyTmdbBundleToMovie(movie,bundle);if(bundle.mediaType==='tv')collapseTvSeriesForMovie(movie,bundle);batchTmdbState.pending=batchTmdbState.pending.filter(x=>x.movieId!==movieId);batchTmdbState.completed++;save();renderAll();renderBatchTmdbPending();toastMsg(`《${movie.info.title}》已完成 TMDb 补全`)}catch(err){toastMsg(`补全失败：${err.message}`)}finally{if(btn)btn.disabled=false}}
  const tmdbSearchByTitle=(query,tvOnly=false)=>tmdbClient.search(query,{tvOnly});
  const matchBatchTmdbMovie=movie=>TmdbMatch.matchMovie(movie,tmdbSearchByTitle);
  async function runSeasonDedupe(){if(matchCenterState.running)return;try{tmdbCredential()}catch(err){toastMsg(err.message);setView('settings');return}const seasonal=(appState.movies||[]).filter(m=>seasonInfoFromMovie(m));if(!seasonal.length){toastMsg('没有发现豆瓣分季条目 ✦');return}const byKey=new Map();for(const m of seasonal){const k=seasonGroupKey(m)||cleanTmdbTitle(seasonInfoFromMovie(m)?.titleBase||m.info.title);if(!byKey.has(k))byKey.set(k,[]);byKey.get(k).push(m)}if(!confirm(`发现 ${seasonal.length} 个分季条目，归属于 ${byKey.size} 个剧集名称。将按“去掉季号后的剧名 → TMDb TV”匹配；确认一次后同系列所有季会合并为一个剧集主体。继续吗？`))return;matchCenterState.running=true;matchCenterState.stop=false;setView('match');let done=0,mergedTotal=0,pending=0,failed=0;matchCenterProgress(`准备整理 ${byKey.size} 组豆瓣分季…`,0);for(const group of byKey.values()){if(matchCenterState.stop)break;const anchor=group[0];matchCenterProgress(`正在整理 ${done+1}/${byKey.size} · 《${seasonInfoFromMovie(anchor)?.titleBase||anchor.info.title}》`,done/byKey.size*100);try{const match=await matchBatchTmdbMovie(anchor);if(match.auto&&match.auto.media_type==='tv'){const bundle=await fetchTmdbBundle(match.auto.id,'tv');applyTmdbBundleToMovie(anchor,bundle);mergedTotal+=collapseTvSeriesForMovie(anchor,bundle).merged}else if((match.candidates||[]).length){upsertMatchRow({movieId:anchor.id,status:'pending',candidates:(match.candidates||[]).filter(c=>c.media_type==='tv'),reason:`分季归并：${match.reason||'请选择正确剧集'}；确认后会自动合并同系列所有季。`});pending++}else{upsertMatchRow({movieId:anchor.id,status:'noresult',candidates:[],reason:'分季归并：TMDb 没有找到可唯一确认的剧集主体'});pending++}}catch(err){upsertMatchRow({movieId:anchor.id,status:'failed',candidates:[],reason:`分季归并失败：${err.message}`});failed++}done++;if(done%3===0)save();renderMatchCenter();await new Promise(r=>setTimeout(r,90))}save();matchCenterState.running=false;matchCenterProgress(`分季整理完成：已处理 ${done} 组 · 合并移除 ${mergedTotal} 个重复条目 · 待确认 ${pending} 组${failed?` · 失败 ${failed} 组`:''}`,100);renderAll();renderMatchCenter();toastMsg(`豆瓣分季整理完成：已合并 ${mergedTotal} 个重复条目`)}

  async function runBatchTmdbCompletion(autoStart=false){if(batchTmdbState.running)return;try{tmdbCredential()}catch(err){toastMsg(err.message);setView('settings');return}const targets=compactSeasonTargets((appState.movies||[]).filter(movieNeedsTmdb));if(!targets.length){toastMsg('影视库中的 TMDb 基础资料已经完整 ✦');return}if(!autoStart&&!confirm(`将扫描 ${targets.length} 部资料不完整的影视作品。匹配顺序：外文原名唯一精确匹配 → 中文名唯一精确匹配 → 仍不唯一才手动确认。标记时间与年份不参与自动确认。开始吗？`))return;Object.assign(batchTmdbState,{running:true,stop:false,pending:[],total:targets.length,processed:0,completed:0,skipped:0,failed:0});els.batchTmdbDialog.showModal();renderBatchTmdbPending();batchTmdbUpdateUi(`准备扫描 ${targets.length} 部影视作品…`,0);for(const movie of targets){if(batchTmdbState.stop)break;const idx=batchTmdbState.processed+1;batchTmdbUpdateUi(`正在匹配 ${idx}/${targets.length} · 《${movie.info.title}》`,(batchTmdbState.processed/targets.length)*100);try{if(movie.info.tmdbId&&!seasonInfoFromMovie(movie)){const bundle=await fetchTmdbBundle(movie.info.tmdbId,movie.mediaType);applyTmdbBundleToMovie(movie,bundle);if(bundle.mediaType==='tv')collapseTvSeriesForMovie(movie,bundle);batchTmdbState.completed++}else{const match=await matchBatchTmdbMovie(movie);if(match.auto){const bundle=await fetchTmdbBundle(match.auto.id,match.auto.media_type||movie.mediaType);applyTmdbBundleToMovie(movie,bundle);if(bundle.mediaType==='tv')collapseTvSeriesForMovie(movie,bundle);batchTmdbState.completed++}else{if(!(match.candidates||[]).length)batchTmdbState.failed++;batchTmdbState.pending.push({movieId:movie.id,candidates:(match.candidates||[]).slice(0,6),reason:match.reason||'需要手动确认'})}}}catch(err){batchTmdbState.failed++;batchTmdbState.pending.push({movieId:movie.id,candidates:[],reason:`请求失败：${err.message}`})}batchTmdbState.processed++;if(batchTmdbState.processed%4===0)save();renderBatchTmdbPending();await new Promise(r=>setTimeout(r,80))}save();batchTmdbState.running=false;batchTmdbUpdateUi(batchTmdbState.stop?`已停止：完成 ${batchTmdbState.completed} 部，待确认 ${batchTmdbState.pending.length} 部`:`扫描完成：自动补全 ${batchTmdbState.completed} 部，待确认 ${batchTmdbState.pending.length} 部`,targets.length?batchTmdbState.processed/targets.length*100:100);els.batchTmdbInline.classList.add('hidden');renderAll();renderBatchTmdbPending();toastMsg(batchTmdbState.stop?'批量补全已停止':'TMDb 批量扫描完成')
  }
  function zhCountry(code,fallback=''){try{return new Intl.DisplayNames(['zh-CN'],{type:'region'}).of(code)||fallback}catch{return fallback||code}}
  function setTmdbSearchState(text,kind=''){els.tmdbMovieSearchState.textContent=text;els.tmdbMovieSearchState.className=`tmdb-search-state ${kind}`.trim()}
  function renderTmdbCandidates(results=[]){els.tmdbMovieResults.innerHTML='';els.tmdbMovieResults.classList.toggle('hidden',!results.length);for(const item of results){const row=document.createElement('div');row.className='tmdb-candidate';const poster=item.poster_path?`<div class="tmdb-thumb"><img src="${escAttr(tmdbImage(item.poster_path,'w92'))}" alt=""></div>`:'<div class="tmdb-thumb">无海报</div>';const type=item.media_type||els.movieMediaTypeInput.value||'movie',title=candidateTitle({...item,media_type:type}),original=candidateOriginal({...item,media_type:type}),year=candidateDate({...item,media_type:type}).slice(0,4)||'年份未知';row.innerHTML=`${poster}<div><div class="tmdb-candidate-title"><span class="tmdb-kind ${escAttr(type)}">${type==='tv'?'剧集':'电影'}</span>${esc(title||'未命名')}</div><div class="tmdb-candidate-meta">${esc(original&&original!==title?original+' · ':'')}${esc(year)} · TMDb ${esc(item.id)}</div></div><button type="button" data-tmdb-use="${escAttr(item.id)}" data-tmdb-type="${escAttr(type)}">选用</button>`;els.tmdbMovieResults.appendChild(row)}}
  async function searchTmdbMovie(){const q=(els.tmdbMovieQuery.value||els.movieTitleInput.value).trim();if(!q){setTmdbSearchState('请输入片名。','bad');return}appState.settings.tmdbLanguage=els.settingsTmdbLanguage?.value||appState.settings.tmdbLanguage||'zh-CN';setTmdbSearchState('正在搜索 TMDb…');els.tmdbMovieSearchBtn.disabled=true;try{const type=els.movieMediaTypeInput.value==='tv'?'tv':'movie',year=els.movieYearInput.value.trim(),params={query:q,include_adult:false,language:appState.settings.tmdbLanguage||'zh-CN',page:1};if(year)params[type==='tv'?'first_air_date_year':'primary_release_year']=year;const data=await tmdbFetch(type==='tv'?'/search/tv':'/search/movie',params);const results=(data.results||[]).slice(0,8).map(x=>({...x,media_type:type}));renderTmdbCandidates(results);setTmdbSearchState(results.length?`找到 ${results.length} 个${type==='tv'?'剧集':'电影'}候选，请选择正确作品。`:`没有找到匹配${type==='tv'?'剧集':'电影'}，可以尝试原名或清空年份。`,results.length?'ok':'bad')}catch(err){renderTmdbCandidates([]);setTmdbSearchState(`搜索失败：${err.message}`,'bad')}finally{els.tmdbMovieSearchBtn.disabled=false}}
  async function applyTmdbMovie(tmdbId,mediaType){setTmdbSearchState('正在读取作品详情与主创信息…');els.tmdbMovieSearchBtn.disabled=true;try{const type=mediaType==='tv'?'tv':'movie',bundle=await fetchTmdbBundle(tmdbId,type),d=bundle.detail||{},tmp=normalizeMovie({mediaType:type,info:{}});applyTmdbBundleToMovie(tmp,bundle,{overwrite:true});els.movieMediaTypeInput.value=type;updateMovieModalType();els.movieTitleInput.value=tmp.info.title||candidateTitle({...d,media_type:type})||'';els.movieOriginalTitleInput.value=tmp.info.originalTitle||'';els.movieYearInput.value=tmp.info.year||'';els.movieReleaseDateInput.value=tmp.info.releaseDate||'';els.movieLastAirDateInput.value=tmp.info.lastAirDate||'';els.movieRuntimeInput.value=tmp.info.runtime||'';els.movieSeasonsInput.value=tmp.info.numberOfSeasons??'';els.movieEpisodesInput.value=tmp.info.numberOfEpisodes??'';els.movieTvStatusInput.value=tmp.info.tvStatus||'';els.movieTmdbIdInput.value=d.id||tmdbId;els.movieDirectorInput.value=(tmp.info.directors||[]).join(' / ');els.movieCountryInput.value=(tmp.info.countries||[]).join(' / ');els.movieGenresInput.value=(tmp.info.genres||[]).join(' / ');els.movieOverviewInput.value=tmp.info.overview||'';els.moviePosterInput.value=tmp.info.posterUrl||'';els.tmdbMovieQuery.value=tmp.info.title||'';renderTmdbCandidates([]);setTmdbSearchState(`已关联 ${type==='tv'?'TV':'Movie'} · TMDb #${d.id}，保存后写入影视库。`,'ok')}catch(err){setTmdbSearchState(`补全失败：${err.message}`,'bad')}finally{els.tmdbMovieSearchBtn.disabled=false}}
  function tvStatusOptions(current='want'){const opts=[['want','想看'],['watching','在看'],['watched','已看完'],['paused','暂停'],['dropped','弃剧']];els.movieStatusInput.innerHTML=opts.map(([v,t])=>`<option value="${v}" ${v===current?'selected':''}>${t}</option>`).join('')}
  function movieStatusOptions(current='want'){const opts=[['follow','关注'],['want','想看'],['watched','已看']];els.movieStatusInput.innerHTML=opts.map(([v,t])=>`<option value="${v}" ${v===current?'selected':''}>${t}</option>`).join('')}
  function updateMovieModalType(currentStatus){const tv=els.movieMediaTypeInput.value==='tv';document.querySelectorAll('#movieModal .tv-extra').forEach(x=>x.classList.toggle('hidden',!tv));els.movieReleaseDateLabel.textContent=tv?'首播日期':'上映日期';els.movieRuntimeLabel.textContent=tv?'单集时长（分钟）':'时长（分钟）';els.movieCreatorInputLabel.textContent=tv?'主创':'导演';if(tv)tvStatusOptions(currentStatus||els.movieStatusInput.value);else movieStatusOptions(currentStatus||els.movieStatusInput.value)}
  function normalizeBulkTitleLine(line){let v=String(line||'').trim();v=v.replace(/^\s*(?:(?:[-*•]+)|(?:\d+[.、)）])|(?:[（(]\d+[）)]))\s*/,'').trim();v=v.replace(/^《(.+)》$/,'$1').trim();return v}
  function parseBulkTitles(raw){const seen=new Set(),rows=[];for(const line of String(raw||'').split(/\r?\n/)){const clean=normalizeBulkTitleLine(line);if(!clean)continue;const pair=doubanParseTitlePair(clean),title=String(pair.title||'').trim(),originalTitle=String(pair.originalTitle||'').trim();if(!title)continue;const key=cleanTmdbTitle(originalTitle||title);if(!key||seen.has(key))continue;seen.add(key);rows.push({title,originalTitle})}return rows}
  function bulkTitleExisting(row){
    const t=cleanTmdbTitle(row.title),o=cleanTmdbTitle(row.originalTitle);
    return (appState.movies||[]).find(m=>{const mt=cleanTmdbTitle(m.info?.title),mo=cleanTmdbTitle(m.info?.originalTitle);return (t&&(t===mt||t===mo))||(o&&(o===mt||o===mo))})||null;
  }
  function makeBulkTitleMovie(row){return normalizeMovie({id:uid(),mediaType:'unknown',info:{title:row.title,originalTitle:row.originalTitle},personal:{status:'want',rating:null,tags:[],shortReview:'',favorite:false},watchHistory:[],plans:[],radar:{discovered:false,ignored:false}})}
  function setBulkTitleBusy(busy){els.bulkAddTitlesText.disabled=busy;els.bulkAddTitlesOnly.disabled=busy;els.bulkAddTitlesMatch.disabled=busy;els.bulkAddTitlesCancel.disabled=busy;els.bulkAddTitlesClose.disabled=busy}
  function openBulkAddTitles(){els.bulkAddTitlesText.value='';els.bulkAddTitlesStatus.className='tmdb-status';els.bulkAddTitlesStatus.textContent='粘贴片名后即可批量匹配；不会使用年份或观看日期参与自动确认。';setBulkTitleBusy(false);els.bulkAddTitlesDialog.showModal();setTimeout(()=>els.bulkAddTitlesText.focus(),0)}
  function addBulkTitles(){
    const rows=parseBulkTitles(els.bulkAddTitlesText.value);if(!rows.length){els.bulkAddTitlesStatus.className='tmdb-status bad';els.bulkAddTitlesStatus.textContent='没有识别到片名，请按“每行一部”粘贴片单。';return}
    let added=0,skipped=0;for(const row of rows){if(bulkTitleExisting(row)){skipped++;continue}appState.movies.push(makeBulkTitleMovie(row));added++}
    save();renderAll();els.bulkAddTitlesDialog.close();toastMsg(`批量添加完成：新增 ${added} 部${skipped?` · 跳过 ${skipped} 部已存在作品`:''}`)
  }
  async function addBulkTitlesAutoMatch(){
    const rows=parseBulkTitles(els.bulkAddTitlesText.value);if(!rows.length){els.bulkAddTitlesStatus.className='tmdb-status bad';els.bulkAddTitlesStatus.textContent='没有识别到片名，请按“每行一部”粘贴片单。';return}
    try{tmdbCredential()}catch(err){els.bulkAddTitlesStatus.className='tmdb-status bad';els.bulkAddTitlesStatus.textContent='请先在“设置 → TMDb”填写并保存 API Key / Token，再使用自动匹配。';return}
    setBulkTitleBusy(true);let added=0,skipped=0,auto=0,pending=0,failed=0;
    try{
      for(let i=0;i<rows.length;i++){
        const row=rows[i];els.bulkAddTitlesStatus.className='tmdb-status';els.bulkAddTitlesStatus.textContent=`正在匹配 ${i+1}/${rows.length} · 《${row.title}》`;
        if(bulkTitleExisting(row)){skipped++;continue}
        const movie=makeBulkTitleMovie(row);
        try{
          const match=await matchBatchTmdbMovie(movie);
          if(match.auto){
            const mt=match.auto.media_type||'movie',existing=(appState.movies||[]).find(m=>m.mediaType===mt&&Number(m.info?.tmdbId)===Number(match.auto.id));if(existing){skipped++;continue}
            appState.movies.push(movie);const bundle=await fetchTmdbBundle(match.auto.id,mt);applyTmdbBundleToMovie(movie,bundle);if(bundle.mediaType==='tv')collapseTvSeriesForMovie(movie,bundle);removeMatchRow(movie.id);added++;auto++;
          }else{
            appState.movies.push(movie);added++;if((match.candidates||[]).length){upsertMatchRow({movieId:movie.id,status:'pending',candidates:match.candidates,reason:match.reason||'存在多个候选，需要确认'});pending++}else{upsertMatchRow({movieId:movie.id,status:'noresult',candidates:[],reason:match.reason||'TMDb 没有返回候选'});failed++}
          }
        }catch(err){appState.movies.push(movie);added++;failed++;upsertMatchRow({movieId:movie.id,status:'failed',candidates:[],reason:`请求失败：${err.message}`})}
        if((i+1)%2===0)save();await new Promise(r=>setTimeout(r,80));
      }
      save();renderAll();els.bulkAddTitlesStatus.className='tmdb-status good';els.bulkAddTitlesStatus.textContent=`完成：输入 ${rows.length} · 自动关联 ${auto} · 已存在 ${skipped} · 待确认 ${pending} · 未找到/失败 ${failed}`;toastMsg(`批量导入完成：自动关联 ${auto} 部${pending||failed?` · ${pending+failed} 部待处理`:''}`);
      if(pending||failed){setTimeout(()=>{els.bulkAddTitlesDialog.close();setView('match');renderMatchCenter()},450)}else setTimeout(()=>els.bulkAddTitlesDialog.close(),450)
    }finally{setBulkTitleBusy(false)}
  }
  function openMovieModal(id=null){libraryState.editingId=id;const m=id?appState.movies.find(x=>x.id===id):normalizeMovie({mediaType:'movie'});els.movieModalTitle.textContent=id?'编辑影视资料':'添加影视';els.movieMediaTypeInput.value=m?.mediaType==='tv'?'tv':'movie';updateMovieModalType(m?.personal.status||'want');els.movieTitleInput.value=m?.info.title||'';els.movieOriginalTitleInput.value=m?.info.originalTitle||'';els.movieYearInput.value=m?.info.year||'';els.movieReleaseDateInput.value=m?.info.releaseDate||m?.info.firstAirDate||'';els.movieLastAirDateInput.value=m?.info.lastAirDate||'';els.movieRuntimeInput.value=m?.info.runtime||'';els.movieSeasonsInput.value=m?.info.numberOfSeasons??'';els.movieEpisodesInput.value=m?.info.numberOfEpisodes??'';els.movieTvStatusInput.value=m?.info.tvStatus||'';els.movieTmdbIdInput.value=m?.info.tmdbId||'';els.movieDirectorInput.value=(m?.info.directors||[]).join(' / ');els.movieCountryInput.value=(m?.info.countries||[]).join(' / ');els.movieGenresInput.value=(m?.info.genres||[]).join(' / ');els.movieOverviewInput.value=m?.info.overview||'';els.movieRatingInput.value=m?.personal.rating??'';els.movieTagsInput.value=(m?.personal.tags||[]).join(' / ');els.moviePosterInput.value=m?.info.posterUrl||'';els.tmdbMovieQuery.value=m?.info.title||'';renderTmdbCandidates([]);setTmdbSearchState(m?.info.tmdbId?`已关联 ${m.mediaType==='tv'?'TV':'Movie'} · TMDb #${m.info.tmdbId}`:'');els.movieDeleteBtn.classList.toggle('hidden',!id);els.movieModal.showModal()}
  function saveMovieFromModal(){const title=els.movieTitleInput.value.trim();if(!title)return;const tmdbId=els.movieTmdbIdInput.value?Number(els.movieTmdbIdInput.value):null,type=els.movieMediaTypeInput.value==='tv'?'tv':'movie';let m=libraryState.editingId?appState.movies.find(x=>x.id===libraryState.editingId):null;if(!m&&tmdbId)m=appState.movies.find(x=>Number(x.info?.tmdbId)===tmdbId&&x.mediaType===type);if(!m){m=normalizeMovie({id:uid(),mediaType:type});appState.movies.push(m)}m.mediaType=type;m.info.title=title;m.info.originalTitle=els.movieOriginalTitleInput.value.trim();m.info.year=els.movieYearInput.value?Number(els.movieYearInput.value):null;m.info.releaseDate=els.movieReleaseDateInput.value||'';m.info.firstAirDate=type==='tv'?(els.movieReleaseDateInput.value||''):'';m.info.lastAirDate=type==='tv'?(els.movieLastAirDateInput.value||''):'';m.info.numberOfSeasons=type==='tv'&&els.movieSeasonsInput.value!==''?Number(els.movieSeasonsInput.value):null;m.info.numberOfEpisodes=type==='tv'&&els.movieEpisodesInput.value!==''?Number(els.movieEpisodesInput.value):null;m.info.tvStatus=type==='tv'?els.movieTvStatusInput.value.trim():'';m.info.runtime=els.movieRuntimeInput.value?Number(els.movieRuntimeInput.value):null;m.info.tmdbId=tmdbId;m.info.directors=splitList(els.movieDirectorInput.value);m.info.countries=splitList(els.movieCountryInput.value);m.info.genres=splitList(els.movieGenresInput.value);m.info.overview=els.movieOverviewInput.value.trim();m.info.posterUrl=els.moviePosterInput.value.trim();m.personal.status=els.movieStatusInput.value;m.personal.rating=els.movieRatingInput.value!==''?Number(els.movieRatingInput.value):null;m.personal.tags=splitList(els.movieTagsInput.value);if(m.personal.status==='watched'&&!m.watchHistory.length)m.watchHistory=[{date:localToday(),rating:m.personal.rating,note:'',venue:''}];m.updatedAt=new Date().toISOString();if(m.info.tmdbId&&!movieNeedsTmdb(m))removeMatchRow(m.id);save();renderAll();if(els.matchView&&!els.matchView.classList.contains('hidden'))renderMatchCenter();els.movieModal.close();toastMsg(`《${title}》已保存 · ${mediaTypeLabel(m)}${tmdbId?' · TMDb 已关联':''}`)}
  function deleteEditing(){if(!libraryState.editingId)return;const m=appState.movies.find(x=>x.id===libraryState.editingId);if(!m)return;if(!confirm(`删除《${m.info.title}》？`))return;appState.movies=appState.movies.filter(x=>x.id!==m.id);libraryState.selected.delete(m.id);save();renderAll();els.movieModal.close();toastMsg('已删除作品')}


  const {parseMonth,monthLabel,shiftMonth,statusText,statusClass}=window.CineversePlan;
  function planEntries(month){return Domain.planEntries(appState,month)}
  function getPlan(movie,month=planState.month){return Domain.getPlan(movie,month)}
  window.CineversePlan.normalizePlans(appState.movies);
  function renderPlanPage(){
    const entries=planEntries(planState.month),{done,deferred,planned,total,percent:pct,minutes}=window.CineversePlan.summarize(entries);
    els.planPageMonthLabel.textContent=monthLabel(planState.month);els.planCalendarTitle.textContent=monthLabel(planState.month);els.planPageTotal.textContent=total;els.planPageDone.textContent=done;els.planPagePercent.textContent=pct+'%';els.planPageMinutes.textContent=minutes;els.planPageSummary.textContent=total?`${monthLabel(planState.month)}共有 ${total} 部计划，已完成 ${done} 部${deferred?`，延期 ${deferred} 部`:''}。`:`${monthLabel(planState.month)}还没有观影计划。`;
    els.planRing.style.setProperty('--pct',pct+'%');els.planRingPercent.textContent=pct+'%';els.planProgressCopy.textContent=`${done} / ${total}`;
    const bar=(label,count,cls='')=>`<div class="plan-status-row ${cls}"><span>${label}</span><div class="plan-status-track"><i style="width:${total?Math.round(count/total*100):0}%"></i></div><b>${count}</b></div>`;els.planStatusBars.innerHTML=bar('计划中',planned)+bar('已完成',done,'done')+bar('已延期',deferred,'defer');
    renderPlanCalendar(entries);renderPlanUnscheduled(entries);renderPlanListPage(entries);applyPlanView();
  }
  function renderPlanCalendar(entries){
    const {y,m}=parseMonth(planState.month),days=new Date(y,m,0).getDate(),firstRaw=new Date(y,m-1,1).getDay(),first=firstRaw===0?6:firstRaw-1,today=localToday();els.planCalendar.innerHTML='';
    for(let i=0;i<first;i++){const blank=document.createElement('div');blank.className='plan-day blank';els.planCalendar.appendChild(blank)}
    for(let d=1;d<=days;d++){const date=`${y}-${String(m).padStart(2,'0')}-${String(d).padStart(2,'0')}`,cell=document.createElement('div');cell.className='plan-day'+(date===today?' today':'');cell.innerHTML=`<div class="plan-day-number">${d}</div><button class="plan-day-add" data-plan-day="${date}" title="安排影视">＋</button><div class="plan-day-movies"></div>`;const list=cell.querySelector('.plan-day-movies');entries.filter(x=>x.plan.plannedDate===date).forEach(({movie,plan})=>{const chip=document.createElement('div');chip.className=`plan-day-chip ${statusClass(plan.status)}`;chip.dataset.openDetail=movie.id;chip.title=`${movie.info.title} · ${statusText(plan.status)}`;chip.textContent=movie.info.title;list.appendChild(chip)});els.planCalendar.appendChild(cell)}
    const used=first+days,tail=(7-used%7)%7;for(let i=0;i<tail;i++){const blank=document.createElement('div');blank.className='plan-day blank';els.planCalendar.appendChild(blank)}
  }
  function renderPlanUnscheduled(entries){const list=entries.filter(x=>!x.plan.plannedDate&&x.plan.status!=='deferred');els.planUnscheduledCount.textContent=`${list.length} 部`;els.planUnscheduled.innerHTML=list.length?list.map(({movie})=>`<div class="unscheduled-item"><div><b>${esc(movie.info.title)}</b><span>${movie.info.runtime?movie.info.runtime+' 分钟':'时长未知'} · ${movie.personal?.rating!=null?'★ '+Number(movie.personal.rating).toFixed(1):'未评分'}</span></div><button data-schedule-id="${escAttr(movie.id)}">安排日期</button></div>`).join(''):'<div class="plan-empty" style="padding:18px 8px">这个月的计划都有日期了 ✦</div>'}
  function renderPlanListPage(entries){
    const sorted=[...entries].sort((a,b)=>String(a.plan.plannedDate||'9999').localeCompare(String(b.plan.plannedDate||'9999'))||a.movie.info.title.localeCompare(b.movie.info.title,'zh-Hans-CN'));
    if(!sorted.length){els.planPageList.innerHTML='<div class="plan-empty">这个月还没有影视作品进入轨道。<br><button data-plan-add-empty>＋ 安排第一部影视作品</button></div>';return}
    els.planPageList.innerHTML=sorted.map(({movie,plan})=>`<div class="plan-list-row"><div class="plan-list-movie"><div class="plan-list-poster">${movie.info.posterUrl?`<img src="${escAttr(movie.info.posterUrl)}" alt="${escAttr(movie.info.title)} 海报" loading="lazy">`:''}</div><div><b>${esc(movie.info.title)}</b><span>${movie.info.year||'年份未知'} · ${esc((movie.info.directors||[])[0]||'导演未知')}</span></div></div><input type="date" value="${escAttr(plan.plannedDate||'')}" data-plan-date-id="${escAttr(movie.id)}"><select data-plan-status-id="${escAttr(movie.id)}"><option value="planned" ${plan.status==='planned'?'selected':''}>计划中</option><option value="completed" ${plan.status==='completed'?'selected':''}>已完成</option><option value="deferred" ${plan.status==='deferred'?'selected':''}>已延期</option></select><div class="library-count">${movie.info.runtime?movie.info.runtime+' 分钟':'—'}</div><div class="plan-row-actions"><button data-open-detail="${escAttr(movie.id)}">详情</button><button data-plan-defer-id="${escAttr(movie.id)}">延期到下月</button><button class="remove" data-plan-remove-id="${escAttr(movie.id)}">移除</button></div></div>`).join('')
  }
  function applyPlanView(){const cal=planState.view==='calendar';els.planCalendarWrap.classList.toggle('hidden',!cal);els.planSide.classList.toggle('hidden',!cal);els.planListPanel.classList.toggle('hidden',cal);els.planCalendarBtn.classList.toggle('active',cal);els.planListBtn.classList.toggle('active',!cal)}
  function openPlanMovieModal(prefillDate='',movieId=''){planState.prefillDate=prefillDate||'';const existing=new Set(planEntries(planState.month).map(x=>x.movie.id));let candidates=(appState.movies||[]).filter(m=>!existing.has(m.id)||m.id===movieId);if(!candidates.length){toastMsg('这个月你的收藏都已经加入计划了');return}els.planMovieSelect.innerHTML=candidates.map(m=>`<option value="${escAttr(m.id)}" ${m.id===movieId?'selected':''}>${esc(m.info.title)}${m.info.year?' · '+m.info.year:''}</option>`).join('');els.planMovieMonth.value=monthLabel(planState.month);els.planMovieDate.min=`${planState.month}-01`;const {y,m}=parseMonth(planState.month);els.planMovieDate.max=`${planState.month}-${String(new Date(y,m,0).getDate()).padStart(2,'0')}`;els.planMovieDate.value=prefillDate||'';els.planMovieModal.showModal()}
  function savePlanMovie(){const movie=appState.movies.find(m=>m.id===els.planMovieSelect.value);if(!movie)return;movie.plans=movie.plans||[];let p=getPlan(movie,planState.month);if(!p){p={month:planState.month,status:'planned',plannedDate:null};movie.plans.push(p)}p.status=p.status==='deferred'?'planned':p.status;p.plannedDate=els.planMovieDate.value||null;movie.updatedAt=new Date().toISOString();save();renderAll();renderPlanPage();els.planMovieModal.close();toastMsg(`《${movie.info.title}》已进入 ${monthLabel(planState.month)}`)}
  function deferPlan(movieId){const movie=appState.movies.find(m=>m.id===movieId),p=movie&&getPlan(movie,planState.month);if(!movie||!p)return;const next=shiftMonth(planState.month,1);p.status='deferred';p.movedTo=next;let np=getPlan(movie,next);if(!np){np={month:next,status:'planned',plannedDate:null,movedFrom:planState.month};movie.plans.push(np)}movie.updatedAt=new Date().toISOString();save();renderAll();renderPlanPage();toastMsg(`《${movie.info.title}》已延期到 ${monthLabel(next)}`)}
  function removePlan(movieId){const movie=appState.movies.find(m=>m.id===movieId);if(!movie)return;movie.plans=(movie.plans||[]).filter(p=>p.month!==planState.month);movie.updatedAt=new Date().toISOString();save();renderAll();renderPlanPage();toastMsg('已从这个月的计划中移除')}


  function watchEntries(){return Domain.watchEntries(appState)}
  const isSeasonSourceWatch=Domain.isSeasonSourceWatch;
  function sessionWatchEntries(){return watchEntries().filter(x=>!isSeasonSourceWatch(x.movie,x.watch))}
  function seasonMarkEntries(){return watchEntries().filter(x=>isSeasonSourceWatch(x.movie,x.watch))}
  function renderWatchedFilters(){
    const rows=watchEntries();const years=uniq(rows.map(x=>String(x.watch.date||'').slice(0,4)).filter(x=>/^\d{4}$/.test(x))).sort((a,b)=>b-a);populateSelect(els.watchedYear,years);populateSelect(els.watchedDirector,uniq(rows.flatMap(x=>x.movie.info.directors||[])).sort((a,b)=>a.localeCompare(b,'zh-Hans-CN')));populateSelect(els.watchedCountry,uniq(rows.flatMap(x=>x.movie.info.countries||[])).sort((a,b)=>a.localeCompare(b,'zh-Hans-CN')))
  }
  function filteredWatchEntries(){
    const q=els.watchedKeyword.value.trim().toLowerCase(),year=els.watchedYear.value,rating=els.watchedRating.value,director=els.watchedDirector.value,country=els.watchedCountry.value;let rows=watchEntries().filter(({movie,watch})=>{const hay=[movie.info.title,movie.info.originalTitle,(movie.info.directors||[]).join(' '),(movie.info.countries||[]).join(' '),(movie.personal?.tags||[]).join(' '),watch.note,watch.venue].join(' ').toLowerCase();if(q&&!hay.includes(q))return false;if(year&&!String(watch.date||'').startsWith(year))return false;if(director&&!(movie.info.directors||[]).includes(director))return false;if(country&&!(movie.info.countries||[]).includes(country))return false;if(rating==='unrated'&&watch.rating!=null)return false;if(rating&&rating!=='unrated'&&(watch.rating==null||Number(watch.rating)<Number(rating)))return false;return true});const sort=els.watchedSort.value;rows.sort((a,b)=>{if(sort==='oldest')return String(a.watch.date||'9999').localeCompare(String(b.watch.date||'9999'));if(sort==='ratingDesc')return (Number(b.watch.rating)||-1)-(Number(a.watch.rating)||-1)||String(b.watch.date||'').localeCompare(String(a.watch.date||''));if(sort==='titleAsc')return a.movie.info.title.localeCompare(b.movie.info.title,'zh-Hans-CN');return String(b.watch.date||'').localeCompare(String(a.watch.date||''))});return rows
  }
  function renderWatchedStats(){
    const allYear=watchEntries().filter(x=>String(x.watch.date||'').startsWith(String(NOW_YEAR))),rows=allYear.filter(x=>!isSeasonSourceWatch(x.movie,x.watch)),seasonMarks=allYear.filter(x=>isSeasonSourceWatch(x.movie,x.watch)),ids=new Set(allYear.map(x=>x.movie.id)),ratings=rows.map(x=>x.watch.rating).filter(x=>x!=null).map(Number),minutes=rows.filter(x=>x.movie.mediaType!=='tv').reduce((n,x)=>n+(Number(x.movie.info.runtime)||0),0);const byMovie={};rows.forEach(x=>byMovie[x.movie.id]=(byMovie[x.movie.id]||0)+1);const rewatch=Object.values(byMovie).reduce((n,c)=>n+Math.max(0,c-1),0);els.watchedYearFilms.textContent=ids.size;els.watchedYearSessions.textContent=rows.length;if(els.watchedSeasonMarks)els.watchedSeasonMarks.textContent=seasonMarks.length;els.watchedYearMinutes.textContent=minutes>=60?`${Math.floor(minutes/60)}h ${minutes%60}m`:`${minutes}m`;els.watchedAvgRating.textContent=ratings.length?(ratings.reduce((a,b)=>a+b,0)/ratings.length).toFixed(1):'—';els.watchedRewatchCount.textContent=rewatch
  }
  function renderWatchedSide(rows){
    const yearRows=sessionWatchEntries().filter(x=>String(x.watch.date||'').startsWith(String(NOW_YEAR))),monthCounts=Array(12).fill(0);yearRows.forEach(x=>{const m=Number(String(x.watch.date||'').slice(5,7));if(m>=1&&m<=12)monthCounts[m-1]++});const max=Math.max(1,...monthCounts);els.watchedMonthlyBars.innerHTML=monthCounts.map((c,i)=>`<div class="watched-bar"><i style="height:${Math.max(4,Math.round(c/max*100))}%"></i><span>${i+1}月</span></div>`).join('');const ratedRows=rows.filter(x=>!isSeasonSourceWatch(x.movie,x.watch)),bins=[['9-10',x=>x>=9],['8-8.9',x=>x>=8&&x<9],['7-7.9',x=>x>=7&&x<8],['<7',x=>x!=null&&x<7],['未评',x=>x==null]],counts=bins.map(([_,fn])=>ratedRows.filter(x=>fn(x.watch.rating==null?null:Number(x.watch.rating))).length),rmax=Math.max(1,...counts);els.watchedRatingDist.innerHTML=bins.map(([label],i)=>`<div class="rating-row"><span>${label}</span><div class="rating-track"><i style="width:${Math.round(counts[i]/rmax*100)}%"></i></div><b>${counts[i]}</b></div>`).join('');const countsByMovie=new Map();sessionWatchEntries().forEach(x=>countsByMovie.set(x.movie.id,(countsByMovie.get(x.movie.id)||0)+1));const top=[...countsByMovie.entries()].filter(([,c])=>c>1).map(([id,c])=>({movie:appState.movies.find(m=>m.id===id),count:c})).filter(x=>x.movie).sort((a,b)=>b.count-a.count||(b.movie.personal?.rating||0)-(a.movie.personal?.rating||0)).slice(0,5);els.watchedRewatchList.innerHTML=top.length?top.map(x=>`<div class="rewatch-item" data-open-detail="${escAttr(x.movie.id)}"><b>${esc(x.movie.info.title)}</b><span>${x.count} 次</span></div>`).join(''):'<div class="watched-empty" style="padding:20px 5px">还没有重看记录</div>'
  }
  function renderWatchedPage(resetPage=false){
    renderWatchedFilters();renderWatchedStats();if(resetPage)watchedState.page=1;const rows=filteredWatchEntries(),totalPages=Math.max(1,Math.ceil(rows.length/watchedState.pageSize));watchedState.page=Math.min(watchedState.page,totalPages);const start=(watchedState.page-1)*watchedState.pageSize,pageRows=rows.slice(start,start+watchedState.pageSize);els.watchedShownText.textContent=`${rows.length} 条观看 / 标记记录`;els.watchedPageText.textContent=`第 ${watchedState.page} / ${totalPages} 页`;els.watchedPrev.disabled=watchedState.page<=1;els.watchedNext.disabled=watchedState.page>=totalPages;els.watchedFeed.innerHTML='';if(!pageRows.length){els.watchedFeed.innerHTML='<div class="watched-empty">没有符合当前筛选的观影记录。</div>'}else{const groups=new Map();pageRows.forEach(row=>{const month=String(row.watch.date||'日期未知').slice(0,7)||'日期未知';if(!groups.has(month))groups.set(month,[]);groups.get(month).push(row)});for(const [month,items] of groups){const group=document.createElement('section');group.className='watch-month-group';const label=/^\d{4}-\d{2}$/.test(month)?`${month.slice(0,4)} 年 ${Number(month.slice(5))} 月`:month;group.innerHTML=`<div class="watch-month-title"><b>${esc(label)}</b><span>${items.length} 条记录</span></div><div class="watch-feed-list"></div>`;const list=group.querySelector('.watch-feed-list');items.forEach(({movie,watch})=>{const seasonMark=isSeasonSourceWatch(movie,watch),sessions=(movie.watchHistory||[]).filter(w=>w.date&&!isSeasonSourceWatch(movie,w)).slice().sort((a,b)=>String(a.date).localeCompare(String(b.date))),ordinal=seasonMark?0:sessions.indexOf(watch)+1;const item=document.createElement('article');item.className='watch-feed-item';item.dataset.openDetail=movie.id;const day=String(watch.date||'').slice(8,10)||'—',weekday=watch.date?['日','一','二','三','四','五','六'][new Date(watch.date+'T12:00:00').getDay()]:'—',posterUrl=String(movie.info.posterUrl||'');const meta=seasonMark?`📺 剧集 · 第 ${watch.sourceSeason} 季 · 豆瓣分季标记`:`${mediaTypeIcon(movie)} ${mediaTypeLabel(movie)} · ${movie.info.year||'年份未知'} · ${esc((movie.info.directors||[])[0]||(movie.mediaType==='tv'?'主创未知':'导演未知'))} · ${esc(watch.venue||'未记录观影场景')}`;const state=seasonMark?`第 ${watch.sourceSeason} 季标记`:(ordinal>1?`第 ${ordinal} 次 · 重看`:'初次观看');item.innerHTML=`<div class="watch-feed-date"><strong>${esc(day)}</strong><span>周${weekday}</span></div><div class="watch-feed-poster" role="img" aria-label="${escAttr(movie.info.title)} 海报"></div><div class="watch-feed-main"><b>${esc(movie.info.title)}</b><div class="watch-feed-meta">${meta}</div><div class="watch-feed-note">${esc(watch.note||movie.personal?.shortReview||(seasonMark?'来自豆瓣的分季看过记录。':'这次观看还没有留下文字。'))}</div></div><div class="watch-feed-score"><strong>${watch.rating!=null?'★ '+Number(watch.rating).toFixed(1):'未评分'}</strong><span class="${!seasonMark&&ordinal>1?'rewatch':''}">${state}</span></div>`;const watchedPoster=item.querySelector('.watch-feed-poster');if(watchedPoster&&posterUrl){watchedPoster.style.backgroundImage=`url(${JSON.stringify(posterUrl)})`}list.appendChild(item)});els.watchedFeed.appendChild(group)}}renderWatchedSide(rows)
  }


  function allTimeMovieDurationStats(){
    const rows=sessionWatchEntries().filter(x=>x.movie.mediaType!=='tv');
    const counted=rows.filter(x=>Number(x.movie.info.runtime)>0);
    const minutes=counted.reduce((n,x)=>n+Number(x.movie.info.runtime),0);
    return {minutes,counted:counted.length,missing:rows.length-counted.length,total:rows.length};
  }
  function statsRows(scope){return scope==='all'?watchEntries():watchEntries().filter(x=>String(x.watch.date||'').startsWith(String(scope)))}
  function statsMoviesForRows(rows){const map=new Map();rows.forEach(x=>{if(!map.has(x.movie.id))map.set(x.movie.id,x.movie)});return [...map.values()]}
  function statsYears(){const ys=uniq(watchEntries().map(x=>String(x.watch.date||'').slice(0,4)).filter(x=>/^\d{4}$/.test(x))).map(Number).sort((a,b)=>b-a);if(!ys.includes(NOW_YEAR))ys.unshift(NOW_YEAR);return ys}
  function countValues(movies,getter){const map=new Map();movies.forEach(m=>{uniq(getter(m)||[]).forEach(v=>{if(v)map.set(v,(map.get(v)||0)+1)})});return [...map.entries()].map(([name,count])=>({name,count})).sort((a,b)=>b.count-a.count||String(a.name).localeCompare(String(b.name),'zh-Hans-CN'))}
  function renderRankList(el,rows){if(!rows.length){el.innerHTML='<div class="stats-empty">暂无数据</div>';return}const max=Math.max(1,...rows.map(x=>x.count));el.innerHTML=rows.slice(0,5).map(x=>`<div class="rank-row"><span title="${escAttr(x.name)}">${esc(x.name)}</span><div class="rank-track"><i style="width:${Math.round(x.count/max*100)}%"></i></div><b>${x.count}</b></div>`).join('')}
  function statsSummaryText(scope,rows,movies){const all=scope==='all',period=all?'全部历史':`${scope} 年`,sessions=rows.filter(x=>!isSeasonSourceWatch(x.movie,x.watch)),seasonMarks=rows.filter(x=>isSeasonSourceWatch(x.movie,x.watch)),ratings=sessions.map(x=>x.watch.rating).filter(x=>x!=null).map(Number),avg=ratings.length?ratings.reduce((a,b)=>a+b,0)/ratings.length:null,movieRows=sessions.filter(x=>x.movie.mediaType!=='tv'),duration=movieRows.filter(x=>Number(x.movie.info.runtime)>0),minutes=duration.reduce((n,x)=>n+Number(x.movie.info.runtime),0),dirs=countValues(movies,m=>m.info.directors||[]),tags=countValues(movies,m=>m.personal?.tags||[]),nine=new Set(sessions.filter(x=>x.watch.rating!=null&&Number(x.watch.rating)>=9).map(x=>x.movie.id)).size,movieCount=movies.filter(m=>m.mediaType!=='tv').length,tvCount=movies.filter(m=>m.mediaType==='tv').length;let rhythm='';if(all){const byYear=new Map();sessions.forEach(x=>{const y=String(x.watch.date||'').slice(0,4);if(/^\d{4}$/.test(y))byYear.set(y,(byYear.get(y)||0)+1)});const top=[...byYear.entries()].sort((a,b)=>b[1]-a[1])[0];if(top)rhythm=`${top[0]} 年是历史观看记录最密集的一年，共 ${top[1]} 次。`}else{const monthCounts=Array(12).fill(0);sessions.forEach(x=>{const m=Number(String(x.watch.date||'').slice(5,7));if(m>=1&&m<=12)monthCounts[m-1]++});const peak=Math.max(...monthCounts,0),peakMonth=peak?monthCounts.indexOf(peak)+1:null;if(peakMonth)rhythm=`${peakMonth} 月是这一年实际观看最密集的月份，共 ${peak} 次。`}return `${period}，你记录了 ${movies.length} 部不同影视作品（电影 ${movieCount} 部，剧集 ${tvCount} 部），共留下 ${sessions.length} 次观看${seasonMarks.length?`，另有 ${seasonMarks.length} 条豆瓣剧集分季标记`:''}。电影累计时长约 ${minutes>=60?(minutes/60).toFixed(1)+' 小时':minutes+' 分钟'}${duration.length<movieRows.length?`（另有 ${movieRows.length-duration.length} 条电影观看缺少片长）`:''}${avg!=null?`，平均评分 ${avg.toFixed(1)}`:''}。${nine?`其中有 ${nine} 部作品在实际观看记录中获得 9 分以上评价。`:''}${rhythm}${dirs[0]?`最常出现的主创/导演是 ${dirs[0].name}。`:''}${tags[0]?`最常出现的个人标签是「${tags[0].name}」。`:''}`}
  function renderStatsPage(){
    const years=statsYears(),validScopes=new Set(['all',...years.map(String)]);if(!validScopes.has(String(statsState.year)))statsState.year=String(NOW_YEAR);els.statsYear.innerHTML=`<option value="all" ${statsState.year==='all'?'selected':''}>全部</option>`+years.map(y=>`<option value="${y}" ${String(statsState.year)===String(y)?'selected':''}>${y} 年</option>`).join('');const scope=String(statsState.year),all=scope==='all',rows=statsRows(scope),sessions=rows.filter(x=>!isSeasonSourceWatch(x.movie,x.watch)),seasonMarks=rows.filter(x=>isSeasonSourceWatch(x.movie,x.watch)),movies=statsMoviesForRows(rows),ratings=sessions.map(x=>x.watch.rating).filter(x=>x!=null).map(Number),durationRows=sessions.filter(x=>x.movie.mediaType!=='tv'),countedDuration=durationRows.filter(x=>Number(x.movie.info.runtime)>0),scopeMinutes=countedDuration.reduce((n,x)=>n+Number(x.movie.info.runtime),0),missingDuration=durationRows.length-countedDuration.length,avg=ratings.length?ratings.reduce((a,b)=>a+b,0)/ratings.length:null,nine=new Set(sessions.filter(x=>x.watch.rating!=null&&Number(x.watch.rating)>=9).map(x=>x.movie.id)).size;const byMovie={};sessions.forEach(x=>byMovie[x.movie.id]=(byMovie[x.movie.id]||0)+1);const rewatch=Object.values(byMovie).reduce((n,c)=>n+Math.max(0,c-1),0);
    els.statsFilmCount.textContent=movies.length;els.statsMovieCount.textContent=movies.filter(m=>m.mediaType!=='tv').length;els.statsTvCount.textContent=movies.filter(m=>m.mediaType==='tv').length;els.statsSessionCount.textContent=sessions.length;if(els.statsSeasonMarks)els.statsSeasonMarks.textContent=seasonMarks.length;els.statsTotalHours.textContent=scopeMinutes>=60?`${Math.floor(scopeMinutes/60)}h ${scopeMinutes%60}m`:`${scopeMinutes}m`;if(els.statsTotalHoursNote)els.statsTotalHoursNote.textContent=`${all?'全历史':scope+' 年'} · 已统计 ${countedDuration.length} 条${missingDuration?` · ${missingDuration} 条缺片长`:''}`;els.statsAvgRating.textContent=avg!=null?avg.toFixed(1):'—';els.statsNinePlus.textContent=nine;els.statsRewatch.textContent=rewatch;
    if(all){const yCounts=new Map();sessions.forEach(x=>{const y=String(x.watch.date||'').slice(0,4);if(/^\d{4}$/.test(y))yCounts.set(y,(yCounts.get(y)||0)+1)});const series=[...yCounts.entries()].sort((a,b)=>Number(a[0])-Number(b[0])).slice(-12),max=Math.max(1,...series.map(x=>x[1]));els.statsMonthlyChart.style.gridTemplateColumns=`repeat(${Math.max(1,series.length)},1fr)`;els.statsMonthlyChart.innerHTML=series.length?series.map(([y,c])=>`<div class="stats-month-col"><div class="stats-month-bars"><em>${c} 次</em><i style="height:${Math.max(c?7:2,Math.round(c/max*100))}%"></i></div><span>${esc(y)}</span></div>`).join(''):'<div class="stats-empty" style="grid-column:1/-1">暂无历史观看记录</div>';els.statsYearCompare.textContent=`全部历史 · ${sessions.length} 次实际观看`;els.statsPaceTitle.textContent='历年观影节奏';els.statsPaceLegend.textContent='最近 12 个有记录年份'}else{const curr=Array(12).fill(0);sessions.forEach(x=>{const m=Number(String(x.watch.date||'').slice(5,7));if(m>=1&&m<=12)curr[m-1]++});const max=Math.max(1,...curr);els.statsMonthlyChart.style.gridTemplateColumns='repeat(12,1fr)';els.statsMonthlyChart.innerHTML=curr.map((c,i)=>`<div class="stats-month-col"><div class="stats-month-bars"><em>${c} 次</em><i style="height:${Math.max(c?7:2,Math.round(c/max*100))}%"></i></div><span>${i+1}月</span></div>`).join('');els.statsYearCompare.textContent=`${scope} 年 · ${sessions.length} 次实际观看`;els.statsPaceTitle.textContent='年度观影节奏';els.statsPaceLegend.textContent='所选年份'}
    const bins=[{label:'9.0–10',color:'#f4c96b',fn:v=>v>=9},{label:'8.0–8.9',color:'#a87cff',fn:v=>v>=8&&v<9},{label:'7.0–7.9',color:'#6587ed',fn:v=>v>=7&&v<8},{label:'7分以下',color:'rgba(127,140,173,.55)',fn:v=>v<7}],counts=bins.map(b=>ratings.filter(b.fn).length),rated=Math.max(1,ratings.length),p9=counts[0]/rated*100,p8=counts[1]/rated*100,p7=counts[2]/rated*100;els.statsRatingDonut.style.setProperty('--p9',p9);els.statsRatingDonut.style.setProperty('--p8',p8);els.statsRatingDonut.style.setProperty('--p7',p7);els.statsDonutAvg.textContent=avg!=null?avg.toFixed(1):'—';els.statsRatedCount.textContent=`${ratings.length} 条实际观看评分`;els.statsRatingLegend.innerHTML=bins.map((b,i)=>`<div class="donut-legend-row"><i class="donut-dot" style="background:${b.color}"></i><span>${b.label}</span><b>${counts[i]}</b></div>`).join('');
    const dirs=countValues(movies,m=>m.info.directors||[]),topRated=sessions.filter(x=>x.watch.rating!=null).slice().sort((a,b)=>Number(b.watch.rating)-Number(a.watch.rating)||String(b.watch.date||'').localeCompare(String(a.watch.date||'')))[0];let peakLabel='—',peakSmall='暂无观看记录';if(all){const byYear=new Map();sessions.forEach(x=>{const y=String(x.watch.date||'').slice(0,4);if(/^\d{4}$/.test(y))byYear.set(y,(byYear.get(y)||0)+1)});const peak=[...byYear.entries()].sort((a,b)=>b[1]-a[1])[0];if(peak){peakLabel=peak[0]+' 年';peakSmall=peak[1]+' 次观看'}}else{const curr=Array(12).fill(0);sessions.forEach(x=>{const m=Number(String(x.watch.date||'').slice(5,7));if(m>=1&&m<=12)curr[m-1]++});const n=Math.max(...curr,0),pm=n?curr.indexOf(n)+1:null;if(pm){peakLabel=pm+' 月';peakSmall=n+' 次观看'}}els.statsInsights.innerHTML=`<div class="stats-insight"><span>${all?'最密集年份':'最密集月份'}</span><b>${peakLabel}</b><small>${peakSmall}</small></div><div class="stats-insight"><span>最高评分</span><b>${topRated?esc(topRated.movie.info.title):'—'}</b><small>${topRated?'★ '+Number(topRated.watch.rating).toFixed(1):'暂无评分'}</small></div><div class="stats-insight"><span>最常见主创</span><b>${dirs[0]?esc(dirs[0].name):'—'}</b><small>${dirs[0]?dirs[0].count+' 部作品':'暂无数据'}</small></div>`;
    els.statsInsightsTitle.textContent=all?'历史观看的三个坐标':'这一年的三个坐标';els.statsPreferenceNote.textContent=all?'按全部历史看过的不同影视作品统计':'按本年度看过的不同影视作品统计';els.statsTopTitle.textContent=all?'历史高分影视':'年度高分影视';els.statsTopNote.textContent=all?'按全部实际观看评分':'按当年实际观看评分';els.statsSummaryTitle.textContent=all?'历史影视摘要':'年度影视摘要';
    renderRankList(els.statsDirectors,dirs);renderRankList(els.statsCountries,countValues(movies,m=>m.info.countries||[]));renderRankList(els.statsGenres,countValues(movies,m=>m.info.genres||[]));renderRankList(els.statsTags,countValues(movies,m=>m.personal?.tags||[]));
    const filmScores=new Map();sessions.forEach(x=>{if(x.watch.rating!=null){if(!filmScores.has(x.movie.id))filmScores.set(x.movie.id,{movie:x.movie,ratings:[]});filmScores.get(x.movie.id).ratings.push(Number(x.watch.rating))}});const top=[...filmScores.values()].map(x=>({...x,score:x.ratings.reduce((a,b)=>a+b,0)/x.ratings.length})).sort((a,b)=>b.score-a.score||b.ratings.length-a.ratings.length).slice(0,5);els.statsTopFilms.innerHTML=top.length?top.map((x,i)=>`<div class="stats-film-row" data-open-detail="${escAttr(x.movie.id)}"><div class="rank">${i+1}</div><div><b>${esc(x.movie.info.title)}</b><span>${x.movie.info.year||'年份未知'} · ${esc((x.movie.info.directors||[])[0]||(x.movie.mediaType==='tv'?'主创未知':'导演未知'))}${x.ratings.length>1?' · '+x.ratings.length+'次观看':''}</span></div><strong>★ ${x.score.toFixed(1)}</strong></div>`).join(''):`<div class="stats-empty">${all?'历史记录中':'这一年'}还没有实际观看评分记录</div>`;els.statsSummaryCopy.innerHTML=`<b>${all?'全部历史':scope+' 年'} · 你的影视星图</b><br>${esc(statsSummaryText(scope,rows,movies))}`;
  }


  function formatDate(v){if(!v)return '—';const d=String(v);return /^\d{4}-\d{2}-\d{2}$/.test(d)?d:d.slice(0,10)||'—'}
  function isCurrentWeek(v){if(!/^\d{4}-\d{2}-\d{2}/.test(String(v||'')))return false;const d=new Date(String(v).slice(0,10)+'T12:00:00'),now=new Date();now.setHours(12,0,0,0);const day=(now.getDay()+6)%7,start=new Date(now);start.setDate(now.getDate()-day);const end=new Date(start);end.setDate(start.getDate()+7);return d>=start&&d<end}
  function currentDetailMovie(){return appState.movies.find(m=>m.id===detailState.movieId)||detailState.detachedMovie}
  function openDetail(id){const m=appState.movies.find(x=>x.id===id);if(!m)return;detailState.movieId=id;detailState.detachedMovie=null;setView('detail',{hash:'detail/'+encodeURIComponent(id)})}
  function renderDetail(){
    const m=currentDetailMovie();if(!m)return;const info=m.info||{},personal=m.personal||{},tv=m.mediaType==='tv',history=[...(m.watchHistory||[])].sort((a,b)=>String(b.date||'').localeCompare(String(a.date||''))),plans=m.plans||[],currentPlan=plans.find(p=>p.month===CURRENT_MONTH)||plans.find(p=>p.status!=='deferred');
    els.detailTitle.textContent=info.title||(tv?'未命名剧集':'未命名电影');els.detailOriginal.textContent=[info.originalTitle,info.year].filter(Boolean).join(' · ')||String(info.year||'');els.detailCreatorLabel.textContent=tv?'主创':'导演';els.detailDirectors.textContent=(info.directors||[]).join(' / ')||'未知';els.detailCountries.textContent=(info.countries||[]).join(' / ')||'未知';els.detailGenres.textContent=(info.genres||[]).join(' / ')||'待补全';els.detailRuntimeLabel.textContent=tv?'单集时长':'片长';els.detailRuntime.textContent=info.runtime?`${info.runtime} 分钟`:'未知';els.detailReleaseLabel.textContent=tv?'首播':'上映';els.detailRelease.textContent=(tv?(info.firstAirDate||info.releaseDate):info.releaseDate)||info.year||'未知';els.detailMediaType.textContent=mediaTypeLabel(m);els.detailSeriesFact.classList.toggle('hidden',!tv);els.detailSeriesMeta.textContent=tv?[info.numberOfSeasons!=null?info.numberOfSeasons+'季':null,info.numberOfEpisodes!=null?info.numberOfEpisodes+'集':null,info.lastAirDate?(info.lastAirDate.slice(0,4)+' '+(info.tvStatus||'')):info.tvStatus].filter(Boolean).join(' · ')||'待补全':'—';els.detailTmdb.textContent=info.tmdbId||'未关联';els.detailOverview.textContent=info.overview||`暂无简介。可在编辑${tv?'剧集':'电影'}时通过 TMDb 搜索补全资料。`;
    els.detailPoster.classList.toggle('has-image',Boolean(info.posterUrl));els.detailPoster.innerHTML=info.posterUrl?`<img src="${escAttr(info.posterUrl)}" alt="${escAttr(info.title)} 海报">`:`<div class="poster-title">${esc(info.title||mediaTypeLabel(m))}</div>`;
    els.detailFavorite.classList.toggle('on',Boolean(personal.favorite));const wants=personal.want==null?personal.status==='want':Boolean(personal.want),watched=history.length>0;els.detailStatusBtn.textContent=wants?'已想看':'想看';els.detailStatusBtn.classList.toggle('primary',wants);els.detailAddWatchBtn.textContent=watched?'已看过':'看过';els.detailAddWatchBtn.classList.toggle('primary',watched);els.detailPlanBtn.textContent=currentPlan?'已加入计划':'加入计划';
    els.detailTags.innerHTML=(personal.tags||[]).length?(personal.tags||[]).map(t=>`<span>${esc(t)}</span>`).join(''):'<span>尚未添加个人标签</span>';els.detailRating.textContent=personal.rating!=null?personal.rating.toFixed(1):'—';els.detailStars.textContent=personal.rating==null?'☆☆☆☆☆':'★★★★★'.slice(0,Math.max(1,Math.round(personal.rating/2)))+'☆☆☆☆☆'.slice(Math.max(1,Math.round(personal.rating/2)));els.detailLastWatch.textContent=history[0]?.date?`最近观看：${formatDate(history[0].date)}`:'尚未记录观看日期';els.detailPlanMeta.textContent=currentPlan?`${currentPlan.plannedDate||currentPlan.month} · ${currentPlan.status==='completed'?'已完成':currentPlan.status==='deferred'?'延期':'计划中'}`:'未加入观影计划';
    const r=m.radar||{},publicScore=info.tmdbVoteAverage??r.publicReputation;els.detailRadarBadge.textContent='大众口碑';els.detailRadarDate.textContent='';els.detailPublicScore.textContent=publicScore!=null?Number(publicScore).toFixed(1):'—';els.detailMatchScore.textContent='';els.detailRadarReason.textContent='';const publicStars=$('detailPublicStars'),publicFilled=publicScore==null?0:Math.max(1,Math.round(Number(publicScore)/2));if(publicStars)publicStars.textContent='★★★★★'.slice(0,publicFilled)+'☆☆☆☆☆'.slice(publicFilled);
    renderWatchHistory(m,history);renderRelated(m);
  }
  function renderWatchHistory(m,history){els.detailWatchList.innerHTML='';if(!history.length){els.detailWatchList.innerHTML='<div class="watch-empty">还没有观影记录。看完之后，把这一晚留在这里。</div>'}else history.forEach((w,i)=>{const seasonMark=isSeasonSourceWatch(m,w),el=document.createElement('div');el.className='watch-entry';el.innerHTML=`<div class="watch-date">${esc(formatDate(w.date))}</div><div><div class="watch-rating">${w.rating!=null?'★ '+Number(w.rating).toFixed(1):'未评分'}${seasonMark?` · 第 ${w.sourceSeason} 季`:''}</div><div class="watch-copy">${seasonMark?'豆瓣分季标记':esc(w.venue||'未记录观影场景')}${w.note?`<br>${esc(w.note)}`:''}</div></div><button title="删除这条记录" data-delete-watch="${i}">×</button>`;els.detailWatchList.appendChild(el)});
    const sessions=history.filter(w=>!isSeasonSourceWatch(m,w)),seasonMarks=history.filter(w=>isSeasonSourceWatch(m,w));els.detailWatchCount.textContent=sessions.length+(seasonMarks.length?` + ${seasonMarks.length}季标记`:'');const asc=[...history].filter(w=>w.date).sort((a,b)=>String(a.date||'9999').localeCompare(String(b.date||'9999')));els.detailFirstWatch.textContent=asc[0]?.date?String(asc[0].date).slice(0,10):'—';els.detailTotalMinutes.textContent=m.mediaType==='tv'?'—':(m.info.runtime||0)*sessions.length;const ratings=sessions.slice().reverse().map(x=>x.rating).filter(x=>x!=null);els.detailTrend.innerHTML=ratings.length?ratings.map(v=>`<div class="trend-bar" style="height:${Math.max(12,Number(v)*9)}%"><span>${Number(v).toFixed(1)}</span></div>`).join(''):'<div class="watch-empty" style="width:100%">暂无实际观看评分趋势</div>';
  }
  function renderRelated(m){const tags=new Set(m.personal?.tags||[]),dirs=new Set(m.info.directors||[]);const related=(appState.movies||[]).filter(x=>x.id!==m.id).map(x=>({m:x,score:(x.personal?.tags||[]).filter(t=>tags.has(t)).length*2+(x.info.directors||[]).filter(d=>dirs.has(d)).length*3})).filter(x=>x.score>0).sort((a,b)=>b.score-a.score||(b.m.personal?.rating||0)-(a.m.personal?.rating||0)).slice(0,4);els.detailRelated.innerHTML=(related.length?related.map(({m:x})=>`<article class="related-card" data-related-id="${escAttr(x.id)}"><div class="related-poster">${x.info.posterUrl?`<img src="${escAttr(x.info.posterUrl)}" alt="${escAttr(x.info.title)} 海报" loading="lazy">`:''}</div><div class="related-body"><div class="related-title">${esc(x.info.title)}</div><div class="related-meta">${x.info.year||'年份未知'} · ${esc((x.info.directors||[])[0]||'导演未知')}</div><div class="related-score">${x.personal?.rating!=null?'★ '+x.personal.rating.toFixed(1):'未评分'}</div></div></article>`).join(''):'<div class="watch-empty" style="grid-column:1/-1">暂无足够的关联收藏</div>')}

  function normalizeRadarItem(r,index=0){
    const title=String(r?.title||'').trim();
    return {id:r?.id||('radar-'+uid()),title,year:r?.year?Number(r.year):null,releaseDate:r?.releaseDate||'',meta:r?.meta||[r?.year,r?.runtime?`${r.runtime} 分钟`:null].filter(Boolean).join(' · '),public:r?.public!==''&&r?.public!=null?Number(r.public):null,match:r?.match!==''&&r?.match!=null?Number(r.match):null,badge:r?.badge||'值得关注',kind:r?.kind||((Number(r?.match)||0)>=93?'hot':(Number(r?.public)||0)>=9?'good':'focus'),poster:r?.poster||('p'+((index%4)+1)),posterUrl:r?.posterUrl||'',category:r?.category||'week',reason:r?.reason||'这部电影值得继续观察，等待更多口碑与个人观看判断。',discoveredAt:r?.discoveredAt||localToday(),source:r?.source||'电影雷达',ignored:Boolean(r?.ignored),runtime:r?.runtime?Number(r.runtime):null,directors:Array.isArray(r?.directors)?r.directors:splitList(r?.directors),countries:Array.isArray(r?.countries)?r.countries:splitList(r?.countries),genres:Array.isArray(r?.genres)?r.genres:splitList(r?.genres),tmdbId:r?.tmdbId||null};
  }
  function radarItems(){appState.home=appState.home||{};appState.home.radar=(appState.home.radar||[]).filter(r=>!String(r?.id||'').startsWith('radar-demo-')).map(normalizeRadarItem);return appState.home.radar}
  function findMovieForRadar(r){return appState.movies.find(m=>m.info.title===r.title&&(!r.year||!m.info.year||Number(m.info.year)===Number(r.year)))||null}
  function radarMovieWasWatched(m){return Boolean(m&&m.mediaType!=='tv'&&(m.personal?.status==='watched'||(m.watchHistory||[]).some(w=>!isSeasonSourceWatch(m,w))))}
  function radarMatchesLibraryMovie(r,m){if(!m||m.mediaType==='tv')return false;if(r.tmdbId&&m.info?.tmdbId&&Number(r.tmdbId)===Number(m.info.tmdbId))return true;const norm=v=>String(v||'').toLowerCase().normalize('NFKC').replace(/[\s·・:：,，.。!！?？'"“”‘’()（）\[\]【】_-]+/g,'');const rt=norm(r.title),mt=norm(m.info?.title),mo=norm(m.info?.originalTitle);if(!rt||!(rt===mt||rt===mo))return false;return !r.year||!m.info?.year||Number(r.year)===Number(m.info.year)}
  function radarAlreadyWatched(r){return (appState.movies||[]).some(m=>radarMovieWasWatched(m)&&radarMatchesLibraryMovie(r,m))}
  function collectRadarItem(r,{plan=false}={}){
    let m=findMovieForRadar(r);
    if(!m){m=normalizeMovie({id:uid(),info:{title:r.title,originalTitle:'',year:r.year,releaseDate:r.releaseDate,directors:r.directors,countries:r.countries,runtime:r.runtime,genres:r.genres,posterUrl:r.posterUrl,overview:'',tmdbId:r.tmdbId},personal:{status:'want',rating:null,tags:[],shortReview:'',favorite:false},watchHistory:[],plans:[],radar:{discovered:true,discoveredAt:r.discoveredAt,publicReputation:r.public,matchScore:r.match,reason:r.reason,source:r.source,ignored:false}});appState.movies.push(m)}
    else {m.radar={...(m.radar||{}),discovered:true,discoveredAt:r.discoveredAt||m.radar?.discoveredAt||localToday(),publicReputation:r.public,matchScore:r.match,reason:r.reason,source:r.source,ignored:false};if(m.personal.status==='follow')m.personal.status='want'}
    if(plan&&!getPlan(m,CURRENT_MONTH))m.plans.push({month:CURRENT_MONTH,status:'planned',plannedDate:null,movedTo:null});
    m.updatedAt=new Date().toISOString();save();renderAll();if(!els.radarView.classList.contains('hidden'))renderRadarPage();return m;
  }
  function collectExternalDetail(model,action='want'){
    const existing=(appState.movies||[]).find(m=>Number(model.tmdbId)&&Number(m.info?.tmdbId)===Number(model.tmdbId)&&m.mediaType===model.type);
    const now=new Date().toISOString();
    const m=existing||normalizeMovie({id:uid(),mediaType:model.type,info:{title:model.title,originalTitle:model.originalTitle,year:model.year,releaseDate:model.releaseDate,firstAirDate:model.releaseDate,directors:model.creators,countries:model.countries,runtime:model.runtime,genres:model.genres,posterUrl:model.posterUrl,overview:model.overview,tmdbId:model.tmdbId},personal:{status:action==='watch'?'watched':'want',rating:null,tags:[],shortReview:'',favorite:false},watchHistory:[],plans:[],radar:model.source==='radar'?{discovered:true,discoveredAt:model.discoveredAt,publicReputation:model.publicScore,matchScore:model.matchScore,reason:model.reason,source:'电影雷达',ignored:false}:{discovered:false,ignored:false},createdAt:now,updatedAt:now});
    if(!existing)appState.movies.push(m);
    m.personal=m.personal||{};
    m.personal.status=action==='watch'?'watched':'want';
    m.plans=m.plans||[];
    if(action==='plan'&&!getPlan(m,CURRENT_MONTH))m.plans.push({month:CURRENT_MONTH,status:'planned',plannedDate:null,movedTo:null});
    m.updatedAt=now;
    save();renderAll();openDetail(m.id);
    if(action==='watch')openWatchModal();
    else toastMsg(action==='plan'?`《${m.info.title}》已加入影视库和本月计划`:`《${m.info.title}》已加入影视库，状态为想看`);
    return m;
  }
  window.CineverseLibrary=window.CineverseLibrary.extend({collectExternalDetail});
  function radarFiltered(){
    const q=(els.radarKeyword?.value||'').trim().toLowerCase(),min=Number(els.radarMatch?.value||0),sort=els.radarSort?.value||'matchDesc';let rows=radarItems().filter(r=>{
      if(radarState.tab==='week'&&(r.category!=='week'||r.ignored||!isCurrentWeek(r.discoveredAt)))return false;
      if(radarState.tab==='upcoming'&&(r.category!=='upcoming'||r.ignored))return false;
      if(radarState.tab==='high'&&((r.public||0)<9||r.ignored))return false;
      if(radarState.tab!=='history'&&r.ignored)return false;
      if(q&&![r.title,r.reason,r.source,r.badge,r.meta].join(' ').toLowerCase().includes(q))return false;
      if(min&&(r.match||0)<min)return false;return true;
    });
    rows.sort((a,b)=>sort==='publicDesc'?(b.public||0)-(a.public||0):sort==='releaseAsc'?String(a.releaseDate||'9999').localeCompare(String(b.releaseDate||'9999')):sort==='newest'?String(b.discoveredAt||'').localeCompare(String(a.discoveredAt||'')):(b.match||0)-(a.match||0));return rows;
  }
  function radarTasteTags(){
    const counts=new Map();appState.movies.filter(m=>(m.personal?.rating||0)>=8.5||m.personal?.favorite).forEach(m=>(m.personal?.tags||[]).forEach(t=>counts.set(t,(counts.get(t)||0)+1)));return [...counts.entries()].sort((a,b)=>b[1]-a[1]).slice(0,8).map(x=>x[0]);
  }
  function renderRadarPage(){
    const all=radarItems(),rows=radarFiltered();els.radarTotal.textContent=all.length;els.radarStrong.textContent=all.filter(r=>!r.ignored&&((r.match||0)>=93||r.badge==='强烈推荐')).length;els.radarHigh.textContent=all.filter(r=>!r.ignored&&(r.public||0)>=9).length;els.radarCollected.textContent=all.filter(r=>findMovieForRadar(r)).length;
    document.querySelectorAll('[data-radar-tab]').forEach(b=>b.classList.toggle('active',b.dataset.radarTab===radarState.tab));els.radarPageGrid.innerHTML='';els.radarEmpty.classList.toggle('hidden',rows.length>0);
    rows.forEach((r,i)=>{const m=findMovieForRadar(r),planned=m&&getPlan(m,CURRENT_MONTH),card=document.createElement('article');card.className='radar-result-card'+(r.ignored?' ignored':'');const art=r.posterUrl?`<img src="${escAttr(r.posterUrl)}" alt="${escAttr(r.title)} 海报" loading="lazy">`:`<div class="poster-name">${esc(r.title)}</div>`;card.innerHTML=`<div class="radar-result-poster ${escAttr(r.poster||('p'+((i%4)+1)))}">${art}<span class="radar-result-badge ${escAttr(r.kind||'focus')}">${esc(r.ignored?'已忽略':r.badge)}</span></div><div class="radar-result-body"><div class="radar-result-title">${esc(r.title)}</div><div class="radar-result-meta">${esc(r.releaseDate?`${r.releaseDate} · ${r.source}`:(r.meta||r.source||'电影雷达'))}</div><div class="radar-score-row"><div class="radar-score-box public"><span>公众口碑</span><b>${r.public!=null?'★ '+Number(r.public).toFixed(1):'—'}</b></div><div class="radar-score-box match"><span>与你匹配</span><b>${r.match!=null?`${Math.round(r.match)}%`:'—'}</b></div></div><div class="radar-reason">${esc(r.reason)}</div><div class="radar-card-actions">${m?`<button class="success" data-radar-detail="${escAttr(r.id)}">已收藏 · 查看详情</button>`:`<button data-radar-collect="${escAttr(r.id)}">＋ 收藏</button>`}<button ${planned?'class="success"':''} data-radar-plan="${escAttr(r.id)}">${planned?'✓ 已在本月计划':'＋ 加入计划'}</button><button class="wide ${r.ignored?'success':'muted'}" data-radar-ignore="${escAttr(r.id)}">${r.ignored?'恢复关注':'忽略这次推荐'}</button></div></div>`;els.radarPageGrid.appendChild(card)});
    const tags=radarTasteTags();els.radarTasteTags.innerHTML=tags.length?tags.map(t=>`<span>${esc(t)}</span>`).join(''):'<span>暂无足够个人标签</span>';const active=all.filter(r=>!r.ignored&&isCurrentWeek(r.discoveredAt)),pulse=Math.min(100,Math.round(active.length/12*100));els.radarPulseBar.style.width=pulse+'%';els.radarPulseCopy.textContent=active.length?`本周捕捉到 ${active.length} 部真实雷达信号，其中 ${active.filter(r=>(r.match||0)>=90).length} 部与你高度匹配。`:'本周还没有雷达信号。';const signals=[['90%+ 高匹配',active.filter(r=>(r.match||0)>=90).length],['9.0+ 高口碑',active.filter(r=>(r.public||0)>=9).length],['即将上映',active.filter(r=>r.category==='upcoming').length]];els.radarSignalList.innerHTML=signals.map(([k,v])=>`<div class="radar-side-item"><span>${esc(k)}</span><b>${v}</b></div>`).join('');
  }
  function radarPreferenceGenreNames(){const counts=new Map();for(const m of (appState.movies||[])){const weight=(m.personal?.favorite?3:0)+(Number(m.personal?.rating)>=9?3:Number(m.personal?.rating)>=8?1:0);if(!weight)continue;for(const g of (m.info?.genres||[]))counts.set(g,(counts.get(g)||0)+weight)}return [...counts.entries()].sort((a,b)=>b[1]-a[1]).slice(0,10).map(x=>x[0])}
  async function updateRadarFromTmdb(){if(els.radarAutoUpdateBtn.disabled)return;try{tmdbCredential()}catch(err){toastMsg(err.message);setView('settings');return}els.radarAutoUpdateBtn.disabled=true;els.radarAutoUpdateBtn.textContent='正在读取 TMDb…';try{const language=appState.settings.tmdbLanguage||'zh-CN';const [trend,upcoming,genres]=await Promise.all([tmdbFetch('/trending/movie/week',{language}),tmdbFetch('/movie/upcoming',{language,region:'CN',page:1}),tmdbFetch('/genre/movie/list',{language})]);const genreMap=new Map((genres.genres||[]).map(g=>[g.id,g.name])),prefs=new Set(radarPreferenceGenreNames()),existing=radarItems(),today=localToday();const scoreMatch=x=>{const names=(x.genre_ids||[]).map(id=>genreMap.get(id)).filter(Boolean),overlap=names.filter(g=>prefs.has(g)).length;return Math.max(55,Math.min(98,60+overlap*10+(Number(x.vote_average)||0)*2+((Number(x.vote_count)||0)>=1000?3:0)))};const make=(x,category)=>normalizeRadarItem({id:`tmdb-${category}-${x.id}-${today}`,title:x.title||x.original_title||'',year:Number(String(x.release_date||'').slice(0,4))||null,releaseDate:x.release_date||'',public:x.vote_average!=null?Number(x.vote_average):null,match:scoreMatch(x),badge:scoreMatch(x)>=90?'强烈推荐':Number(x.vote_average)>=8.5?'高口碑':'值得关注',category,reason:`TMDb 自动雷达：${prefs.size?'与你高评分/收藏作品的类型偏好进行匹配；':''}本周热度与公开评分共同排序。`,discoveredAt:today,source:'TMDb 自动雷达',posterUrl:tmdbImage(x.poster_path,'w500'),genres:(x.genre_ids||[]).map(id=>genreMap.get(id)).filter(Boolean),tmdbId:x.id});const rawCandidates=[];for(const x of (trend.results||[]).filter(x=>x.id&&x.title).slice(0,20))rawCandidates.push(make(x,'week'));for(const x of (upcoming.results||[]).filter(x=>x.id&&x.title).slice(0,20))rawCandidates.push(make(x,'upcoming'));const skippedWatched=rawCandidates.filter(r=>radarAlreadyWatched(r)).length,candidates=rawCandidates.filter(r=>!radarAlreadyWatched(r));const byKey=new Map(existing.map(r=>[`${r.source==='TMDb 自动雷达'?'auto':'other'}|${r.tmdbId||r.title}|${r.category}`,r]));for(const r of candidates){const key=`auto|${r.tmdbId||r.title}|${r.category}`;const old=byKey.get(key);if(old){Object.assign(old,{...r,id:old.id,ignored:old.ignored})}else existing.push(r)}appState.home.radar=existing;save();renderAll();renderRadarPage();toastMsg(`已从 TMDb 更新雷达：本周 ${candidates.filter(x=>x.category==='week').length} 部 · 即将上映 ${candidates.filter(x=>x.category==='upcoming').length} 部${skippedWatched?` · 已排除 ${skippedWatched} 部看过电影`:''}`)}catch(err){toastMsg(`雷达更新失败：${err.message}`)}finally{els.radarAutoUpdateBtn.disabled=false;els.radarAutoUpdateBtn.textContent='✦ 从 TMDb 更新'}}

  function importRadarJSON(){
    const raw=els.radarImportText.value.trim();if(!raw){toastMsg('请先粘贴雷达 JSON');return}let parsed;try{parsed=JSON.parse(raw)}catch{toastMsg('JSON 格式无法解析');return}const arr=Array.isArray(parsed)?parsed:Array.isArray(parsed?.radar)?parsed.radar:null;if(!arr){toastMsg('需要 JSON 数组，或包含 radar 数组的对象');return}const parsedIncoming=arr.map(normalizeRadarItem).filter(r=>r.title),skippedWatched=parsedIncoming.filter(r=>radarAlreadyWatched(r)).length,incoming=parsedIncoming.filter(r=>!radarAlreadyWatched(r));if(!parsedIncoming.length){toastMsg('没有识别到有效电影');return}if(!incoming.length){toastMsg(`没有可导入的新电影：${skippedWatched} 部都已经看过`);return}const list=radarItems();for(const r of incoming){const idx=list.findIndex(x=>x.title===r.title&&(!r.year||!x.year||Number(x.year)===Number(r.year)));if(idx>=0)list[idx]={...list[idx],...r,id:list[idx].id};else list.push(r)}appState.home.radar=list;save();els.radarImportDialog.close();els.radarImportText.value='';renderAll();renderRadarPage();toastMsg(`已导入 ${incoming.length} 条电影雷达${skippedWatched?` · 已排除 ${skippedWatched} 部看过电影`:''}`);
  }


  function hexToRgb(hex){const v=String(hex||'#9f7cff').replace('#','');if(!/^[0-9a-fA-F]{6}$/.test(v))return {r:159,g:124,b:255};return {r:parseInt(v.slice(0,2),16),g:parseInt(v.slice(2,4),16),b:parseInt(v.slice(4,6),16)}}
  function shadeHex(hex,amount){const {r,g,b}=hexToRgb(hex),cl=x=>Math.max(0,Math.min(255,x));return '#'+[cl(r+amount),cl(g+amount),cl(b+amount)].map(x=>Math.round(x).toString(16).padStart(2,'0')).join('')}
  function applyAppearance(){
    appState.settings=normalizeSettings(appState.settings);const st=appState.settings,p=THEME_PRESETS[st.themePreset]||THEME_PRESETS.nebula,root=document.documentElement,accent=st.accentColor||p.purple,op=Math.max(.45,Math.min(.94,Number(st.panelOpacity||72)/100));
    root.style.setProperty('--bg-0',p.bg0);root.style.setProperty('--bg-1',p.bg1);root.style.setProperty('--bg-2',p.bg2);root.style.setProperty('--purple',accent);root.style.setProperty('--purple-2',p.purple2||shadeHex(accent,-38));root.style.setProperty('--blue',p.blue);root.style.setProperty('--cyan',p.cyan);root.style.setProperty('--gold',p.gold);root.style.setProperty('--gold-2',p.gold2||p.gold);root.style.setProperty('--text',p.text||'#f7f3ff');root.style.setProperty('--muted',p.muted||'#aab3cf');root.style.setProperty('--muted-2',p.muted2||'#7f8aaa');root.style.setProperty('--line',p.line||'rgba(161,179,255,.16)');root.style.setProperty('--line-bright',p.lineBright||'rgba(173,140,255,.34)');root.style.setProperty('--shadow',p.shadow||'0 28px 70px rgba(0,0,0,.28)');root.style.setProperty('--panel',`rgba(${p.panelRgb},${op})`);root.style.setProperty('--panel-strong',`rgba(${p.panelRgb},${Math.min(.97,op+.18)})`);root.style.setProperty('--panel-soft',`rgba(${p.panelRgb},${Math.max(.35,op-.14)})`);
    const themeId=['forest','snow','ocean'].includes(st.themePreset)?st.themePreset:'star';document.body.dataset.uiTheme=themeId;root.dataset.uiTheme=themeId;root.style.colorScheme=themeId==='snow'||themeId==='ocean'?'light':'dark';
    document.body.classList.toggle('motion-off',!st.motion);document.body.classList.toggle('stars-low',st.starDensity==='low');document.body.classList.toggle('stars-high',st.starDensity==='high');
    const wallpaper=st.wallpaperDataUrl,backdrop=document.querySelector('.theme-backdrop');if(themeId!=='star'){document.body.style.backgroundImage='none';document.body.style.backgroundAttachment='scroll';if(backdrop){const tint=themeId==='forest'?'linear-gradient(180deg,rgba(4,11,6,.10),rgba(4,10,6,.46))':themeId==='snow'?'linear-gradient(180deg,rgba(238,248,255,.04),rgba(222,239,250,.20))':'linear-gradient(180deg,rgba(246,253,250,.02),rgba(242,249,245,.13))';backdrop.style.backgroundImage=wallpaper?`${tint},url(${JSON.stringify(wallpaper).slice(1,-1)})`:'';backdrop.style.backgroundSize='cover';backdrop.style.backgroundPosition='center'}}else{if(backdrop)backdrop.removeAttribute('style');document.body.style.backgroundImage=wallpaper?`linear-gradient(145deg,rgba(4,8,23,.72),rgba(8,18,42,.78)),radial-gradient(circle at 78% 10%,rgba(130,82,255,.18),transparent 25%),url(${JSON.stringify(wallpaper).slice(1,-1)})`:`radial-gradient(circle at 78% 10%,rgba(130,82,255,.18),transparent 25%),radial-gradient(circle at 22% 82%,rgba(64,126,255,.13),transparent 32%),linear-gradient(145deg,var(--bg-0),var(--bg-1) 44%,var(--bg-2) 78%,var(--bg-0))`;document.body.style.backgroundSize=wallpaper?'cover,auto,cover':'auto';document.body.style.backgroundPosition=wallpaper?'center,center,center':'center';document.body.style.backgroundAttachment='fixed'}
  }
  const LEGACY_SEED_RATINGS={1:9.8,2:9.6,3:9.4,4:9.3,5:9.2,8:9,9:8.9,11:8.8,13:9.7,15:8.6,16:8.6,18:8.5,19:9.5,20:9.3,21:9.2,22:9,23:8.7,24:8.6,25:8.5};
  function legacySeedIndex(movie){const m=String(movie?.id||'').match(/^demo-(\d+)$/);return m?Number(m[1]):null}
  function legacySeedSyntheticDate(index){if(!index||!(index in LEGACY_SEED_RATINGS))return'';return `2026-${String(((index-1)%7)+1).padStart(2,'0')}-${String((((index-1)*3)%27)+1).padStart(2,'0')}`}
  function legacySeedCleanupPreview(){let legacyMovies=0,syntheticWatches=0,syntheticPlans=0;for(const m of (appState.movies||[])){const i=legacySeedIndex(m);if(!i)continue;legacyMovies++;const d=legacySeedSyntheticDate(i),r=LEGACY_SEED_RATINGS[i];syntheticWatches+=(m.watchHistory||[]).filter(w=>d&&String(w.date||'')===d&&Number(w.rating)===Number(r)&&!String(w.venue||'').trim()&&!String(w.note||'').trim()&&!w.sourceDoubanId).length;const seedMonth=String(m.createdAt||'').slice(0,7);if(seedMonth&&['demo-12','demo-19','demo-20','demo-26'].includes(m.id))syntheticPlans+=(m.plans||[]).filter(p=>p.month===seedMonth&&!p.plannedDate&&((m.id==='demo-19'||m.id==='demo-20')?p.status==='completed':p.status==='planned')).length}return{legacyMovies,syntheticWatches,syntheticPlans}}
  function updateLegacySeedStatus(){if(!els.settingsLegacySeedStatus)return;const x=legacySeedCleanupPreview();els.settingsLegacySeedStatus.className='tmdb-status '+((x.syntheticWatches+x.syntheticPlans)?'bad':'ok');els.settingsLegacySeedStatus.textContent=x.legacyMovies?`检测到 ${x.legacyMovies} 条旧版演示来源作品；其中可安全清理 ${x.syntheticWatches} 条虚构观看记录、${x.syntheticPlans} 条演示计划。作品本体不会删除。`:'没有检测到旧版演示来源记录。'}
  function cleanupLegacySeedArtifacts(){const x=legacySeedCleanupPreview();if(!(x.syntheticWatches+x.syntheticPlans)){updateLegacySeedStatus();toastMsg('没有可安全清理的旧版演示痕迹');return}if(!confirm(`将删除 ${x.syntheticWatches} 条可精确识别的旧演示观看记录和 ${x.syntheticPlans} 条演示计划。\n\n不会删除作品本体、豆瓣导入记录或 TMDb 资料。继续吗？`))return;let watches=0,plans=0;for(const m of (appState.movies||[])){const i=legacySeedIndex(m);if(!i)continue;const d=legacySeedSyntheticDate(i),r=LEGACY_SEED_RATINGS[i],before=(m.watchHistory||[]).length;m.watchHistory=(m.watchHistory||[]).filter(w=>!(d&&String(w.date||'')===d&&Number(w.rating)===Number(r)&&!String(w.venue||'').trim()&&!String(w.note||'').trim()&&!w.sourceDoubanId));watches+=before-m.watchHistory.length;const seedMonth=String(m.createdAt||'').slice(0,7);if(seedMonth&&['demo-12','demo-19','demo-20','demo-26'].includes(m.id)){const pb=(m.plans||[]).length;m.plans=(m.plans||[]).filter(p=>!(p.month===seedMonth&&!p.plannedDate&&((m.id==='demo-19'||m.id==='demo-20')?p.status==='completed':p.status==='planned')));plans+=pb-m.plans.length}if(before!==m.watchHistory.length)m.updatedAt=new Date().toISOString()}save();renderAll();renderSettings();toastMsg(`已清理 ${watches} 条旧演示观看记录与 ${plans} 条演示计划`) }

  function applyProfile(){
    appState.settings=normalizeSettings(appState.settings);const st=appState.settings,name=String(st.profileName||'电影爱好者').trim()||'电影爱好者',role=String(st.profileRole??'').trim(),avatar=String(st.profileAvatarDataUrl||'');
    if(els.profileName)els.profileName.textContent=name;if(els.profileRole){els.profileRole.textContent=role;els.profileRole.classList.toggle('hidden',!role)}if(els.homeGreetingName)els.homeGreetingName.textContent=name;
    if(els.profileAvatar){els.profileAvatar.classList.toggle('has-image',Boolean(avatar));els.profileAvatar.style.backgroundImage=avatar?`url(${JSON.stringify(avatar)})`:''}
  }
  function applyBrand(){
    if(els.sidebarBrandTitle)els.sidebarBrandTitle.textContent='光影宇宙';
    if(els.sidebarBrandSubtitle){els.sidebarBrandSubtitle.textContent='定制化影视收藏夹';els.sidebarBrandSubtitle.classList.remove('hidden')}
    document.title='光影宇宙';
  }
  function updateProfileSettingsUI(){const st=normalizeSettings(appState.settings),name=String(st.profileName||'电影爱好者'),role=String(st.profileRole??''),avatar=String(st.profileAvatarDataUrl||'');if(els.settingsProfileName)els.settingsProfileName.value=name;if(els.settingsProfileRole)els.settingsProfileRole.value=role;if(els.settingsProfilePreviewName)els.settingsProfilePreviewName.textContent=name||'电影爱好者';if(els.settingsProfilePreviewRole){els.settingsProfilePreviewRole.textContent=role;els.settingsProfilePreviewRole.classList.toggle('hidden',!role)}if(els.settingsProfileAvatarPreview){els.settingsProfileAvatarPreview.classList.toggle('has-image',Boolean(avatar));els.settingsProfileAvatarPreview.style.backgroundImage=avatar?`url(${JSON.stringify(avatar)})`:''}}
  const imageCropState={target:'',source:null,zoom:1,offsetX:0,offsetY:0,dragging:false,lastX:0,lastY:0,stageSize:0,baseScale:1,outputSize:320};
  function renderImageCrop(){const s=imageCropState;if(!s.source||!els.imageCropStage)return;const size=els.imageCropStage.getBoundingClientRect().width||360;s.stageSize=size;const scale=s.baseScale*s.zoom,w=s.source.naturalWidth*scale,h=s.source.naturalHeight*scale,maxX=Math.max(0,(w-size)/2),maxY=Math.max(0,(h-size)/2);s.offsetX=Math.max(-maxX,Math.min(maxX,s.offsetX));s.offsetY=Math.max(-maxY,Math.min(maxY,s.offsetY));const img=els.imageCropImage;img.style.width=`${w}px`;img.style.height=`${h}px`;img.style.left=`${(size-w)/2+s.offsetX}px`;img.style.top=`${(size-h)/2+s.offsetY}px`}
  function resetImageCrop(){const s=imageCropState;if(!s.source)return;const size=els.imageCropStage.getBoundingClientRect().width||360;s.stageSize=size;s.zoom=1;s.offsetX=0;s.offsetY=0;s.baseScale=Math.max(size/s.source.naturalWidth,size/s.source.naturalHeight);els.imageCropZoom.value='1';renderImageCrop()}
  function openImageCropper(file,target){return new Promise((resolve,reject)=>{const reader=new FileReader();reader.onerror=()=>reject(new Error('读取图片失败'));reader.onload=()=>{const img=new Image();img.onerror=()=>reject(new Error('图片无法解析'));img.onload=()=>{imageCropState.target=target;imageCropState.source=img;imageCropState.outputSize=320;els.imageCropImage.src=reader.result;els.imageCropTitle.textContent='裁切个人头像';els.imageCropDialog.showModal();requestAnimationFrame(resetImageCrop);resolve()};img.src=reader.result};reader.readAsDataURL(file)})}
  function applyImageCrop(){const s=imageCropState;if(!s.source)return;const size=s.stageSize||els.imageCropStage.getBoundingClientRect().width||360,scale=s.baseScale*s.zoom,w=s.source.naturalWidth*scale,h=s.source.naturalHeight*scale,left=(size-w)/2+s.offsetX,top=(size-h)/2+s.offsetY,srcX=Math.max(0,-left/scale),srcY=Math.max(0,-top/scale),srcSize=Math.min(s.source.naturalWidth-srcX,s.source.naturalHeight-srcY,size/scale),canvas=document.createElement('canvas');canvas.width=canvas.height=s.outputSize;const ctx=canvas.getContext('2d');ctx.drawImage(s.source,srcX,srcY,srcSize,srcSize,0,0,s.outputSize,s.outputSize);const data=canvas.toDataURL('image/webp',.9);appState.settings=normalizeSettings({...appState.settings,profileAvatarDataUrl:data});save();applyProfile();updateProfileSettingsUI();toastMsg('头像已裁切并保存 ✦');els.imageCropDialog.close()}
  function saveProfileFromControls(){appState.settings=normalizeSettings({...appState.settings,profileName:String(els.settingsProfileName?.value||'').trim()||'电影爱好者',profileRole:String(els.settingsProfileRole?.value??'').trim()});save();applyProfile();updateProfileSettingsUI();toastMsg('个人资料已保存 ✦')}
  function renderSettings(){updateLegacySeedStatus();
    appState.settings=normalizeSettings(appState.settings);const st=appState.settings;updateProfileSettingsUI();document.querySelectorAll('[data-theme-preset]').forEach(b=>b.classList.toggle('active',b.dataset.themePreset===st.themePreset));els.settingsTmdbLanguage.value=st.tmdbLanguage||'zh-CN';els.settingsDoubanMode.value=st.doubanMode||'auto';els.settingsDoubanAutoTmdb.classList.toggle('on',st.doubanAutoTmdb!==false);els.settingsDoubanSyncWatchDate.classList.toggle('on',st.doubanSyncWatchDate!==false);els.settingsDoubanLast.textContent=st.doubanLastImport?`上次导入 ${String(st.doubanLastImport).slice(0,16).replace('T',' ')} · ${st.doubanLastCount||0} 条`:'尚未导入';
    if(st.wallpaperDataUrl){els.settingsWallpaperPreview.classList.add('has-image');els.settingsWallpaperPreview.style.backgroundImage=`url(${JSON.stringify(st.wallpaperDataUrl).slice(1,-1)})`;els.settingsWallpaperPreview.querySelector('span').textContent='正在使用自定义壁纸'}else{els.settingsWallpaperPreview.classList.remove('has-image');els.settingsWallpaperPreview.style.backgroundImage='';els.settingsWallpaperPreview.querySelector('span').textContent='当前使用主题背景'}updateSettingsStorage();
  }
  function updateSettingsStorage(){const movies=appState.movies||[],watch=movies.reduce((n,m)=>n+(m.watchHistory||[]).length,0),plans=movies.reduce((n,m)=>n+(m.plans||[]).length,0),radar=(appState.home?.radar||[]).length,raw=localStorage.getItem(V2_KEY)||'',bytes=new Blob([raw]).size,kb=bytes/1024;if(els.settingsMovieCount)els.settingsMovieCount.textContent=movies.length;if(els.settingsWatchCount)els.settingsWatchCount.textContent=watch;if(els.settingsPlanCount)els.settingsPlanCount.textContent=plans;if(els.settingsRadarCount)els.settingsRadarCount.textContent=radar;if(els.settingsStorageCopy)els.settingsStorageCopy.textContent=`当前本地数据约 ${kb<1024?kb.toFixed(1)+' KB':(kb/1024).toFixed(2)+' MB'} · 建议定期导出完整备份。`;const latest=movies.map(m=>m.updatedAt).filter(Boolean).sort().at(-1);if(els.settingsLastUpdated)els.settingsLastUpdated.textContent=latest?`最近更新 ${String(latest).slice(0,10)}`:'暂无更新记录'}
  function saveAppearanceFromControls(){appState.settings=normalizeSettings(appState.settings);save();applyAppearance();renderSettings();toastMsg('外观设置已保存 ✦')}
  function downloadText(filename,text,type='text/plain;charset=utf-8'){const blob=new Blob([text],{type}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=filename;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),500)}
  function exportFullBackup(){const payload=backupState(appState);downloadText(`影视收藏夹V2_完整备份_${localToday()}.json`,JSON.stringify(payload,null,2),'application/json;charset=utf-8');toastMsg('完整 JSON 备份已导出（不含 TMDb Key）')}
  function exportMoviesCsv(){const head=['mediaType','title','originalTitle','year','releaseDate','lastAirDate','numberOfSeasons','numberOfEpisodes','tvStatus','directors','countries','runtime','genres','posterUrl','status','rating','tags','shortReview','favorite','tmdbId','doubanId'];const rows=(appState.movies||[]).map(m=>[m.mediaType,m.info.title,m.info.originalTitle,m.info.year,m.info.releaseDate,m.info.lastAirDate,m.info.numberOfSeasons,m.info.numberOfEpisodes,m.info.tvStatus,(m.info.directors||[]).join(' / '),(m.info.countries||[]).join(' / '),m.info.runtime,(m.info.genres||[]).join(' / '),m.info.posterUrl,m.personal.status,m.personal.rating,(m.personal.tags||[]).join(' / '),m.personal.shortReview,m.personal.favorite?'1':'0',m.info.tmdbId,m.info.doubanId].map(csvEscape).join(','));downloadText(`影视收藏夹V2_影视库_${localToday()}.csv`,'\ufeff'+[head.join(','),...rows].join('\r\n'),'text/csv;charset=utf-8');toastMsg('影视库 CSV 已导出')}
  function importMoviesCsvText(text){const rows=parseCsv(text.replace(/^\ufeff/,''));if(rows.length<2)throw new Error('CSV 没有数据');const header=rows[0].map(x=>x.trim()),idx=k=>header.indexOf(k),get=(r,k)=>idx(k)>=0?r[idx(k)]:'';let count=0;for(const r of rows.slice(1)){const title=get(r,'title')?.trim();if(!title)continue;const year=get(r,'year')?Number(get(r,'year')):null,type=['movie','tv','unknown'].includes(get(r,'mediaType'))?get(r,'mediaType'):'movie',existing=appState.movies.find(m=>m.info.title===title&&(!year||!m.info.year||Number(m.info.year)===year)),data=normalizeMovie({id:existing?.id||uid(),mediaType:type,info:{title,originalTitle:get(r,'originalTitle')||'',year,releaseDate:get(r,'releaseDate')||'',firstAirDate:type==='tv'?(get(r,'releaseDate')||''):'',lastAirDate:get(r,'lastAirDate')||'',numberOfSeasons:get(r,'numberOfSeasons')!==''?Number(get(r,'numberOfSeasons')):null,numberOfEpisodes:get(r,'numberOfEpisodes')!==''?Number(get(r,'numberOfEpisodes')):null,tvStatus:get(r,'tvStatus')||'',directors:splitList(get(r,'directors')),countries:splitList(get(r,'countries')),runtime:get(r,'runtime')?Number(get(r,'runtime')):null,genres:splitList(get(r,'genres')),posterUrl:get(r,'posterUrl')||'',overview:existing?.info?.overview||'',tmdbId:get(r,'tmdbId')||existing?.info?.tmdbId||null,doubanId:get(r,'doubanId')||existing?.info?.doubanId||null},personal:{status:get(r,'status')||existing?.personal?.status||'want',rating:get(r,'rating')!==''&&get(r,'rating')!=null?Number(get(r,'rating')):existing?.personal?.rating??null,tags:splitList(get(r,'tags')),shortReview:get(r,'shortReview')||'',favorite:['1','true','TRUE'].includes(get(r,'favorite'))},watchHistory:existing?.watchHistory||[],plans:existing?.plans||[],radar:existing?.radar||{discovered:false,ignored:false}});if(existing){const pos=appState.movies.indexOf(existing);appState.movies[pos]=data}else appState.movies.push(data);count++}save();renderAll();return count}
  async function importBackupFile(file){const text=await file.text(),parsed=JSON.parse(text),candidate=parsed?.state||parsed;appState=restoreState(candidate);appStore.replace(appState,{persist:true,silent:true,reason:'backup-restore'});applyAppearance();applyProfile();applyBrand();renderAll();renderSettings();toastMsg(`已完整恢复 ${appState.movies.length} 部影视作品与匹配中心状态`)}
  function findDoubanExisting(e){if(e.doubanId){const byId=appState.movies.find(m=>String(m.info?.doubanId||'')===String(e.doubanId)||(m.external?.doubanSeasons||[]).some(x=>String(x.id||'')===String(e.doubanId)));if(byId)return byId}const key=doubanCleanTitle(e.title).toLowerCase();return appState.movies.find(m=>doubanCleanTitle(m.info?.title).toLowerCase()===key&&(!e.year||!m.info?.year||Number(m.info.year)===Number(e.year)))||null}
  function mergeDoubanEntry(e){let m=findDoubanExisting(e),created=false;if(!m){m=normalizeMovie({id:uid(),mediaType:'unknown',info:{title:e.title,originalTitle:e.originalTitle||'',year:e.year,doubanId:e.doubanId},personal:{status:e.status==='watched'?'watched':'want',rating:e.rating,tags:e.tags||[],shortReview:e.comment||'',favorite:false},watchHistory:[],plans:[],radar:{discovered:false,ignored:false},external:{}});appState.movies.push(m);created=true}else{if(e.doubanId&&!m.info.doubanId)m.info.doubanId=e.doubanId;if(!m.info.tmdbId&&m.external?.douban)m.mediaType='unknown';if(e.originalTitle&&!m.info.originalTitle)m.info.originalTitle=e.originalTitle;if(e.year&&!m.info.year)m.info.year=e.year;if(e.tags?.length)m.personal.tags=[...new Set([...(m.personal.tags||[]),...e.tags])];if(e.comment&&!m.personal.shortReview)m.personal.shortReview=e.comment;if(e.rating!=null)m.personal.rating=e.rating;if(e.status==='watched')m.personal.status='watched';else if(m.personal.status!=='watched')m.personal.status='want'}
    if(appState.settings.doubanSyncWatchDate!==false&&e.status==='watched'&&e.date){const d=String(e.date).match(/((?:19|20)\d{2})[-\/.年](\d{1,2})[-\/.月](\d{1,2})/);const date=d?`${d[1]}-${String(d[2]).padStart(2,'0')}-${String(d[3]).padStart(2,'0')}`:String(e.date).slice(0,10);if(date&&!m.watchHistory.some(w=>w.date===date&&String(w.venue||'').includes('豆瓣'))){m.watchHistory.push({date,rating:e.rating??null,note:e.comment||'',venue:'豆瓣导入'})}}
    m.external=m.external||{};m.external.douban={id:e.doubanId||m.info.doubanId||null,lastImportedAt:new Date().toISOString(),status:e.status};m.updatedAt=new Date().toISOString();return created?'created':'updated'}
  function renderDoubanFiles(){const fs=[...(els.settingsDoubanFiles?.files||[])];els.settingsDoubanFileList.innerHTML=fs.map(f=>`<div class="douban-file"><span>${esc(f.name)}</span><em>${(f.size/1024).toFixed(1)} KB</em></div>`).join('');els.settingsDoubanStatus.className='douban-status';els.settingsDoubanStatus.textContent=fs.length?`已选择 ${fs.length} 个文件，正在预览可识别条目…`:'请选择文件后开始导入。'}
  async function previewDoubanFiles(){const files=[...(els.settingsDoubanFiles?.files||[])];if(!files.length)return;const mode=els.settingsDoubanMode.value||'auto';let total=0,samples=[],failed=[];for(const f of files){try{const text=await f.text(),rows=parseDoubanFile(f.name,text,mode);total+=rows.length;samples.push(...rows.slice(0,3).map(x=>x.originalTitle?`${x.title} / ${x.originalTitle}`:x.title));if(!rows.length)failed.push(f.name)}catch{failed.push(f.name)}}els.settingsDoubanStatus.className='douban-status '+(total?'ok':'bad');els.settingsDoubanStatus.textContent=total?`预览成功：共识别 ${total} 条影视。${samples.length?`\n示例：${[...new Set(samples)].slice(0,6).join('、')}`:''}${failed.length?`\n另有 ${failed.length} 个文件暂未识别。`:''}`:`没有识别到影视条目。请确认保存的是豆瓣“想看 / 看过”列表页；若仍失败，可以把 HTML 文件发给我针对实际结构修复。`;}
  async function importDoubanFiles(){const files=[...(els.settingsDoubanFiles?.files||[])];if(!files.length){els.settingsDoubanStatus.className='douban-status bad';els.settingsDoubanStatus.textContent='请先选择至少一个 HTML / CSV / JSON 文件。';return}const btn=els.settingsDoubanImport;btn.disabled=true;const mode=els.settingsDoubanMode.value||'auto';let parsed=0,created=0,updated=0,watchTimesSynced=0,failed=[];els.settingsDoubanStatus.className='douban-status';els.settingsDoubanStatus.textContent=`正在解析 ${files.length} 个文件…`;try{for(const f of files){try{const text=await f.text(),rows=parseDoubanFile(f.name,text,mode);if(!rows.length)throw new Error('没有识别到影视条目');for(const e of rows){const before=findDoubanExisting(e);const beforeCount=before?.watchHistory?.length||0;const r=mergeDoubanEntry(e);const after=findDoubanExisting(e);const afterCount=after?.watchHistory?.length||0;if(afterCount>beforeCount)watchTimesSynced+=afterCount-beforeCount;parsed++;if(r==='created')created++;else updated++}}catch(err){failed.push(`${f.name}：${err.message||'解析失败'}`)}}if(!parsed){els.settingsDoubanStatus.className='douban-status bad';els.settingsDoubanStatus.textContent=`导入失败：没有识别到任何影视条目。${failed.length?`\n${failed.join('\n')}`:''}`;return}appState.settings.doubanMode=mode;appState.settings.doubanAutoTmdb=els.settingsDoubanAutoTmdb.classList.contains('on');appState.settings.doubanSyncWatchDate=els.settingsDoubanSyncWatchDate.classList.contains('on');appState.settings.doubanLastImport=new Date().toISOString();appState.settings.doubanLastCount=parsed;save();renderAll();renderSettings();const summary=`豆瓣导入完成：识别 ${parsed} 条 · 新增 ${created} 部 · 更新 ${updated} 部 · 同步观看时间 ${watchTimesSynced} 条`;els.settingsDoubanStatus.className='douban-status '+(failed.length?'':'ok');els.settingsDoubanStatus.textContent=summary+(failed.length?`\n失败 ${failed.length} 个文件：\n${failed.join('\n')}`:'');clearFilters();setView('library');renderLibrary();toastMsg(`${summary}，已同步到影视库`);if(appState.settings.doubanAutoTmdb){setTimeout(()=>runBatchTmdbCompletion(true),500)}}finally{btn.disabled=false}}

  async function testTmdbConnection(){appState.settings.tmdbLanguage=els.settingsTmdbLanguage.value||'zh-CN';els.settingsTmdbStatus.className='tmdb-status';els.settingsTmdbStatus.textContent='正在连接 TMDb 资料服务…';try{await tmdbFetch('/configuration');els.settingsTmdbStatus.className='tmdb-status ok';els.settingsTmdbStatus.textContent='连接成功。TMDb 资料服务已可直接用于搜索、批量补全和电影雷达。'}catch(err){els.settingsTmdbStatus.className='tmdb-status bad';els.settingsTmdbStatus.textContent=`连接失败：${err.message||'代理服务异常'}。`}}

  async function runTmdbSelfTest(){
    appState.settings.tmdbLanguage=els.settingsTmdbLanguage.value||'zh-CN';
    const btn=els.settingsSelfTestTmdb;btn.disabled=true;
    const lines=[];const show=(ok=false)=>{els.settingsTmdbStatus.className='tmdb-status'+(ok?' ok':'');els.settingsTmdbStatus.textContent=lines.join('\n')};
    const step=(msg)=>{lines.push('… '+msg);show(false)};
    const pass=(msg)=>{lines[lines.length-1]='✓ '+msg;show(false)};
    const fail=(msg,err)=>{lines[lines.length-1]='✕ '+msg+'：'+(err?.message||err||'未知错误');els.settingsTmdbStatus.className='tmdb-status bad';els.settingsTmdbStatus.textContent=lines.join('\n')};
    try{
      step('1/5 凭证与 TMDb API 连通性');
      const config=await tmdbFetch('/configuration');
      if(!config?.images?.secure_base_url)throw new Error('configuration 返回异常');
      pass('1/5 API 连通，凭证有效');

      step('2/5 搜索接口（测试词：The Matrix）');
      const search=await tmdbFetch('/search/movie',{query:'The Matrix',include_adult:false,language:appState.settings.tmdbLanguage||'zh-CN'});
      const hit=(search?.results||[]).find(x=>x.id)||search?.results?.[0];
      if(!hit?.id)throw new Error('搜索未返回电影结果');
      pass(`2/5 搜索成功，命中 TMDb #${hit.id} · ${hit.title||hit.original_title||'电影'}`);

      step('3/5 电影详情接口');
      const detail=await tmdbFetch(`/movie/${hit.id}`,{language:appState.settings.tmdbLanguage||'zh-CN'});
      if(!detail?.id||!detail?.title)throw new Error('详情字段不完整');
      pass(`3/5 详情成功 · ${detail.title}${detail.runtime?` · ${detail.runtime} 分钟`:''}`);

      step('4/5 演职员接口 / 导演识别');
      const credits=await tmdbFetch(`/movie/${hit.id}/credits`,{language:appState.settings.tmdbLanguage||'zh-CN'});
      const directors=(credits?.crew||[]).filter(x=>x.job==='Director').map(x=>x.name).filter(Boolean);
      if(!directors.length)throw new Error('未识别到导演');
      pass(`4/5 演职员成功 · 导演：${directors.join(' / ')}`);

      step('5/5 海报 CDN');
      if(!detail.poster_path)throw new Error('测试电影没有 poster_path');
      const poster=tmdbImage(detail.poster_path,'w185');
      await new Promise((resolve,reject)=>{const img=new Image();const timer=setTimeout(()=>reject(new Error('海报加载超时')),8000);img.onload=()=>{clearTimeout(timer);resolve()};img.onerror=()=>{clearTimeout(timer);reject(new Error('image.tmdb.org 加载失败'))};img.src=poster});
      pass('5/5 海报 CDN 加载成功');

      lines.push('✓ 完整自检通过：搜索 → 详情 → 导演 → 海报链路均可用。');
      els.settingsTmdbStatus.className='tmdb-status ok';els.settingsTmdbStatus.textContent=lines.join('\n');
      save();toastMsg('TMDb 完整自检通过');
    }catch(err){
      if(lines.length && lines[lines.length-1].startsWith('…'))fail(lines[lines.length-1].replace(/^…\s*/,''),err);
      else{lines.push('✕ 自检失败：'+(err?.message||err));els.settingsTmdbStatus.className='tmdb-status bad';els.settingsTmdbStatus.textContent=lines.join('\n')}
    }finally{btn.disabled=false}
  }

  function toggleDetailPlan(){const m=currentDetailMovie();if(!m)return;const plan=getPlan(m,CURRENT_MONTH)||(m.plans||[]).find(p=>p.status!=='deferred');detailState.planMonth=plan?.month||null;els.detailPlanTitle.textContent=plan?'修改观影计划':'加入观影计划';els.detailPlanDate.value=plan?.plannedDate||localToday();els.detailPlanCurrent.classList.toggle('hidden',!plan);els.detailPlanCurrent.textContent=plan?`当前计划时间：${plan.plannedDate||plan.month+'（未指定日期）'}`:'';els.detailPlanClear.classList.toggle('hidden',!plan);els.detailPlanSave.textContent=plan?'保存新时间':'加入计划';els.detailPlanDialog.showModal()}
  function saveDetailPlan(){const m=currentDetailMovie();if(!m)return;const date=els.detailPlanDate.value||localToday(),month=date.slice(0,7);m.plans=m.plans||[];const current=detailState.planMonth?m.plans.find(p=>p.month===detailState.planMonth):null;if(current&&current.month!==month)m.plans=m.plans.filter(p=>p!==current);let plan=m.plans.find(p=>p.month===month);if(plan){plan.status='planned';plan.plannedDate=date;plan.movedTo=null}else m.plans.push({month,status:'planned',plannedDate:date,movedTo:null});detailState.planMonth=null;m.updatedAt=new Date().toISOString();save();renderAll();renderDetail();els.detailPlanDialog.close();toastMsg(`已安排在 ${date} 观看《${m.info.title}》`)}
  function clearDetailPlan(){const m=currentDetailMovie();if(!m)return;m.plans=(m.plans||[]).filter(p=>p.month!==detailState.planMonth);detailState.planMonth=null;m.updatedAt=new Date().toISOString();save();renderAll();renderDetail();els.detailPlanDialog.close();toastMsg(`已清除《${m.info.title}》的观影计划`)}
  function showLibraryWarning(m,kind){const dialog=$('libraryWarningDialog'),title=$('libraryWarningTitle'),copy=$('libraryWarningCopy');title.textContent=kind==='watch'?'删除全部观看记录？':'移除想看状态？';copy.innerHTML=kind==='watch'?`确认删除《${esc(m.info.title)}》的全部观看记录吗？<br>观看日期、文字记录与个人评分会一并移除。`:`确认移除《${esc(m.info.title)}》的想看状态吗？`;dialog.showModal();return new Promise(resolve=>{const finish=value=>{dialog.close();resolve(value)};$('libraryWarningCancel').onclick=()=>finish(false);$('libraryWarningConfirm').onclick=()=>finish(true);dialog.oncancel=e=>{e.preventDefault();finish(false)}})}
  function removeDetailMovieIfOrphan(m){if(!window.CineverseDetail.isOrphan(m))return false;appState.movies=appState.movies.filter(x=>x.id!==m.id);detailState.detachedMovie=m;save();renderAll();renderDetail();toastMsg(`《${m.info.title}》已移出影视库`);return true}
  async function toggleDetailWant(){const m=currentDetailMovie();if(!m)return;const wants=window.CineverseDetail.wantsMovie(m);if(wants){if(!await showLibraryWarning(m,'want'))return;window.CineverseDetail.setWanted(m,false);if(removeDetailMovieIfOrphan(m))return;toastMsg('已移除想看标记')}else{window.CineverseDetail.setWanted(m,true);toastMsg('已标记为想看，并保留在影视库')}save();renderAll();renderDetail()}
  async function toggleDetailWatched(){const m=currentDetailMovie();if(!m)return;if(!window.CineverseDetail.hasWatches(m)){openWatchModal();return}if(!await showLibraryWarning(m,'watch'))return;window.CineverseDetail.clearWatches(m);if(removeDetailMovieIfOrphan(m))return;save();renderAll();renderDetail();toastMsg('已删除全部观看记录与评分')}
  function renderQuickWatchPicker(){const q=(els.quickWatchSearch.value||'').trim().toLowerCase(),rows=(appState.movies||[]).filter(m=>!q||[m.info.title,m.info.originalTitle,(m.info.directors||[]).join(' ')].join(' ').toLowerCase().includes(q)).sort((a,b)=>String(b.updatedAt||'').localeCompare(String(a.updatedAt||''))).slice(0,80);els.quickWatchList.innerHTML=rows.length?rows.map(m=>`<div class="quick-watch-row" data-quick-watch-id="${escAttr(m.id)}"><div class="quick-watch-poster">${m.info.posterUrl?`<img src="${escAttr(m.info.posterUrl)}" alt="">`:''}</div><div class="quick-watch-copy"><b>${esc(m.info.title)}</b><span>${mediaTypeIcon(m)} ${mediaTypeLabel(m)} · ${m.info.year||'年份未知'} · ${esc((m.info.directors||[])[0]||(m.mediaType==='tv'?'主创未知':'导演未知'))}</span></div><em>记录 →</em></div>`).join(''):'<div class="quick-watch-empty">没有找到匹配的影视作品。</div>'}
  function openQuickWatchPicker(){if(!(appState.movies||[]).length){toastMsg('影视库还是空的，先添加一部作品');openMovieModal();return}els.quickWatchSearch.value='';renderQuickWatchPicker();els.quickWatchDialog.showModal()}

  function openWatchModal(){const m=currentDetailMovie();if(!m)return;els.watchDateInput.value=localToday();els.watchRatingInput.value=m.personal?.rating??'';els.watchVenueInput.value='';els.watchNoteInput.value='';els.watchModal.showModal()}
  function saveWatchRecord(){const m=currentDetailMovie();if(!m)return;const rating=els.watchRatingInput.value!==''?Number(els.watchRatingInput.value):null;m.watchHistory=m.watchHistory||[];m.watchHistory.push({date:els.watchDateInput.value||localToday(),rating,note:els.watchNoteInput.value.trim(),venue:els.watchVenueInput.value.trim()});m.personal.status='watched';if(rating!=null)m.personal.rating=rating;const month=(els.watchDateInput.value||'').slice(0,7);const p=(m.plans||[]).find(x=>x.month===month);if(p)p.status='completed';m.updatedAt=new Date().toISOString();save();renderAll();renderDetail();els.watchModal.close();toastMsg('这一晚已经写进你的影视宇宙 ✦')}

  const router=window.CineverseRouter.createRouter({
    pages:{
      home:{element:els.homeView},library:{element:els.libraryView,enter:renderLibrary},
      detail:{element:els.detailView,enter:renderDetail},match:{element:els.matchView,enter:renderMatchCenter},
      radar:{element:els.radarView,enter:renderRadarPage},plan:{element:els.planView,enter:renderPlanPage},
      watched:{element:els.watchedView,enter:renderWatchedPage},stats:{element:els.statsView,enter:renderStatsPage},
      settings:{element:els.settingsView,enter:renderSettings}
    },
    detail:{
      enter(id){if(!appState.movies.some(movie=>movie.id===id))return false;detailState.movieId=id;detailState.detachedMovie=null;return true},
      fallback:'library'
    },
    passthrough:['external-preview/','radar-preview/']
  });
  function setView(name,options){return router.navigate(name,options)}
  function renderAll(){renderMetrics();renderRadar();renderRecent();renderPlan();renderRandom();renderMonthInsight();renderLibrary();if(els.matchView&&!els.matchView.classList.contains('hidden'))renderMatchCenter();if(!els.radarView.classList.contains('hidden'))renderRadarPage();if(!els.planView.classList.contains('hidden'))renderPlanPage();if(!els.watchedView.classList.contains('hidden'))renderWatchedPage();if(!els.statsView.classList.contains('hidden'))renderStatsPage();if(detailState.movieId&&!els.detailView.classList.contains('hidden'))renderDetail();if(els.settingsView&&!els.settingsView.classList.contains('hidden'))renderSettings()}
  stateGateway.subscribe(state=>state,(nextState,_previousState,metadata)=>{if(metadata?.source==='app-main')return;appState=nextState;renderAll()});

  document.addEventListener('click',e=>{
    const nav=e.target.closest('.nav a[data-view]');if(nav){e.preventDefault();setView(nav.dataset.view);return}
    const viewLink=e.target.closest('[data-view-link]');if(viewLink){setView(viewLink.dataset.viewLink);return}
    const op=e.target.closest('[data-open-detail]');if(op){openDetail(op.dataset.openDetail);return}
    const rel=e.target.closest('[data-related-id]');if(rel){openDetail(rel.dataset.relatedId);return}
    const libWant=e.target.closest('[data-lib-want]');if(libWant){detailState.movieId=libWant.dataset.libWant;detailState.detachedMovie=null;toggleDetailWant();return}
    const libWatched=e.target.closest('[data-lib-watched]');if(libWatched){detailState.movieId=libWatched.dataset.libWatched;detailState.detachedMovie=null;toggleDetailWatched();return}
    const libPlan=e.target.closest('[data-lib-plan]');if(libPlan){detailState.movieId=libPlan.dataset.libPlan;detailState.detachedMovie=null;toggleDetailPlan();return}
    const libWatch=e.target.closest('[data-lib-watch]');if(libWatch){detailState.movieId=libWatch.dataset.libWatch;detailState.detachedMovie=null;openWatchModal();return}
    const dw=e.target.closest('[data-delete-watch]');if(dw){const m=currentDetailMovie();if(m&&confirm('删除这条观影记录？')){const hist=[...(m.watchHistory||[])].sort((a,b)=>String(b.date||'').localeCompare(String(a.date||'')));const target=hist[Number(dw.dataset.deleteWatch)];const idx=m.watchHistory.indexOf(target);if(idx>=0)m.watchHistory.splice(idx,1);if(!m.watchHistory.length){if(m.personal.status==='watched')m.personal.status=m.personal.want?'want':'follow';m.personal.rating=null}if(removeDetailMovieIfOrphan(m))return;save();renderAll();renderDetail();toastMsg('已删除观影记录')}return}
    const ex=e.target.closest('.exclude-toggle');if(ex){toggleExclude(ex.dataset.exclude,ex);return}
    const fav=e.target.closest('[data-favorite-id]');if(fav){const m=appState.movies.find(x=>x.id===fav.dataset.favoriteId);if(m){m.personal.favorite=!m.personal.favorite;m.updatedAt=new Date().toISOString();save();renderLibrary();toastMsg(m.personal.favorite?'已点亮收藏星标':'已取消星标')}return}
    const ed=e.target.closest('[data-edit-id]');if(ed){openMovieModal(ed.dataset.editId);return}
    const cyc=e.target.closest('[data-cycle-status]');if(cyc){const m=appState.movies.find(x=>x.id===cyc.dataset.cycleStatus);if(m)cycleStatus(m);return}
    const sel=e.target.closest('[data-select-id]');if(sel){if(sel.checked)libraryState.selected.add(sel.dataset.selectId);else libraryState.selected.delete(sel.dataset.selectId);updateBatchBar();return}
    const favRadar=e.target.closest('[data-fav-radar]');if(favRadar){const r=radarItems().find(x=>x.title===favRadar.dataset.favRadar);if(r){collectRadarItem(r);toastMsg(`已将《${r.title}》加入收藏`)}return}
    const planRadar=e.target.closest('[data-plan-radar]');if(planRadar){const r=radarItems().find(x=>x.title===planRadar.dataset.planRadar);if(r){collectRadarItem(r,{plan:true});toastMsg(`已将《${r.title}》加入本月计划`)}return}
    const radarTab=e.target.closest('[data-radar-tab]');if(radarTab){radarState.tab=radarTab.dataset.radarTab;renderRadarPage();return}
    const radarCollect=e.target.closest('[data-radar-collect]');if(radarCollect){const r=radarItems().find(x=>x.id===radarCollect.dataset.radarCollect);if(r){collectRadarItem(r);toastMsg(`《${r.title}》已进入影视库`)}return}
    const radarPlan=e.target.closest('[data-radar-plan]');if(radarPlan){const r=radarItems().find(x=>x.id===radarPlan.dataset.radarPlan);if(r){const m=collectRadarItem(r,{plan:true});toastMsg(`《${m.info.title}》已进入本月计划`)}return}
    const radarDetail=e.target.closest('[data-radar-detail]');if(radarDetail){const r=radarItems().find(x=>x.id===radarDetail.dataset.radarDetail),m=r&&findMovieForRadar(r);if(m)openDetail(m.id);return}
    const radarIgnore=e.target.closest('[data-radar-ignore]');if(radarIgnore){const r=radarItems().find(x=>x.id===radarIgnore.dataset.radarIgnore);if(r){r.ignored=!r.ignored;const m=findMovieForRadar(r);if(m&&m.radar)m.radar.ignored=r.ignored;save();renderAll();renderRadarPage();toastMsg(r.ignored?'已忽略这次推荐':'已恢复关注')}return}
    const day=e.target.closest('[data-plan-day]');if(day){openPlanMovieModal(day.dataset.planDay);return}
    const schedule=e.target.closest('[data-schedule-id]');if(schedule){openPlanMovieModal('',schedule.dataset.scheduleId);return}
    const defer=e.target.closest('[data-plan-defer-id]');if(defer){deferPlan(defer.dataset.planDeferId);return}
    const remove=e.target.closest('[data-plan-remove-id]');if(remove){if(confirm('从这个月的计划中移除这部作品？'))removePlan(remove.dataset.planRemoveId);return}
    const emptyAdd=e.target.closest('[data-plan-add-empty]');if(emptyAdd){openPlanMovieModal();return}
    const bs=e.target.closest('[data-batch-status]');if(bs){for(const id of libraryState.selected){const m=appState.movies.find(x=>x.id===id);if(m){m.personal.status=bs.dataset.batchStatus;if(m.personal.status==='watched'&&!m.watchHistory.length)m.watchHistory=[{date:localToday(),rating:m.personal.rating,note:'',venue:''}];m.updatedAt=new Date().toISOString()}}save();renderAll();toastMsg('批量状态已更新');return}
  });
  document.addEventListener('change',e=>{
    const d=e.target.closest('[data-plan-date-id]');if(d){const m=appState.movies.find(x=>x.id===d.dataset.planDateId),p=m&&getPlan(m,planState.month);if(p){p.plannedDate=d.value||null;m.updatedAt=new Date().toISOString();save();renderAll();renderPlanPage();toastMsg('计划日期已更新')}return}
    const st=e.target.closest('[data-plan-status-id]');if(st){const m=appState.movies.find(x=>x.id===st.dataset.planStatusId),p=m&&getPlan(m,planState.month);if(p){p.status=st.value;if(st.value==='deferred'&&!p.movedTo)p.movedTo=shiftMonth(planState.month,1);m.updatedAt=new Date().toISOString();save();renderAll();renderPlanPage();toastMsg('计划状态已更新')}return}
  });
  document.addEventListener('movie-library:state-updated',e=>{const next=e.detail?.state;if(!next||!Array.isArray(next.movies))return;appState.movies=next.movies;if(next.tmdbMatchCenter)appState.tmdbMatchCenter=next.tmdbMatchCenter;renderAll()});
  [els.libKeyword,els.libMediaType,els.libYear,els.libDirector,els.libCountry,els.libStatus,els.libTag,els.libPlan,els.libSort].forEach(el=>el.addEventListener('input',()=>{libraryState.page=1;renderLibrary()}));els.libRatingOp.addEventListener('change',()=>{syncRatingFilterUI();libraryState.page=1;renderLibrary()});els.libRatingScore.addEventListener('input',()=>{libraryState.page=1;renderLibrary()});syncRatingFilterUI();
  els.libFirstPage.addEventListener('click',()=>goLibraryPage(1));els.libPrevPage.addEventListener('click',()=>goLibraryPage(libraryState.page-1));els.libNextPage.addEventListener('click',()=>goLibraryPage(libraryState.page+1));els.libLastPage.addEventListener('click',()=>goLibraryPage(Math.ceil(getFilteredMovies().length/libraryState.pageSize)));
  els.libPageNumbers.addEventListener('click',e=>{const b=e.target.closest('[data-library-page]');if(b)goLibraryPage(Number(b.dataset.libraryPage))});
  els.cardViewBtn.addEventListener('click',()=>{libraryState.view='card';els.cardViewBtn.classList.add('active');els.listViewBtn.classList.remove('active');renderLibrary()});
  els.listViewBtn.addEventListener('click',()=>{libraryState.view='list';els.listViewBtn.classList.add('active');els.cardViewBtn.classList.remove('active');renderLibrary()});
  els.multiBtn.addEventListener('click',()=>{libraryState.multi=!libraryState.multi;if(!libraryState.multi)libraryState.selected.clear();renderLibrary()});
  els.batchCancel.addEventListener('click',()=>{libraryState.multi=false;libraryState.selected.clear();renderLibrary()});
  els.batchSelectShown.addEventListener('click',()=>{const {pageMovies}=libraryPageData();pageMovies.forEach(m=>libraryState.selected.add(m.id));renderLibrary()});
  els.batchPlan.addEventListener('click',()=>{for(const id of libraryState.selected){const m=appState.movies.find(x=>x.id===id);if(m&&!hasPlan(m))m.plans.push({month:CURRENT_MONTH,status:'planned'})}save();renderAll();toastMsg(`已批量加入 ${monthLabel(CURRENT_MONTH)} 月度计划`)});
  els.clearLibFilters.addEventListener('click',clearFilters);els.bulkAddTitlesBtn.addEventListener('click',openBulkAddTitles);els.bulkAddTitlesClose.addEventListener('click',()=>els.bulkAddTitlesDialog.close());els.bulkAddTitlesCancel.addEventListener('click',()=>els.bulkAddTitlesDialog.close());els.bulkAddTitlesOnly.addEventListener('click',addBulkTitles);els.bulkAddTitlesMatch.addEventListener('click',addBulkTitlesAutoMatch);els.addMovieBtn.addEventListener('click',()=>openMovieModal());els.quickAdd.addEventListener('click',()=>openMovieModal());
  els.batchTmdbBtn.addEventListener('click',()=>{setView('match');setTimeout(()=>runMatchCenterScan(),0)});
  els.matchStartScan.addEventListener('click',()=>runMatchCenterScan());els.matchPauseResume.addEventListener('click',()=>{if(matchCenterState.running){matchCenterState.stop=true;els.matchPauseResume.disabled=true;els.matchPauseResume.textContent='正在暂停…'}else if(appState.tmdbMatchCenter?.paused&&(appState.tmdbMatchCenter?.resumeIds||[]).length){runMatchCenterScan({confirmStart:false,resume:true})}});
  els.matchDedupSeasons.addEventListener('click',runSeasonDedupe);
  els.matchRetryProblems.addEventListener('click',()=>runMatchCenterScan({confirmStart:false,onlyProblems:true}));
  els.matchResetSkipped.addEventListener('click',()=>{const rows=matchCenterRows();const n=rows.filter(r=>r.status==='skipped').length;if(!n){toastMsg('没有暂缓项目');return}rows.forEach(r=>{if(r.status==='skipped')r.status=(r.candidates||[]).length?'pending':'noresult'});save();renderMatchCenter();toastMsg(`已恢复 ${n} 个暂缓项目`)});
  els.matchKeyword.addEventListener('input',renderMatchCenter);
  els.matchTabs.addEventListener('click',e=>{const b=e.target.closest('[data-match-tab]');if(!b)return;matchCenterState.tab=b.dataset.matchTab;renderMatchCenter()});
  els.matchList.addEventListener('click',e=>{const use=e.target.closest('[data-match-use]');if(use){resolveMatchCenterCandidate(use.dataset.matchUse,use.dataset.tmdbId,use.dataset.tmdbType);return}const retry=e.target.closest('[data-match-retry]');if(retry){retryMatchCenterMovie(retry.dataset.matchRetry);return}const edit=e.target.closest('[data-match-edit]');if(edit){openMovieModal(edit.dataset.matchEdit);return}const skip=e.target.closest('[data-match-skip]');if(skip){const row=matchRowFor(skip.dataset.matchSkip);if(!row)return;row.status=row.status==='skipped'?((row.candidates||[]).length?'pending':'noresult'):'skipped';row.updatedAt=new Date().toISOString();save();renderMatchCenter();return}});
  els.batchTmdbDialog.addEventListener('cancel',e=>{if(batchTmdbState.running){e.preventDefault();toastMsg('批量补全正在运行，可点击“停止”后关闭')}});els.batchTmdbClose.addEventListener('click',()=>{if(!batchTmdbState.running)els.batchTmdbDialog.close()});els.batchTmdbDone.addEventListener('click',()=>{if(!batchTmdbState.running)els.batchTmdbDialog.close()});els.batchTmdbStop.addEventListener('click',()=>{batchTmdbState.stop=true;els.batchTmdbStop.disabled=true;els.batchTmdbProgressText.textContent='正在停止，将在当前电影处理完成后结束…'});els.batchTmdbPending.addEventListener('click',e=>{const choose=e.target.closest('[data-batch-tmdb-candidate]');if(choose){resolveBatchTmdbCandidate(choose.dataset.batchTmdbCandidate,choose.dataset.tmdbId,choose.dataset.tmdbType);return}const edit=e.target.closest('[data-batch-edit]');if(edit){if(batchTmdbState.running)return;els.batchTmdbDialog.close();openMovieModal(edit.dataset.batchEdit)}});
  els.movieModalClose.addEventListener('click',()=>els.movieModal.close());els.movieCancelBtn.addEventListener('click',()=>els.movieModal.close());els.movieDeleteBtn.addEventListener('click',deleteEditing);els.movieForm.addEventListener('submit',e=>{e.preventDefault();saveMovieFromModal()});
  els.movieMediaTypeInput.addEventListener('change',()=>{updateMovieModalType();renderTmdbCandidates([]);setTmdbSearchState('')});els.tmdbMovieSearchBtn.addEventListener('click',searchTmdbMovie);els.tmdbMovieQuery.addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();searchTmdbMovie()}});els.tmdbMovieResults.addEventListener('click',e=>{const btn=e.target.closest('[data-tmdb-use]');if(btn)applyTmdbMovie(btn.dataset.tmdbUse,btn.dataset.tmdbType)});
  els.reroll.addEventListener('click',()=>{renderRandom(true);toastMsg('星星从你的想看片单里换了一部 ✦')});els.recentSeeAll.addEventListener('click',()=>{setView('watched');els.watchedSort.value='newest';renderWatchedPage(true)});
  els.watchTonight.addEventListener('click',()=>{const m=(appState.movies||[]).find(x=>x.id===homeRandomState.movieId);if(!m){toastMsg('想看片单里暂时没有可选择的电影');return}m.plans=m.plans||[];let p=getPlan(m,CURRENT_MONTH);if(!p){p={month:CURRENT_MONTH,status:'planned',plannedDate:localToday(),movedTo:null};m.plans.push(p)}else if(p.status!=='completed'){p.status='planned';if(!p.plannedDate)p.plannedDate=localToday()}m.updatedAt=new Date().toISOString();save();renderAll();toastMsg(`今晚就看《${m.info.title}》 ✦`);openDetail(m.id)});
  els.globalSearch.addEventListener('keydown',e=>{if(e.key==='Enter'){const q=e.target.value.trim();setView('library');els.libKeyword.value=q;libraryState.page=1;renderLibrary();toastMsg(q?`正在影视库搜索：${q}`:'已打开全部影视')}});
  [els.radarKeyword,els.radarMatch,els.radarSort].forEach(el=>el.addEventListener(el.tagName==='INPUT'?'input':'change',renderRadarPage));
  els.radarAutoUpdateBtn.addEventListener('click',updateRadarFromTmdb);els.radarImportBtn.addEventListener('click',()=>els.radarImportDialog.showModal());els.radarGoLibrary.addEventListener('click',()=>setView('library'));els.radarImportClose.addEventListener('click',()=>els.radarImportDialog.close());els.radarImportCancel.addEventListener('click',()=>els.radarImportDialog.close());els.radarImportForm.addEventListener('submit',e=>{e.preventDefault();importRadarJSON()});
  [els.watchedKeyword,els.watchedYear,els.watchedRating,els.watchedDirector,els.watchedCountry,els.watchedSort].forEach(el=>el.addEventListener(el.tagName==='INPUT'?'input':'change',()=>renderWatchedPage(true)));
  els.watchedPrev.addEventListener('click',()=>{if(watchedState.page>1){watchedState.page--;renderWatchedPage()}});els.watchedNext.addEventListener('click',()=>{watchedState.page++;renderWatchedPage()});els.watchedThisYear.addEventListener('click',()=>{els.watchedYear.value=String(NOW_YEAR);renderWatchedPage(true)});els.watchedRecordBtn.addEventListener('click',openQuickWatchPicker);els.quickWatchSearch.addEventListener('input',renderQuickWatchPicker);els.quickWatchClose.addEventListener('click',()=>els.quickWatchDialog.close());els.quickWatchCancel.addEventListener('click',()=>els.quickWatchDialog.close());els.quickWatchList.addEventListener('click',e=>{const row=e.target.closest('[data-quick-watch-id]');if(!row)return;detailState.movieId=row.dataset.quickWatchId;els.quickWatchDialog.close();openWatchModal()});
  els.statsYear.addEventListener('change',()=>{statsState.year=els.statsYear.value==='all'?'all':String(Number(els.statsYear.value)||NOW_YEAR);renderStatsPage()});
  els.statsCopySummary.addEventListener('click',async()=>{const scope=String(statsState.year),rows=statsRows(scope),text=statsSummaryText(scope,rows,statsMoviesForRows(rows));try{await navigator.clipboard.writeText(text);toastMsg('统计摘要已复制')}catch{toastMsg(text)}});
  els.planPrevMonth.addEventListener('click',()=>{planState.month=shiftMonth(planState.month,-1);renderPlanPage()});
  els.planNextMonth.addEventListener('click',()=>{planState.month=shiftMonth(planState.month,1);renderPlanPage()});
  els.planToday.addEventListener('click',()=>{planState.month=CURRENT_MONTH;renderPlanPage()});
  els.planCalendarBtn.addEventListener('click',()=>{planState.view='calendar';applyPlanView()});els.planListBtn.addEventListener('click',()=>{planState.view='list';applyPlanView()});
  els.planAddMovie.addEventListener('click',()=>openPlanMovieModal());els.planMovieModalClose.addEventListener('click',()=>els.planMovieModal.close());els.planMovieCancel.addEventListener('click',()=>els.planMovieModal.close());els.planMovieForm.addEventListener('submit',e=>{e.preventDefault();savePlanMovie()});
  els.detailBack.addEventListener('click',()=>setView('library'));
  els.detailFavorite.addEventListener('click',()=>{const m=currentDetailMovie();if(!m)return;m.personal.favorite=!m.personal.favorite;m.updatedAt=new Date().toISOString();save();renderAll();renderDetail();toastMsg(m.personal.favorite?'已点亮收藏星标':'已取消星标')});
  els.detailStatusBtn.addEventListener('click',toggleDetailWant);
  els.detailPlanBtn.addEventListener('click',toggleDetailPlan);els.detailEditBtn.addEventListener('click',()=>{const m=currentDetailMovie();if(m)openMovieModal(m.id)});els.detailAddWatchBtn.addEventListener('click',toggleDetailWatched);els.detailAddWatchBtn2.addEventListener('click',openWatchModal);
  els.detailPlanClose.addEventListener('click',()=>els.detailPlanDialog.close());els.detailPlanCancel.addEventListener('click',()=>els.detailPlanDialog.close());els.detailPlanClear.addEventListener('click',clearDetailPlan);els.detailPlanForm.addEventListener('submit',e=>{e.preventDefault();saveDetailPlan()});
  els.watchModalClose.addEventListener('click',()=>els.watchModal.close());els.watchCancelBtn.addEventListener('click',()=>els.watchModal.close());els.watchForm.addEventListener('submit',e=>{e.preventDefault();saveWatchRecord()});

  if(els.settingsSaveProfile)els.settingsSaveProfile.addEventListener('click',saveProfileFromControls);
  if(els.settingsProfileAvatarFile)els.settingsProfileAvatarFile.addEventListener('change',async()=>{const file=els.settingsProfileAvatarFile.files?.[0];if(!file)return;try{await openImageCropper(file,'profile')}catch(err){toastMsg(err.message)}finally{els.settingsProfileAvatarFile.value=''}});
  if(els.settingsProfileAvatarClear)els.settingsProfileAvatarClear.addEventListener('click',()=>{appState.settings=normalizeSettings({...appState.settings,profileAvatarDataUrl:''});save();applyProfile();updateProfileSettingsUI();toastMsg('已恢复默认头像')});
  els.imageCropZoom.addEventListener('input',()=>{imageCropState.zoom=Number(els.imageCropZoom.value)||1;renderImageCrop()});els.imageCropReset.addEventListener('click',resetImageCrop);els.imageCropClose.addEventListener('click',()=>els.imageCropDialog.close());els.imageCropCancel.addEventListener('click',()=>els.imageCropDialog.close());els.imageCropApply.addEventListener('click',applyImageCrop);els.imageCropStage.addEventListener('pointerdown',e=>{if(!imageCropState.source)return;imageCropState.dragging=true;imageCropState.lastX=e.clientX;imageCropState.lastY=e.clientY;els.imageCropStage.classList.add('dragging');els.imageCropStage.setPointerCapture?.(e.pointerId)});els.imageCropStage.addEventListener('pointermove',e=>{if(!imageCropState.dragging)return;imageCropState.offsetX+=e.clientX-imageCropState.lastX;imageCropState.offsetY+=e.clientY-imageCropState.lastY;imageCropState.lastX=e.clientX;imageCropState.lastY=e.clientY;renderImageCrop()});const endCropDrag=e=>{imageCropState.dragging=false;els.imageCropStage.classList.remove('dragging');try{els.imageCropStage.releasePointerCapture?.(e.pointerId)}catch{}};els.imageCropStage.addEventListener('pointerup',endCropDrag);els.imageCropStage.addEventListener('pointercancel',endCropDrag);
  document.querySelectorAll('[data-theme-preset]').forEach(b=>b.addEventListener('click',()=>{appState.settings=normalizeSettings({...appState.settings,themePreset:b.dataset.themePreset,accentColor:THEME_PRESETS[b.dataset.themePreset]?.purple||DEFAULT_SETTINGS.accentColor});save();renderSettings();applyAppearance()}));
  els.settingsSaveAppearance.addEventListener('click',saveAppearanceFromControls);
  els.settingsResetAppearance.addEventListener('click',()=>{const keep={profileName:appState.settings.profileName,profileRole:appState.settings.profileRole,profileAvatarDataUrl:appState.settings.profileAvatarDataUrl,tmdbLanguage:appState.settings.tmdbLanguage,doubanMode:appState.settings.doubanMode,doubanAutoTmdb:appState.settings.doubanAutoTmdb,doubanSyncWatchDate:appState.settings.doubanSyncWatchDate,doubanLastImport:appState.settings.doubanLastImport,doubanLastCount:appState.settings.doubanLastCount};appState.settings=normalizeSettings({...keep});save();applyAppearance();renderSettings();toastMsg('已恢复默认星空主题')});
  els.settingsWallpaperFile.addEventListener('change',()=>{const f=els.settingsWallpaperFile.files?.[0];if(!f)return;if(f.size>1.8*1024*1024){toastMsg('壁纸建议小于 1.8 MB，避免 localStorage 空间不足');els.settingsWallpaperFile.value='';return}const reader=new FileReader();reader.onload=()=>{appState.settings.wallpaperDataUrl=String(reader.result||'');save();applyAppearance();renderSettings();toastMsg('壁纸已保存到本地')};reader.readAsDataURL(f)});
  els.settingsWallpaperClear.addEventListener('click',()=>{appState.settings.wallpaperDataUrl='';save();applyAppearance();renderSettings();toastMsg('已清除自定义壁纸')});
  els.settingsTmdbLanguage.addEventListener('change',()=>{appState.settings.tmdbLanguage=els.settingsTmdbLanguage.value||'zh-CN';save()});els.settingsTestTmdb.addEventListener('click',testTmdbConnection);els.settingsSelfTestTmdb.addEventListener('click',runTmdbSelfTest);
  els.settingsExportJson.addEventListener('click',exportFullBackup);if(els.settingsCleanupLegacySeed)els.settingsCleanupLegacySeed.addEventListener('click',cleanupLegacySeedArtifacts);els.settingsExportCsv.addEventListener('click',exportMoviesCsv);
  els.settingsImportJson.addEventListener('change',async()=>{const f=els.settingsImportJson.files?.[0];if(!f)return;try{await importBackupFile(f)}catch(err){toastMsg('恢复失败：'+(err.message||'文件格式异常'))}els.settingsImportJson.value=''});
  els.settingsImportCsv.addEventListener('change',async()=>{const f=els.settingsImportCsv.files?.[0];if(!f)return;try{const n=importMoviesCsvText(await f.text());toastMsg(`已导入/更新 ${n} 部影视作品`)}catch(err){toastMsg('CSV 导入失败：'+(err.message||'格式异常'))}els.settingsImportCsv.value=''});
  els.settingsDoubanFiles.addEventListener('change',async()=>{renderDoubanFiles();await previewDoubanFiles()});
  els.settingsDoubanMode.addEventListener('change',async()=>{appState.settings.doubanMode=els.settingsDoubanMode.value;save();await previewDoubanFiles()});
  els.settingsDoubanAutoTmdb.addEventListener('click',()=>{els.settingsDoubanAutoTmdb.classList.toggle('on');appState.settings.doubanAutoTmdb=els.settingsDoubanAutoTmdb.classList.contains('on');save()});
  els.settingsDoubanSyncWatchDate.addEventListener('click',()=>{els.settingsDoubanSyncWatchDate.classList.toggle('on');appState.settings.doubanSyncWatchDate=els.settingsDoubanSyncWatchDate.classList.contains('on');save()});
  els.settingsDoubanImport.addEventListener('click',importDoubanFiles);
  els.settingsDoubanClear.addEventListener('click',()=>{els.settingsDoubanFiles.value='';renderDoubanFiles()});
  ['dragenter','dragover'].forEach(t=>els.settingsDoubanDrop.addEventListener(t,e=>{e.preventDefault();els.settingsDoubanDrop.classList.add('drag')}));['dragleave','drop'].forEach(t=>els.settingsDoubanDrop.addEventListener(t,e=>{e.preventDefault();els.settingsDoubanDrop.classList.remove('drag')}));
  els.settingsDoubanDrop.addEventListener('drop',async e=>{try{const dt=new DataTransfer();[...(e.dataTransfer?.files||[])].forEach(f=>dt.items.add(f));els.settingsDoubanFiles.files=dt.files;renderDoubanFiles();await previewDoubanFiles()}catch{toastMsg('当前浏览器不支持拖放赋值，请点击选择文件')}});
  if(els.settingsClearData)els.settingsClearData.addEventListener('click',()=>{if(!confirm('确定清空全部影视、观看记录、月度计划和电影雷达吗？\n\n这个操作不可撤销，建议先导出完整 JSON。'))return;if(!confirm('再次确认：真的要清空全部影视数据？'))return;appState.movies=[];appState.home=structuredClone(EMPTY_HOME);appState.tmdbMatchCenter={lastScanAt:'',lastTotal:0,lastAuto:0,lastManual:0,rows:[],resumeIds:[],paused:false};save();renderAll();renderSettings();toastMsg('全部影视数据已清空')});

  if(els.quoteNextBtn)els.quoteNextBtn.addEventListener('click',e=>{e.stopPropagation();chooseVerifiedQuote({avoidCurrent:true})});

  applyAppearance();
  applyProfile();
  applyBrand();
  chooseVerifiedQuote({avoidCurrent:true});
  renderAll();
  router.start();
})();
